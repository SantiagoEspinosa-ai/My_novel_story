# Fixture roto a proposito

Cita una ruta que no existe, con el typo que de verdad ocurrio en este
repositorio, y otra que si existe. VER-45 tiene que cazar la primera y no la
segunda.

El modelo de dominio esta en `Docs/defintions.md`, que es la ruta rota.
El modelo de dominio esta en `Docs/definitions.md`, que es la buena.
Y esto no es una ruta: `/escenas/{id}/generar`.
