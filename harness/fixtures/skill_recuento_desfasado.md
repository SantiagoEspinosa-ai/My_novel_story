# Fixture roto a proposito

Recuento de tipos desfasado: dice once y cinco donde el documento real dice doce
y cuatro. Sirve para demostrar que VER-38 caza que la skill se quedo atras.

**El reparto ya esta decidido**, en la columna `Tipo` de la tabla de invariantes.
No lo reinterpretes caso por caso: once son de regla, cinco son de juez.

De **obra**: `INV-06`, `INV-09`, `INV-11`, `INV-12`, `INV-13`, `INV-14` e `INV-16`.
De **capitulo**: `INV-08` e `INV-15`.
