"""Validadores que solo leen documentos de `Docs/`.

Se distinguen de `harness/invariantes/` a propósito: estos comparan
documentos entre sí y se pueden ejecutar sin que exista `backend/`;
aquellos comprobarán el comportamiento del código cuando lo haya.
"""
