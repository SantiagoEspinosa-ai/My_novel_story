\* OBSOLETO desde el 2026-09-24: modela la rama `main` (src/, EJECUCION.md), no
\* `backend/`. El modelo vigente es `HarnessBackend.tla`. Se conserva como historia
\* de CE-1..CE-5 (README.md, seccion "Historia").
\* Configuracion 1 de 2: EL SISTEMA TAL COMO ESTA HOY.
\*
\* Los tres interruptores llevan el valor que describe `EJECUCION.md`. Esta
\* ejecucion NO pasa, y ese es su proposito: cada violacion que TLC encuentra
\* aqui es un defecto real del harness, no un error de la especificacion.
\* Los contraejemplos estan transcritos en README.md.
\*
\*   java -cp tla2tools.jar tlc2.TLC -config CodigoDeHoy.cfg HarnessNovela.tla

SPECIFICATION Spec

CONSTANTS
    NumCapitulos = 5
    MaxIntentos = 2
    MaxCaidas = 1
    MaxRegeneraciones = 1
    ExigirValidacionCompleta = FALSE
    PublicarAlAgotarTope = TRUE
    ReanudarPorCursor = FALSE
    ConservarVersionAlRegenerar = FALSE

INVARIANTS
    TypeOK
    NuncaPublicaSinValidar
    NuncaPierdeCapitulos
    NoReescribeCerrados
    NoSaltaCapitulos
    ReintentosAcotados

PROPERTIES
    VersionesSoloCrecen
    Terminacion
