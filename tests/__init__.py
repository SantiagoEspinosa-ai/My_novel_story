"""Paquete de tests.

Vacio a proposito, igual que `src/__init__.py`. Marcar `tests/` como paquete
hace que pytest anada la raiz del proyecto al path de importacion, y por eso
los tests pueden hacer `from src.config import ...`.
"""
