# Verificación — My_novel_story

2026-09-22 · @Santiago Espinosa Domínguez

Cómo se prueba que **el sistema** hace lo que dice que hace. Este documento no
evalúa la novela que el sistema escribe: de eso se ocupan las invariantes
`INV-01`…`INV-16`, que aquí son el objeto verificado, no el sujeto.

Generado con la skill `verification-plan` (`.agents/skills/verification-plan/`) y
reorganizado según `docs/revisiones/REV-02 - Verification.md`, que añadió el eje
que faltaba: **el punto ciego de cada validador**.

## Qué se verifica aquí

| Pregunta | Quién la responde |
| --- | --- |
| ¿Es buena la novela? ¿Da miedo? ¿Se sostiene la continuidad? | Las invariantes `INV-01`…`INV-16` de `docs/definitions.md`, ejecutadas por el harness |
| ¿Están bien implementadas esas invariantes, el presupuesto de contexto, la máquina de estados y los agentes? | **Este documento** |

Dicho de otro modo: `INV-05` dice que ninguna escena se da por buena sin su
delta aplicado. Que esa regla exista es dominio. Que el código la cumpla de
verdad, y que falle cuando debe fallar, es lo que se planifica aquí.

---

# Modos de fallo

Este documento respondía a *cómo se verifica* y no a *qué puede salir mal*. Sin
esa lista, cada validador parece una buena idea suelta y no hay forma de saber si
el conjunto cubre algo. Aquí está el problema; después vienen los detectores.

## Las tres rejillas y qué aporta cada una

| Fuente | Qué aporta | Qué no |
| --- | --- | --- |
| **MAST** — *Why Do Multi-Agent LLM Systems Fail?*, arXiv 2503.13657. Catorce modos en tres categorías: diseño del sistema, desalineación entre agentes, verificación de tareas | La estructura de la sección y **prevalencias medidas**. Su crítica central —*los agentes verificadores hacen comprobaciones superficiales y hace falta verificación por capas*— es exactamente el eje de puntos ciegos de este documento | No sabe nada de narrativa. Ninguno de sus modos habla de continuidad ni de voz |
| **ConStory-Bench** — arXiv 2603.05890. Cinco categorías de error de consistencia narrativa con diecinueve subtipos: cronología y trama, caracterización, construcción de mundo, factual, estilo narrativo | El único inventario de fallos **del producto**, no del sistema. Y cuatro observaciones metodológicas que valen más que la taxonomía: los errores factuales y temporales dominan, se concentran **hacia la mitad** de la narración, aparecen en segmentos de **mayor entropía por token**, y ciertos tipos **co-ocurren** | No distingue si el error lo causó el modelo, el contexto o la orquestación, que es justo lo que a nosotros nos dice dónde poner el validador |
| **Fallos silenciosos de producción** — completado parcial, completado alucinado, aplicación errónea de acción, desbordamiento de contexto, desconexión entre razonamiento y acción, bucles infinitos | La propiedad que los une y que las otras dos no subrayan: **todos dejan los detectores en verde**. Es la clase de fallo contra la que un plan de verificación sirve de menos | No es una taxonomía publicada ni tiene prevalencias. Se usa como lista de comprobación, no como rejilla |

**Las prevalencias de MAST son de otros sistemas, no del nuestro.** Repetición de
pasos 15,7 %, no reconocer condiciones de terminación 12,4 %, desobedecer la
especificación 11,8 %, pérdida de historial 2,8 %, actuar fuera de rol 1,5 %.
Sirven para ordenar por dónde empezar a mirar, no para afirmar nada sobre esta
implementación: **aquí no se ha medido ninguna**, y no las hay porque no hay
sistema que medir. **Caduca con:** `backend/app/features/orquestacion/`.

ConStory-Bench mide **densidad de errores por diez mil palabras**. Es una métrica
que no tenemos y que conviene adoptar cuando haya texto: sin denominador, contar
hallazgos no dice si la obra mejora o solo se alarga. **Caduca con:** `backend/app/features/orquestacion/`, que es lo que produce texto.

## Cómo leer la tabla

Un modo entra **solo si se puede escribir cómo se manifestaría en una novela de
terror generada por este sistema**. *Alucinación* no es un modo de fallo, es una
categoría; *el delta declara un movimiento que el texto no narra* sí lo es.

**Un modo de fallo tiene tres destinos, no dos.** Los dos primeros se conocían: se **tapa**
con un validador, o se **asume** como punto ciego escrito. El tercero apareció al aplicar
`SPEC-14`: **deja de ser posible porque cambió lo que lo producía**. `MF-25` —el interbloqueo
del presupuesto— salía de cruzar la reserva de `P-2` con la exclusividad de `P-5`; al
retirar la reserva no quedó nada que retener, y el modo desapareció con su causa en vez de
taparse.

Y **lo destapó una prueba que dejó de tener sentido, no una relectura**:
`test_el_presupuesto_se_libera_tambien_cuando_falla` se quedó sin objeto al quitar la
reserva, y preguntarse qué la sustituía fue lo que llevó a ver que no la sustituía nada. Un
modo retirado se marca tachado y su identificador no se reutiliza, igual que uno cubierto:
lo que cambia es que no deja hueco detrás.

La columna de validador se deja **vacía cuando no hay ninguno**. Un hueco marcado
vale más que una fila rellenada por compromiso, y los huecos son el resultado
útil de esta sección.

### A · Diseño del sistema

| ID | En qué consiste | Cómo se manifestaría aquí | Detecta |
| --- | --- | --- | --- |
| **MF-01** | Desobedecer la especificación de la tarea (11,8 % en MAST) | La escaleta asigna a la escena 7 un `pov` en tercera limitada y `tiempo_verbal` pasado; el Escritor devuelve la escena en presente y con narrador omnisciente | `INV-04` solo comprueba que el POV **no cambie dentro** de la escena. **Nadie compara el POV y el tiempo verbal generados con los que la escaleta asignó** |
| **MF-02** | Actuar fuera del rol asignado (1,5 %) | El Juez, en vez de devolver puntuación y hallazgos, devuelve la escena reescrita "mejorada" | `VER-27` |
| **MF-03** | Repetición de pasos (15,7 %, el más prevalente) | La escena 3 gira entre `rechazada` y `generada` sin converger: el Escritor insiste en devolverla sin `cambio_de_valor` y nadie para el bucle | `VER-43` |
| **MF-04** | El mismo paso se ejecuta dos veces por reanudación | El worker muere después de llamar al modelo y antes de registrar el resultado; al reanudar vuelve a llamar, se paga dos veces y sale una escena distinta de la que ya existía | — `VER-04` comprueba que el trabajo **sobrevive**, no que se ejecute **una sola vez** |
| **MF-05** | Pérdida de historial (2,8 %) | El contexto no cabe y el recorte se lleva las fichas de entidad; el Escritor describe a Marta sin tener su ficha delante | **Prevenido para el caso grave**: el orden de `SPEC-01` §2.4 pone el registro de conocimiento en cuarta posición, así que `RF-26` hace fallar el trabajo antes de que el recorte llegue a él y deje a `INV-03` sin datos. Lo que sí se sigue perdiendo son fichas y setups, que es `PC-9` |
| **MF-06** | No reconocer las condiciones de terminación (12,4 %) | El Escaletador sigue planificando escenas después del clímax, o el Auditor no detecta que la obra ya cerró sus arcos | `INV-12` es de nivel obra y queda fuera de la v1 |

### B · Desalineación entre agentes

| ID | En qué consiste | Cómo se manifestaría aquí | Detecta |
| --- | --- | --- | --- |
| **MF-07** | Rellenar el hueco en vez de pedir aclaración | El recorte deja fuera la ficha de Marta; el Escritor le inventa unos rasgos físicos y se los atribuye. En el capítulo siete tendrá otros | — Nada falla en el momento. `INV-06` lo vería al final, y es de nivel obra |
| **MF-08** | Descarrilamiento de la tarea | Se pide una escena de transición con un `cambio_de_valor` previsto de conocimiento; el Escritor entrega el clímax, con un cambio de valor en el eje de vida | — **Nadie compara el cambio de valor previsto en la escaleta con el que declara el delta.** `INV-01` solo comprueba que no sea nulo |
| **MF-09** | Retención de información entre agentes | El Juez detecta un problema, lo menciona en su puntuación y no emite el `Hallazgo` correspondiente; la escena avanza sin rastro | — Una puntuación con la lista de hallazgos vacía es esquema válido. `VER-40` detecta que el Juez **no funciona**, no que se calle algo |
| **MF-10** | Ignorar la aportación de otro agente | El Consolidador aplica el delta con un hallazgo `bloqueante` abierto de la puerta anterior | `VER-10`, `VER-11` |
| **MF-11** | Desconexión entre razonamiento y acción | El texto narra que Marta sale de la casa por la ventana; el delta que acompaña no registra el movimiento. El estado del mundo la deja dentro y la escena siguiente la hace hablar en la cocina | `VER-39` **parcialmente**: caza el delta que habla de quien no aparece, no el que se calla lo que sí ocurre |

### C · Verificación de tareas

Es la categoría donde MAST más insiste, y la que este documento ya tenía como
eje: *"los agentes verificadores hacen comprobaciones superficiales"*.

| ID | En qué consiste | Cómo se manifestaría aquí | Detecta |
| --- | --- | --- | --- |
| **MF-12** | Terminación prematura | El trabajo se marca completado con la escena en `generada`, sin haber pasado la puerta | `VER-28` sobre el modelo declarado. Una ruta que escriba el estado directamente en la base no la ve nadie: es `PC-1` |
| **MF-13** | Verificación nula por clasificación errónea de la propia regla | `INV-01` está implementada como `mayor` en vez de `bloqueante`; la escena sin cambio de valor avanza y tres validadores están en verde | `VER-38` |
| **MF-14** | Verificación incorrecta por salida válida y vacía | El Juez devuelve una lista de hallazgos vacía por un timeout mal capturado, y se lee como "todo bien" | `VER-40` |

### D · Consistencia narrativa (ConStory-Bench)

Aquí el fallo es **del producto**, no del sistema, así que quien detecta es casi
siempre una invariante de `docs/definitions.md` y no una fila `VER`.

| ID | Categoría | Cómo se manifestaría aquí | Detecta |
| --- | --- | --- | --- |
| **MF-15** | Cronología y trama | La escena 12 narra el hallazgo del cuerpo; la escena 9, anterior en discurso y posterior en fábula, describe a la víctima viva sin que la analepsis esté declarada | `INV-08`, de nivel capítulo y fuera de la v1 |
| **MF-16** | Caracterización | Marta pierde sus muletillas y su sintaxis y pasa a hablar como los demás personajes a partir del capítulo cinco | — `INV-15` mide la deriva **global** frente a las anclas de estilo. **No hay ninguna invariante de voz por personaje**. **`SPEC-10` añadió `FraseRecurrente`**, que guarda la frase, desde qué capítulo y cuántas veces ha aparecido: es la señal que detecta la muletilla **entre** capítulos, donde `INV-15` no llega porque mide deriva global. Queda abierto si esa señal se convierte en invariante o se queda como material para el Revisor |
| **MF-17** | Construcción de mundo | La casa tenía una sola salida en la escena 4 y en la 11 aparece una puerta trasera que nadie plantó | — `EstadoDelMundo` guarda ubicaciones y posesiones, **no la topología de un lugar**. `INV-06` solo actúa si alguien lo declaró como `HechoCanonico` |
| **MF-18** | Factual | El texto dice que Marta coge el cuchillo y el delta registra que lo coge Luis | — `VER-39` no lo ve: los dos nombres aparecen en el texto. Es exactamente `PC-5` |
| **MF-19** | Estilo narrativo | La prosa se aplana hacia la media del modelo: frases de longitud uniforme, menos densidad sensorial, el registro de terror se diluye | `INV-15`, **cuando tenga umbral**. Hoy es `VER-32`, no verificable |

**Las cuatro observaciones metodológicas de ConStory-Bench son más útiles que su
taxonomía**, y ninguna está aprovechada todavía:

- **Los errores se concentran hacia la mitad de la narración.** Si hay que
  muestrear para una revisión cara, el centro rinde más que los extremos.
- **Aparecen en segmentos de mayor entropía por token.** Es una **señal lateral
  barata**: se puede calcular sin juez y sin leer, y apunta dónde mirar.
- **Ciertos tipos co-ocurren.** Encontrar uno es razón para buscar su pareja, no
  para dar la escena por revisada.
- **Los factuales y temporales dominan.** Son también los dos que más se prestan
  a comprobación determinista, que es una coincidencia afortunada.

### E · Fallos silenciosos de producción

Lo que los une: **todos dejan los detectores en verde**.

| ID | En qué consiste | Cómo se manifestaría aquí | Detecta |
| --- | --- | --- | --- |
| **MF-20** | Completado parcial | El Consolidador aplica tres de las cinco entradas del delta, falla en la cuarta sin transacción, y la escena queda `consolidada` con el estado a medias | `VER-42` |
| **MF-21** | Completado alucinado | El Resumidor devuelve un resumen bien formado que menciona un hecho que la escena no contiene. Ese resumen entra en el contexto de las escenas siguientes como si fuera canon | — `VER-39` compara el **delta** con el texto, no el **resumen** con la escena |
| **MF-22** | Desbordamiento de contexto | El contador del ensamblador dice 98.000 y el del proveedor 102.000; la llamada se rechaza, o peor, se trunca por el final sin avisar | `VER-05` con referencia externa (Regla 3) y `VER-41` |
| **MF-23** | **Falso positivo del validador** | `VER-46` marca como defecto la prosa de este documento que explica el defecto, porque no distingue una cita de una demostración | — Es el validador el que falla, y **ningún validador vigila a los validadores**. Ya ocurrió: ver "Lo que se aprendió al implementar", F-15 |
| **MF-24** | **Criterio de salida colgante**: un validador cuyo criterio remite a algo que no está escrito en ninguna parte. No falla, no avisa, y **cuenta como cobertura** | `VER-06` decía *"el orden de recorte respeta la prioridad declarada"* y esa prioridad no estaba declarada en ningún documento. `VER-23` dice *"el conjunto de identificadores solo crece"* sin decir **respecto a qué**, así que el criterio lo eligió quien lo implementó | — Ver `PC-13`. Es hermano de `MF-23` y su contrario exacto: aquel es el validador que marca lo correcto, este es el que **no puede marcar nada** |
| **MF-26** | **Verde heredado**: una comprobación que pasó contra un estado que ya no existe y que **nadie ha invalidado**. No es que falle: es que **dejó de significar lo que dice** | Se regenera la escena 7 y su delta nuevo mueve el estado del mundo. Las escenas 8 en adelante ya pasaron `INV-02`, `INV-03` e `INV-06` **contra el estado viejo**, y esos verdes siguen ahí, indistinguibles de los que sí valen. La obra se firma con la mitad de sus puertas evaluadas sobre una obra que ya no es esta | **`VER-115`** (`PLAN-23` A4), **solo para la reverificación**: una `Reverificacion` guarda la huella del estado contra el que se evaluó (`C-3`) y un verde con otra huella no cuenta. **`Hallazgo` sigue sin huella**: los verdes de la primera escritura no caducan solos. Ver `SPEC-23`: es la razón por la que la regeneración selectiva no es una función más |
| **MF-27** | **Artefacto de otro código**: lo produjo un proceso que ejecuta una versión del código **anterior a la del árbol**, y es indistinguible de uno producido por la de ahora | La obra de diez capítulos arrancó antes de que entrara la escritura de `uso_de_hecho`. Su proceso importó los módulos al lanzarse, así que lleva el código de entonces: **su base no tiene esa tabla aunque el fuente de la rama sí**. Una medida de arrastre sobre ella no da un número pequeño, da **cero filas**, y cero se lee como *no arrastra*. Es la familia de `MF-26` en otro eje: allí cambió el mundo bajo una comprobación, aquí cambió el código bajo un artefacto | **Cerrado por `commons/db/procedencia.py`** (`2791ac3`): la base registra el commit del árbol al crearse y `comprobar()` avisa si el proceso corre con otro — la contraparte de lo que la traza ya hacía con el modelo, que `VER-62` vigila. Tres decisiones dentro: la versión de creación **no se pisa** —la pregunta es con qué código se escribió lo que hay, no quién abrió la base el último—, lo indeterminable se dice `sin_determinar`, y **una base sin procedencia no es conforme**, porque leer esa ausencia como *coincide* sería el mismo silencio que esto viene a quitar. Lo midió y lo cerró la sesión de backend |
| **MF-28** | **Misma versión, otra configuración**: dos artefactos generados con el mismo commit y **distinta configuración** son indistinguibles entre sí | La configuración del harness sale a un fichero —topes, modelos, umbrales—. Dos obras generadas con el mismo código y dos ficheros distintos se comparan como si fueran comparables, y la diferencia que explique sus números **no está en ninguna parte**. Es `MF-27` por el otro eje: allí cambió el código bajo el artefacto, aquí cambian los parámetros con los que corrió | **Cerrado por `commons/db/procedencia.py`**, que acabó cubriendo **tres ejes y no dos**: `version_del_harness` —con qué **código** se escribieron las filas—, `huella_del_brief` —qué **novela** se pidió— y `huella_del_sistema` —con qué **máquina**: modelos, topes, techo—. Los tres con las mismas reglas: no se pisan, lo indeterminable se dice `sin_determinar`, y una ausencia devuelve aviso y no `None`. Las huellas se calculan sobre el modelo validado y con las claves ordenadas, así que reordenar un fichero no hace que dos configuraciones idénticas parezcan distintas. **El tercer eje lo añadió quien lo implementó, y es el que esta fila no había visto**: dos tandas de la misma novela con el mismo código dan números distintos si cambia el modelo, y sin registrarlo **se le atribuye a la obra lo que hizo la máquina** |
| **MF-29** | **El contrato se movió de un lado y el otro no se enteró**: el backend cambia la forma de una respuesta y el frontend sigue suponiendo la de antes, o al revés. No falla nada en ninguno de los dos lados: cada uno pasa sus pruebas contra su propia idea de la frontera | Se quita `dedicatoria` de la respuesta del índice. Las pruebas del backend pasan porque nadie la leía allí; las del frontend pasan porque sus fixtures la siguen trayendo. La portada sale sin dedicatoria en la web real y nadie lo ve hasta abrirla | `VER-102` (`PLAN-22` E1): `contrato/openapi.json` se deriva del backend con `harness/documentos/contrato.py`, y cualquier diferencia falla con operación, campo y dirección. **Punto ciego:** corre en local y no hay CI (`SPEC-22` `PCF-6`); un commit que no lo ejecute lo salta |
| ~~**MF-25**~~ | ~~Interbloqueo del presupuesto por un trabajo que no vuelve~~ | ~~El worker muere con una llamada del Escritor en vuelo y el techo se queda retenido~~ | **RETIRADO por `SPEC-14` C-1.** Salía de cruzar `P-2` —el presupuesto se libera al volver— con `P-5` —la llamada del Escritor es exclusiva—. Sin reserva **no hay nada que retener**, así que el interbloqueo desaparece con su causa. El identificador no se reutiliza. Lo sustituye nada: es un modo de fallo que **dejó de ser posible**, no uno que se tape |

## Estado de los nueve modos que no tenían cobertura

**Ninguno se queda sin estado.** Un modo no cubierto y no reconocido es el peor
de los tres estados posibles, así que cada uno acaba como fila `VER` o como punto
ciego asumido. El criterio: si admite comprobación determinista razonable, es
validador; si solo se puede mirar con un juez caro, es punto ciego.

| Modo | Estado | Con qué |
| --- | --- | --- |
| **MF-01** | **Validador** | `VER-47` compara el `pov_usado` declarado con el asignado; `VER-48` comprueba que el texto lo honre |
| **MF-04** | **Validador** | `VER-49`, idempotencia por intento registrado |
| **MF-07** | **Punto ciego** | `PC-9`: distinguir "lo sabía" de "se lo inventó" exige un juez. La traza registra qué fichas se recortaron, que acota el daño |
| **MF-08** | **Validador** | `VER-47` (b), desde que `SPEC-03` puso `cambio_de_valor` en el delta. Ver abajo |
| **MF-09** | **Validador** | `VER-50`, coherencia entre puntuación y hallazgos |
| **MF-16** | **Validador** | `VER-51`, deriva de voz por personaje |
| **MF-17** | **Validador** | `VER-54`, desde que `SPEC-03` convirtió los accesos de un `Lugar` en referencias. `PC-10` queda cerrado |
| **MF-21** | **Validador** | `VER-53`, desde que `SPEC-03` convirtió `hechos_clave` en identificadores. `PC-11` queda cerrado |
| **MF-23** | **Punto ciego** | `PC-12`, y no se resuelve con una capa que vigile validadores |

### `MF-08` resistió, y lo que lo destrabó fue cambiar el dominio

Durante un tiempo fue el único modo que no cabía ni en validador ni en punto
ciego, y el motivo no estaba en la verificación: **`DeltaDeEscena` declaraba
muertes, movimientos, revelaciones, setups pagados, cambios de posesión y
deterioros, y nada más.** Sin `cambio_de_valor` en el delta no había contra qué
comparar lo que la escaleta pidió.

`SPEC-03` lo resolvió por la raíz, y de paso dejó tres cosas que conviene
retener:

- **La persona narrativa y el tiempo verbal no fueron al delta.** El delta es el
  diff del mundo y la superficie del texto no cambia el mundo: `pov_usado` vive
  en `Borrador`, junto a `modelo` y `prompt_hash`.
- **La correspondencia entre ejes y campos del delta contrasta, no deriva.**
  Ningún campo del delta lleva signo y cuatro de los seis ejes solo tienen campo
  para uno, así que el eje se declara y la tabla lo comprueba. Si se pudiera
  derivar, la comprobación sería el propio dato mirándose al espejo: la Regla 3
  otra vez.
- **Quedaron dos huecos del modelo escritos en `docs/definitions.md`**:
  sobrevivir no deja rastro, y olvidar o descubrir que lo que sabías era falso no
  tiene campo. Son cosas que la novela puede hacer y el sistema no puede
  representar.

### Los tres parciales que conviene no dar por cubiertos

**MF-05** se detecta tarde y con la llamada ya pagada. **MF-11** y **MF-18** los
estrecha `VER-39` sin cerrarlos, que es lo que dice `PC-5`.

## Lo que tenemos y las taxonomías publicadas no

Cuatro modos que ninguna de las tres rejillas recoge, y salen de decisiones de
arquitectura propias:

1. **El delta como fuente de verdad divergiendo del texto** (`MF-11`, `MF-18`).
   MAST tiene *desconexión entre razonamiento y acción*, pero ahí el desajuste
   muere con el turno. Aquí el delta **se consolida en el estado del mundo** y
   contamina todas las escenas siguientes. Es la diferencia entre un error y un
   error que hereda.
2. **El recorte de contexto como causa de un fallo de dominio** (`MF-05`). MAST
   tiene *pérdida de historial*, pero como accidente. Aquí el recorte es una
   **decisión de diseño con un orden de prioridad declarado**, así que el fallo
   es predecible y atribuible a una regla concreta.
3. **La clasificación de la propia regla como modo de fallo** (`MF-13`). MAST
   habla de verificación incorrecta; esto es anterior: la regla está bien
   ejecutada y **mal etiquetada**.
4. **El falso positivo del validador** (`MF-23`). Las tres rejillas miran fallos
   del sistema que genera. Ninguna mira fallos de la capa que verifica, y esa
   capa también falla: aquí ya lo hizo.

## Relación con los puntos ciegos

**Un punto ciego es un modo de fallo que sabemos que no vemos.** Las dos listas
no se duplican: `MF-xx` dice **qué puede salir mal**; `PC-xx` dice **qué parte de
eso hemos decidido no mirar, y por qué**. Se apuntan entre sí:

| Punto ciego | Modo de fallo al que corresponde |
| --- | --- |
| `PC-1` — el análisis estático no ve la ejecución | `MF-12`: la terminación prematura por una ruta que escribe el estado a mano |
| `PC-2` — nadie comprueba que quien acepta sea una persona | No tiene `MF` propio porque no es un fallo del sistema sino de su uso. **Es el único `PC` sin modo asociado** |
| `PC-3` — la fiabilidad del Juez no está medida | `MF-09` y `MF-14`: lo que el Juez calla y lo que devuelve vacío |
| `PC-4` — los resúmenes pueden crecer hasta reconstruir la obra | Adyacente a `MF-21`: el mismo agente, un fallo de tamaño en vez de contenido |
| `PC-5` — la comprobación de menciones es léxica, no semántica | `MF-11` y `MF-18`, que son su enunciado en positivo |
| `PC-6` — el identificador del hallazgo existe pero puede ser el equivocado | Adyacente a `MF-13`: los dos son metadatos de la regla, no su ejecución |
| `PC-7` — la migración existe pero puede estar vacía | Sin `MF`: es un fallo del proceso de cambio, no de la ejecución del sistema |
| `PC-8` — el contador se valida contra el proveedor, no contra la verdad | `MF-22` |
| `PC-9` — nadie ve la invención que rellena un recorte | `MF-07` |
| ~~`PC-10`~~ — cerrado por `SPEC-03` | `MF-17`, ahora cubierto por `VER-54` |
| ~~`PC-11`~~ — cerrado por `SPEC-03` | `MF-21`, ahora cubierto por `VER-53` |
| `PC-12` — nadie vigila a los validadores | `MF-23` |
| `PC-13` — un criterio puede remitir a algo que no existe | `MF-24`, **materializado**: ver `F-16` |

Tres lecturas útiles de esa tabla. **`PC-2` y `PC-7` no tienen modo de fallo
asociado** porque no son fallos del sistema en marcha: uno es de uso y otro de
proceso. **Los cuatro puntos ciegos nuevos sí lo tienen**, porque nacieron
precisamente de cerrar modos que no estaban ni cubiertos ni reconocidos. Y **ya
no queda ningún modo sin estado**: los veintitrés acaban en validador o en punto
ciego, salvo el núcleo de `MF-08`, que está bloqueado por una decisión de
dominio y consta como tal.

## Relación con lo que se aprendió al implementar

Varios de los quince hallazgos son **instancias concretas de modos genéricos**, y
conviene que se apunten entre sí en vez de leerse como anécdotas:

| Hallazgo | Modo del que es un caso |
| --- | --- |
| `F-15` — los validadores prohíben citar el defecto | `MF-23`, y es su única instancia observada |
| `F-1` — `VER-38` no puede contrastar la severidad | `MF-13`: la clasificación de la regla es verificable a medias |
| `F-8` — `VER-23` se salta en vez de fallar cuando no hay historial | `MF-14`: un validador que aparece en verde sin haber comprobado nada es verificación incorrecta, y aquí el que la sufre es el propio harness |
| `F-12` — `VER-44` estaba quemado y `VER-23` no lo habría cazado | `MF-14` otra vez, en su forma más incómoda: el validador declaró su punto ciego y el punto ciego se materializó |
| `F-9` — no se pudo comprobar que el nivel del agente cuadre con el de la invariante | `MF-06`: el Auditor no sabe cuándo le toca, porque nadie declara su nivel |
| `F-13` — la columna "Dónde vive" es un plan y el documento no lo dice | `MF-01` aplicado a la documentación: el documento incumple su propia especificación implícita |

---

## Cómo se lee este documento

El modelo no garantiza que sus resultados sean correctos, así que la fiabilidad
no sale de él: sale de los validadores que lo rodean. Y **un validador aislado no
significa nada**, porque cada uno tiene un punto ciego por construcción. Lo que
importa es si esos puntos ciegos se solapan o se cubren entre sí.

De ahí las tres reglas que gobiernan este documento:

### Regla 1 — Todo validador declara su punto ciego

La columna **Punto ciego** no es documentación: es la columna que decide si un
validador vale. Un validador sin punto ciego declarado es un validador que no se
ha entendido.

### Regla 2 — Un validador nuevo entra solo si su punto ciego es distinto

Si el punto ciego de un validador nuevo coincide con el de uno que ya está, es
**cobertura falsa**: parece que se cubre más y no se cubre nada nuevo. El caso de
libro son los validadores ciegos a la ejecución: hay **doce**, los que lista
`PC-1`, y comparten el mismo límite, así que el decimotercero no taparía nada.
Un validador que no puede
justificar un punto ciego propio no entra, y el hueco que iba a tapar se anota en
"Puntos ciegos asumidos".

**Y su reverso, que costó una novela descubrir.** La Regla 2 dice que dos
validadores con el mismo punto ciego no suman. La otra mitad es que **repartir
toda la auditoría entre validadores de juicio deja fuera lo que ninguno mira**, y
el hueco no se ve hasta que pasa.

Ocurrió en la rama `main`, generando la primera novela de verdad —terror, tres
capítulos, rango configurado de 1.200 a 2.200 palabras—. El capítulo 1 dio esta
secuencia:

| Intento | Palabras | Continuidad | Género | Estilo |
| --- | --- | --- | --- | --- |
| 1 | 1.574 | PASA | FALLO: *"el capítulo es largo para el género, condensa"* | FALLO |
| 2 | **944** | PASA | **PASA** | FALLO |

El escritor obedeció la pega de longitud y sobrecorrigió hasta **944 palabras,
256 por debajo del mínimo configurado**. El validador de género, que en el intento
anterior había pedido acortar, **aprobó la nueva longitud sin decir nada**. Los
otros dos tampoco la mencionaron: no es su trabajo.

El diagnóstico de `DECISIONES.md` es el que importa: *"el spec repartió toda la
auditoría entre tres validadores y dio por hecho que un capítulo fuera de rango lo
cazaría el de género. Es una suposición razonable y resultó ser falsa: el
validador de género juzga **ritmo**, y un capítulo corto puede tener buen ritmo."*

Si ese capítulo hubiera acabado aceptado por puntuación, **habría entrado corto en
el manuscrito con los tres validadores conformes**. La longitud es un número, y
pedirle a un juez que compruebe un número es lo que dejó el hueco. Es la misma
lección que la Regla 3 por otra puerta: lo que se puede contar, se cuenta.

### Regla 4 — Una violación de contrato se detecta en la frontera, no en la puerta

**Si llega a la puerta, la puerta juzga lo que puede y se equivoca de diagnóstico.**

Se midió en la primera generación real. El delta volvió con frases donde el dominio quiere
identificadores —`{"sujeto": "casa familiar", "hecho": "el reloj de pared funciona sin que
nadie le haya dado cuerda"}`— y `INV-03` levantó tres `bloqueante` diciendo que **un
personaje actúa sobre un hecho que no conoce**. Es lo único que esa invariante sabe decir, y
la causa era otra: el delta no usaba referencias.

Las consecuencias se acumulan:

- **El hallazgo describe mal el defecto**, así que quien lo lea busca donde no es.
- **Hereda la severidad de la puerta**, no la del defecto real. Un fallo de formato pasa a
  ser `bloqueante`.
- Y como una `bloqueante` **no admite rendición**, eso **detiene la novela** por una causa
  que el hallazgo no nombra.

**Es la misma lección que el bug de `is`** (`F-19`): un dato que entra al dominio sin pasar
por su frontera hace que algo más adentro falle o acierte por accidente. `SPEC-03` decidió
*"referencias, no prosa"* para el dominio; la frontera que lo hace cumplir es el **contrato**,
no la invariante.

**Y el contrato no basta solo.** Si el prompt no dice qué identificadores existen, se está
pidiendo lo imposible y el rechazo es merecido pero inútil. Las dos mitades: el prompt lleva
los identificadores disponibles y el contrato comprueba que la respuesta los use, porque
**un prompt bien construido no garantiza una respuesta bien formada.**

### Regla 3 — Un validador no comparte implementación con lo que valida

**Si el validador usa la misma pieza que la cosa validada, no está verificando:
está haciendo eco.** Un contador de tokens que se comprueba consigo mismo siempre
da que sí. Un aplicador de deltas comparado con otra ejecución de sí mismo
siempre coincide, incluso cuando los dos están mal.

Esto no es el arreglo de dos filas concretas: es una condición de admisión. Al
escribir o revisar un validador hay que responder **de dónde sale su referencia**,
y la respuesta no puede ser "de la misma función". Las referencias válidas son
tres:

1. **Una fuente externa al sistema** —lo que devuelve el proveedor del modelo, lo
   que dice el documento normativo—.
2. **Una implementación de referencia escrita solo para la prueba**,
   deliberadamente ingenua: lenta, sin optimizar y fácil de leer. Si la de
   producción y la ingenua coinciden, el acuerdo significa algo.
3. **Un dato fijado a mano** en el caso de prueba.

Las dos filas que incumplían esta regla, `VER-05` y `VER-09`, están corregidas:
ver la nota bajo la tabla de nivel artefacto.

### Regla 5 — Un contrato que no dice qué significan sus campos no es un contrato

**Fija una forma y deja el significado a la intuición de quien responde. Cuando las dos
intuiciones coinciden, funciona y nadie se entera; cuando divergen, las dos partes cumplen el
contrato y el sistema está roto**, sin error, sin excepción y sin nada que falle.

Es la clase de fallo que explica por qué `F-31` sobrevivió tanto. El prompt le daba al modelo
la **forma** de `revelaciones` —`[{"sujeto": ..., "hecho": ...}]`— y nunca su **significado**.
El modelo dedujo lo único que puede deducir cualquiera de la palabra *revelación*: el momento
en que alguien se entera. El código de la puerta suponía lo contrario: que el sujeto ya lo
sabía y estaba obrando. **Las dos partes cumplían el contrato** —identificadores bien
formados, esquema correcto, nada que rechazar— y el sistema estaba muerto: ningún personaje
podía llegar a saber nada nunca.

No se detectó antes porque **estaba tapado por otro fallo**: sin hechos declarados no había
revelaciones que comprobar, y sin revelaciones la contradicción no llegaba a manifestarse
(`F-29`). Un fallo que no produce error y además necesita que otro se arregle antes de poder
aparecer.

Es la **tercera mitad de la Regla 4**, que ya exigía dos: el prompt lleva los identificadores
disponibles y el contrato comprueba que la respuesta los use. Faltaba que el prompt dijera
**qué se está pidiendo con cada uno**. Un identificador bien formado en el campo equivocado
pasa las dos primeras mitades sin despeinarse.

### Regla 6 — Dos actores sobre un estado compartido pierden trabajo en la ventana entre dos operaciones

**Basta con esto: un estado que dos actores tocan, una operación que en realidad son dos
—preparar y confirmar, leer y escribir, apuntar y avanzar—, y un segundo actor que actúa
entre las dos. El trabajo se pierde y no falla nada**, porque cada operación por separado
terminó bien. No hay excepción que capturar ni código de error que mirar: el sistema queda
en un estado válido que no es el que nadie quiso.

**Esta regla no es del dominio de las novelas.** Se ha manifestado tres veces en este
proyecto, en tres sustratos que no tienen nada que ver entre sí, y las tres veces con la
misma forma:

| Dónde | El estado compartido | La ventana | Qué se perdió |
| --- | --- | --- | --- |
| `F-38`, relanzar tras un bloqueo | El progreso de la obra | Entre decidir por dónde seguir y mirar en qué estado está cada escena | Se regeneró lo ya hecho, se pagó otra vez y **se degradó `consolidada` a `generada`** |
| `CE-3` y `CE-4` de `specs/tla/`, reanudar por cursor | El cursor "iba por el capítulo N" | Entre escribir el capítulo y actualizar el cursor | Con el cursor por detrás se reescribe lo hecho; por delante se salta lo que falta. TLC encontró además una ejecución que **no termina nunca** |
| `F-44`, el índice de Git | El índice, compartido por dos sesiones en el mismo directorio | Entre `git add` y `git commit` | Los cambios preparados se fueron **en el commit de la otra sesión**, con su mensaje y su atribución |

Lo que las tres tienen en común no es el dominio: es la **forma**. Y por eso los tres
remedios posibles son siempre los mismos tres, y conviene saber cuál se está eligiendo:

1. **Hacerlo atómico.** Que las dos operaciones sean una. Es lo que consigue
   `git add <ruta> && git commit -- <ruta>` en un solo comando: la ventana pasa de minutos
   a milisegundos, aunque no desaparece.
2. **No recordar el estado: re-derivarlo.** Si lo que toca hacer se deduce mirando lo que
   hay guardado, no existe nada que pueda quedarse desfasado. Es lo que hace
   `siguiente_paso()` en la rama `main` y lo que `docs/architecture.md` exige ahora en la
   máquina de estados del trabajo. **Es el remedio más fuerte de los tres**, porque elimina
   la ventana en vez de estrecharla.
3. **Dejar de compartir el estado.** Un worktree por sesión, cada uno con su índice. Cierra
   la clase entera de problemas para ese estado concreto y no hace nada por los demás:
   `AGENTS.md` lo dice en una frase que conviene no olvidar, **«un worktree separa
   carpetas, no costumbres»**.

**Y juntar los dos comandos tampoco basta** (`F-48`). `git add <ruta> && git commit -- <ruta>`
en una sola línea protege **del comando que no se lanza, no del que se lanza y falla a
mitad**. Si el `add` funciona y el `commit` aborta —una comilla mal puesta, un `-m` después
del `--`—, el índice **queda cargado durante toda la depuración del error**, que es la
ventana más larga de todas y además la que menos se vigila, porque uno está mirando el
mensaje de error y no el estado del repositorio.

**El remedio real es comprobar que el índice está limpio antes de empezar y después de
cualquier error**, no juntar comandos. Es decir: `git status` antes de preparar nada, y otra
vez en cuanto un comando de git falle, antes de ponerse a corregir la sintaxis. Ayuda además
pasar el mensaje por la entrada estándar —`git commit -F -` con un heredoc—, porque quita de
en medio los errores de comillas y de orden de argumentos, que son los que abren la ventana.

**Y la regla ya ha evitado un daño, que es la primera vez.** Se escribió después de dos
incidentes —`F-44` y `F-48`, las dos veces perdiendo el porqué de un trabajo— y a la tercera
sirvió para **no** llevarse algo. Al ir a commitear un cambio propio en
`obra_diez_capitulos.py`, mirar el índice antes de preparar enseñó que el mismo fichero
llevaba encima un cambio de otra sesión a medias: una llamada a
`procedencia.registrar(..., sistema=...)` cuya firma todavía no estaba commiteada.
Commitearlo habría dejado **el árbol roto para todos**, no solo mal atribuido. Se commitearon
por separado los ficheros enteramente propios y el compartido lo commiteó después la sesión
dueña del código del que dependía.

Conviene dejarlo escrito porque este documento cataloga fallos y **una regla que funciona no
deja rastro en ninguna parte**: sin esta nota, la única evidencia sobre la disciplina del
índice serían las dos veces que faltó. Y el matiz que la hace utilizable es que el daño evitado
era **de otra clase** que el que la motivó: no perder la autoría, sino no publicar código que
no compila.

**La consecuencia para verificar:** un fallo de esta familia **no se caza con pruebas de
caso feliz ni mirando código**, porque cada operación aislada es correcta. Se caza
explorando entrelazados —que es para lo que sirve un *model checker*, y por lo que
`specs/tla/` modela las dos reanudaciones en vez de solo la buena— o no se caza. Es el
mismo límite que `PC-1` por otro eje: **lo que solo existe entre dos pasos no lo ve nada
que mire un paso.**

---

### Regla 7 — Una clave que omite el campo que distingue dos filas no falla: pisa

**Basta con esto: dos filas que no significan lo mismo, y una clave de unicidad que deja
fuera justo el campo por el que se diferencian.** La segunda escritura no da error: sustituye
a la primera, la tabla se queda con una fila perfectamente creíble y **cuál sobrevive depende
del orden de escritura**. No hay excepción que capturar, no hay recuento que se descuadre, y
lo que desaparece no deja hueco.

**Es la hermana de la Regla 6 y no es la misma.** Allí hay dos actores y una ventana entre
dos operaciones; aquí basta un actor y una clave mal elegida. Lo que comparten es el desenlace,
y es lo que las hace difíciles: el sistema queda en un estado válido que no es el que nadie
quiso.

**Se ha manifestado tres veces, y las tres sobre el campo que guardaba la procedencia del
dato.** Esa coincidencia no es casual: los campos que dicen *de quién es esto* o *quién lo
afirma* se añaden después, cuando la clave ya está escrita, y casi nunca se revisa la clave al
añadirlos.

| Dónde | Las dos filas que no cabían | El campo que faltaba en la clave | Qué se perdió |
| --- | --- | --- | --- |
| `F-39`, `hecho_canonico` | El mismo identificador de hecho en dos obras | `obra` | Los nueve capítulos anteriores se quedaron con la lista de hechos vacía |
| `F-42`, `uso_de_hecho` | El mismo `depende` observado y declarado | `origen` | La distinción entre dato medido y afirmación no verificada, que era para lo que existía la columna |
| `hallazgos_abiertos` | Un `mayor` como cadena y como miembro de su enumeración | — (la frontera de deserialización) | Un `mayor` leído de la base no bloqueaba el cierre de capítulo |

**La pregunta que la cierra, al escribir cualquier clave:** *¿qué dos filas distintas podrían
colisionar aquí, y hay algún campo que diga de dónde viene el dato?* Si lo hay, casi siempre
pertenece a la clave. **Y la corrección se demuestra escribiendo las dos filas y enseñando que
antes sobrevivía una**: sin ese caso negativo, «he ampliado la clave» no se distingue de un
cambio cosmético.

### Regla 8 — Una rama que no se ejecuta no es una comprobación que pasa

**Y desde fuera se ven igual.** Un informe que dice *"sin hallazgos"* no distingue entre *se
comprobó y está bien* y *no se llegó a comprobar*, y la segunda es la que deja pasar el
defecto que el validador existía para cazar.

Es la familia más numerosa del proyecto y siempre aparece con la misma forma: **un guardia
que nadie pensó como decisión**.

| Dónde | La guarda | Qué no se ejecutó | Cómo se vio desde fuera |
| --- | --- | --- | --- |
| `F-34`, `INV-04` | `if escena.get("pov_usado")` | La comparación del POV planificado con el usado | Verde. El modelo escribió la escena sobre otro personaje y nadie lo marcó |
| `F-34`, `INV-02` | `if hasta and desde` | La mitad de accesibilidad | Verde, en todas las escenas de todas las ejecuciones |
| `F-34`, `INV-17` | `palabras is not None` | El contraste contra el rango | Verde |
| `F-41`, el resumen | `if c.resumen:` | Guardar el resumen de la escena | La escena se dio por buena; cinco de doce se quedaron sin memoria |
| `F-30`, `INV-03` | — (no había revelaciones que mirar) | La invariante entera, en seis escenas | *"`INV-03` bloquea 0 de 6"*, que se lee como una medida y no lo era |

| La escena anterior | — (el filtro por capítulo, al acotarlo) | Nada: se ejecutó y devolvió vacío | Cadena vacía. La escena 1 de un capítulo **debe** recibir vacío y la 4 nunca; las dos se veían igual |
| `uso_de_hecho`, `contradice` | — (nadie deduce ese tipo) | Nada: la consulta corre entera | Lista vacía, que se lee como *"no hay contradicciones"* y significa *"nadie ha buscado"* |
| `edades()` | — (la fecha de nacimiento es opcional) | La comparación, para quien no la tenga | `incoherentes: []`, que se lee como *"las edades cuadran"* |

> **De las tres, la tercera es la peor. Las otras dos callan; ésta habla y dice otra cosa, y
> eso cuesta más que un silencio porque manda a depurar al sitio equivocado.**

**Y hay una tercera, que es la peor: el error que ni siquiera dice qué se rompió.** Un
parámetro llamado `capitulo` tapó el módulo `capitulo` que su fichero importaba, así que dentro
de esa función `capitulo.cerrar` dejó de ser el módulo y pasó a ser el argumento —`None`—. El
fallo salió **al evaluar la línea `except capitulo.NoSePuedeCerrar`**, de modo que el mensaje
no mencionaba ni el módulo tapado ni la función que lo tapaba: veintinueve pruebas rojas
diciendo `'NoneType' object has no attribute 'NoSePuedeCerrar'`, que no es lo que pasaba.

Las dos formas anteriores callan; ésta habla y **dice otra cosa**, que cuesta más que el
silencio porque manda a quien depura al sitio equivocado. Se cierra con un comprobador
—`app/commons/dominio/tests/test_nombres_que_tapan.py`—, y no con una revisión, porque el
nombre natural del parámetro es el mismo que el del módulo y quien escriba la siguiente
función volverá a elegirlo. En revisión no se ve: **las dos líneas son correctas por separado
y solo chocan al compartir ámbito.** Recorrido el árbol entero: ningún otro caso, y el
comprobador demuestra que sabe encontrar el que lo motivó.

**Hay una segunda forma, y no es una guarda: es un valor de vuelta ambiguo.** Las tres filas
de arriba no se saltan ninguna rama —el código corre entero— y aun así producen el mismo
engaño, porque **el valor que significa *«correctamente vacío»* y el que significa *«no se
pudo»* son el mismo valor**. Ahí no hay ningún `if` que revisar, así que la pregunta de la
guarda no las caza.

**La pregunta que sí las caza:** *¿este vacío tiene dos lecturas?* Si un valor legítimo y un
fallo producen la misma salida, hace falta un segundo campo que los separe —`falta_escena_anterior`,
`sin_fecha_de_nacimiento`, `cobertura_de_tipos()`— y no basta con documentarlo, porque quien
lee el resultado no lee la documentación.

**La corrección no es quitar la guarda**, porque muchas son legítimas: una escena sin
`longitud_objetivo` no tiene rango que comprobar. La corrección es **distinguir los dos
casos y decir el segundo**: si el dato falta y debería estar, se emite un `sin_veredicto`
—que el dominio ya tenía para esto— o se deja constancia como `sin_resumen`. Lo que no vale
es que la ausencia se vea igual que el acierto.

**La pregunta que la cierra, al escribir cualquier `if` que envuelva una comprobación:**
*¿qué pasa si esta condición es falsa, y quien lea el resultado podrá saber que no se
comprobó?* Si la respuesta es que no, falta un caso, no una guarda.

**Y la pregunta hermana, para los datos:** un cero no es lo mismo que un hueco. *"Bloqueó
cero veces"* y *"no llegó a mirar"* son cosas distintas y el segundo no se apunta como cero
(`F-30`, y la razón de que `VER-64` estuviera tres ejecuciones sin dato utilizable).

**Y la instancia que más caro sale: la Regla 8 dentro del informe.** La sección
`=== EL CANON ===` del guion de generación ha salido **en blanco tres veces, por tres causas
distintas**: `F-39`, la clave global de `HechoCanonico`, que atribuyó los diez hechos al
último capítulo y dejó vacíos los nueve anteriores; el arreglo incompleto que la dejó
apuntando al capítulo equivocado; y la consulta que pedía los hechos de la obra pasando un
identificador de capítulo, encontrada antes de la tercera ejecución.

Tres veces la misma sección muda por tres motivos que no se parecen **ya no es mala suerte**:
es que esa sección **no tiene forma de distinguir «vacío porque no hubo» de «vacío porque
pregunté mal»**. Y pesa más que en cualquier otro sitio, porque **el informe es lo que se usa
para juzgar todo lo demás**: un validador mudo deja un hueco en una tabla; un informe mudo
deja una obra entera dada por buena o por mala sin nada detrás.

La corrección tiene la forma de siempre y no es un arreglo por instancia: **una respuesta
vacía tiene que llevar consigo la pregunta que se hizo.** *«0 hechos»* no dice nada; *«0 de
los 10 hechos declarados para `obra-la-casa`»* distingue las dos situaciones sin que nadie
tenga que acordarse de sospechar. Mientras el informe imprima cantidades sin su ámbito,
seguirá habiendo una cuarta causa.

**Y a escala de agente: un agente que nunca se ejecuta no es un agente que funciona.** `F-76` y
`F-77` son la misma familia. El Editor y el Resumidor llevaban semanas **escritos, probados y
contados como hechos**, y nunca habían corrido con datos reales. La primera ejecución real que
llegó hasta ellos (`R0`, `harness/evals/medidas.md`) terminó con **código de salida 0** y un
capítulo aceptado. Por dentro, el Editor no había juzgado nada, porque su esquema rechazaba el
campo `medidas` que añade el transporte, y el Resumidor no había resumido, porque su prompt no le
daba la lista que su definición le exige. Los dos habían pasado todas sus pruebas: los dobles no
añadían `medidas` y no leían definiciones de agente. **El código 0 escondía los dos**, igual que un
cero escondía que `INV-03` no llegó a mirar (`F-30`).

La consecuencia práctica es la de siempre, a otra escala. **Un agente cuenta como hecho cuando ha
corrido con datos reales y se ha mirado lo que devolvió**, no cuando pasa sus pruebas. Y tras una
ejecución real, el código de salida no es la respuesta: hay que leer en la base qué juzgó cada
agente (`.agents/skills/novela-regalo/SKILL.md` § "Inspeccionar").

### Regla 9 — Una comprobación que depende de un orden necesita al menos dos elementos que lo ejerzan

**Con uno solo, puede pasar por un empate que se resolvió al azar, y entonces está en verde
sin distinguir nada.**

Apareció dos veces, en sitios que no se parecen:

| Dónde | La prueba | Por qué pasaba sin comprobar |
| --- | --- | --- |
| `F-40`, la escena anterior | Una escena de otra obra con **un** borrador | `ORDER BY version DESC LIMIT 1` con un solo candidato por lado: el empate lo resolvía el rowid, y salió a favor. Con **dos** borradores el `ORDER BY` elige de verdad y el fallo aparece siempre |
| `CE-5` de TLA+ | `VersionesSoloCrecen` sobre dos versiones | Las dos versiones tenían los mismos capítulos, así que **eran el mismo valor**: la propiedad estaba en verde por no poder distinguir nada |

La forma general: **si la comprobación depende de un orden o de una desambiguación, hace
falta al menos dos elementos que la ejerzan**. Uno la deja pasar por coincidencia, y una
prueba que pasa por coincidencia es peor que no tenerla, porque se cuenta como cobertura.

Es la Regla 8 en el otro lado del espejo: allí una rama no se ejecutaba, aquí se ejecuta y no
decide nada. Las dos se ven igual desde fuera: **verde**.

### Regla 10 — Un comentario que afirma lo que la línea de debajo no cumple es peor que no tener comentario

**Porque quien revisa lee el comentario y sigue.**

El caso: al acotar la memoria por obra quedó escrito *"Acotados a la obra"* justo encima de
tres consultas, y **solo dos lo estaban**. La tercera —la de la escena anterior, la que más
pesa— se leyó como cubierta durante dos revisiones, la mía al escribirla y la siguiente. Un
comentario correcto habría dicho *"acotados a la obra"* sobre las dos y nada sobre la
tercera, y entonces la tercera habría destacado.

No es una regla de estilo: **un comentario es una afirmación sobre el código, y una
afirmación falsa junto al código es una de las pocas cosas que sobreviven a un `grep`**. La
prosa que describe lo que el código debería hacer se escribe como lo que hace, o no se
escribe.

### Regla 11 — Una prueba que pasa por coincidencia es indistinguible de una que pasa por corrección

**Y solo se ve cuando la coincidencia desaparece.**

El caso: el endpoint de cierre de capítulo pedía sus escenas con una consulta que filtra
**por obra**, pasándole un identificador de **capítulo**. Estuvo mal desde que se escribió y
sus dos pruebas estuvieron en verde desde que se escribieron, porque el guion de generación
creaba **una obra por capítulo** y los dos identificadores eran la misma cadena. La consulta
equivocada devolvía el resultado correcto.

No lo destapó una revisión ni un validador: lo destapó **cambiar el guion** a una obra con
diez capítulos dentro. En cuanto los dos identificadores dejaron de coincidir, la consulta
devolvió cero escenas y el endpoint empezó a contestar *«no existe»* sobre capítulos que
existen. Las dos pruebas que llevaban años en verde fallaron **el mismo día**, y fallaron por
lo que nunca habían comprobado.

Lo que la hace distinta de la `Regla 9`: allí falta un segundo elemento que ejerza la
distinción; aquí **los dos elementos existen y valen lo mismo**. Un caso de prueba con dos
escenas no basta si las dos están en la misma obra y esa obra es el capítulo.

Dos consecuencias prácticas:

1. **Un dato de prueba que hace coincidir dos identificadores distintos apaga todas las
   comprobaciones que los distinguen.** Dicho del derecho: **una suite donde `obra` y
   `capitulo` son la misma cadena no puede detectar que se confunden.** No hay revisión que
   lo encuentre ni validador que lo cace, porque desde dentro el código que se mira sí tiene
   su filtro. Por eso los identificadores de un fixture se eligen **distintos a propósito**,
   y distintos entre sí, no solo distintos de los de al lado.

   **Y los que ya existen se cambian al tocarlos, no todos de golpe.** Hoy hay una docena de
   fixtures que usan `"cap-1"` como identificador de obra. No están rotos y una migración
   masiva solo añadiría riesgo sin cerrar nada; lo que hay que evitar es que el terreno
   vuelva a crecer, así que cada uno se corrige la próxima vez que alguien entre en él por
   otro motivo.
2. **Cuando una suposición de datos cambia —un guion, un esquema, un formato— hay que ir a
   buscar lo que la coincidencia estaba tapando**, en vez de esperar a que aparezca. Aquí,
   buscar todas las consultas que reciben un capítulo y filtran por obra encontró dos
   instancias más: la del endpoint y una en el propio guion, que pedía los hechos de la obra
   pasando el primer capítulo y habría impreso el canon vacío en el informe final.

### Regla 12 — Un identificador se lee antes de enviarlo

**Un dato con la forma correcta es más peligroso que uno ausente, porque nadie lo duda.**

El caso, repetido: un hash de commit escrito de memoria en un mensaje a otra sesión
(`1e4e6b6`, cuando el real era otro), un hash citado en un informe sin haberlo leído, y
«commit 4a…» en otro mensaje, redactado **antes** de que el commit existiera. Ninguno
falló al escribirse: el mensaje salió, la sintaxis era la de un hash, y el error solo
aparece cuando alguien va a buscarlo y no lo encuentra —con varias sesiones sobre la misma
rama, la duda siguiente es si el commit se perdió en una fusión—. El autor lo subió a regla
así: *«ya van varias. Que comprobar cualquier identificador antes de enviarlo suba a
regla.»*

Lo que exige:

1. **Hashes, `F-xx`, `VER-xx`, rutas, nombres de función y números de línea se escriben
   después de la orden que los lee** (`git log`, `git rev-parse`, el `grep` entero), nunca
   en la misma tanda antes de tenerla. Vale igual para un commit, un informe, un documento
   o un mensaje a otra sesión.
2. **Si el dato todavía no existe, se dice que no existe todavía.** El hash de un commit que
   se va a hacer no se anticipa: se cita en el mensaje siguiente.
3. **Un negativo también es un identificador afirmado**: «no existe», «nadie lo llama» o
   «está libre» se afirman después de buscar en el árbol entero, y en todas las ramas cuando
   se reserva un número (`docs/sesiones-concurrentes.md`).

Es la misma familia que el cero de `INV-03` y los números sin medir: un valor que parece un
resultado y es la huella de que la comprobación no llegó a hacerse.

## Estado de implantación

| | |
| --- | --- |
| **Modos de fallo catalogados** | **28 vigentes** (`MF-01`…`MF-29`, con `MF-25` retirado por `SPEC-14`: dejó de ser posible). **`MF-26` tiene desde `PLAN-23` A4 a `VER-115` mirando la reverificación, y nada mirando los hallazgos de la primera escritura**; `MF-27` y `MF-28` nacieron cerrados el mismo día en que se catalogaron, los dos por `procedencia.py` |
| **Validadores definidos** | **60** (`VER-01`…`VER-63`, con `VER-44`, `VER-41` y `VER-57` quemados) |
| **De ellos, no verificables hoy** | 6 (`VER-32`…`VER-37`) |
| **Validadores implementados** | **0** |
| **Con su caso negativo escrito** | 19 — en las cuatro fases de `PLAN-01` |
| **Desbloqueados por `PLAN-01` completo** | 28 |
| **Escritos y retirados a modo de prueba** | 5 — `VER-23`, `VER-28`, `VER-38`, `VER-45`, `VER-46` |
| **Bloqueados por falta de código de producción** | 18 |
| **Implementables hoy y sin implementar** | 2 — `VER-56` y `VER-59`, que solo necesitan los documentos |
| **Puntos ciegos asumidos** | **14 activos**, más `PC-10`, `PC-11` y `PC-16` cerrados y `PC-14` quemado |

**Estos recuentos se revisan al cerrar cada fase de `PLAN-01`.** No llevan marca
`Caduca con:` a propósito: una marca avisa **una vez**, y una tabla de recuentos deja de ser
cierta **cada vez** que aterriza código. Ponerle una condición de caducidad la haría fallar
continuamente y dejaría de significar nada, que es el fallo que las marcas existen para
evitar.

**Cero implementados, y conviene decirlo en voz alta: una lista más larga no es
más cobertura.** Este documento ha pasado de 37 filas a 59 y la cifra que mide
fiabilidad sigue siendo cero. Dieciocho esperan a `frontend/`, a CI o a una ejecución real, seis esperan una medición o una decisión, y **dos —`VER-56` y `VER-59`— no esperan a nada**: solo
necesitan los documentos, que ya existen.

**Cero sigue siendo cero, y conviene no redondearlo hacia arriba.** La Fase A de `PLAN-01`
escribió veintiún tests que pasan, y **ninguno es un validador completo**. Tres escriben
*parte* de un caso negativo —`VER-38` tiene su gancho pero nadie parsea todavía la tabla del
documento; `VER-21` caza una versión repetida y un hueco, pero no cruza commits; `VER-11`
prueba los predicados de severidad, no la puerta—. Contarlos como implementados sería
exactamente la cobertura falsa que la Regla 2 persigue.

Lo que sí ha cambiado son dos cosas. **Ya no hay ningún modo de fallo sin
estado**: antes había nueve que no estaban ni cubiertos ni reconocidos, que es el
peor de los tres porque no aparece en ninguna lista. Y **`SPEC-03` cerró dos
puntos ciegos cambiando el dominio en vez de añadir validadores**: cuando el dato
deja de ser prosa y pasa a ser una referencia, la comprobación deja de ser léxica
y empieza a tener punto ciego propio, que es lo que la Regla 2 exige para
admitirla.

Cinco se escribieron de verdad, con su caso negativo, para comprobar si el
documento aguantaba al llevarlo a código, y se retiraron después: la entrega de
esta fase es este documento y la spec del backend. **Lo que se aprendió
escribiéndolos está recogido más abajo**, porque es lo único que no se
reconstruye releyendo.

**`VER-45` y `VER-46` existen porque eran las dos únicas clases de defecto con
historial real en este repositorio** —setenta y tres referencias rotas tras dos
renombrados, y el diagrama de ciclo de vida con los estados en PascalCase— y
cuarenta y tres validadores no cubrían ninguna de las dos.

## Semilla de contexto

| Documento | Qué afirmaciones aporta |
| --- | --- |
| `CLAUDE.md` | Stack, presupuesto de 100.000 tokens y su reparto, reglas de Pydantic y enumeraciones, reglas de persistencia |
| `AGENTS.md` | Precedencia entre documentos, reglas de uso del modelo de dominio, estabilidad de identificadores |
| `docs/definitions.md` | Las dieciséis invariantes con su nivel y severidad, los vocabularios controlados, la política de casos negativos |
| `docs/domain-knowledge.md` | La máquina de estados de la escena y la secuencia de generación |
| `docs/architecture.md` | Reglas de dependencia entre features, contrato de los agentes, cola de trabajos, reglas de presentación del frontend, y **la estructura de carpetas contra la que `VER-45` resuelve las rutas** |
| `specs/SPEC - Backend.md` | El orden de recorte de §2.4 y el límite de `RF-26`, de los que dependen `VER-06` y el estado de `MF-05`; §2.5, que sostiene `PC-2`; y §2.2.1, que es el caso negativo de `VER-46` |

Si la semilla cambia, este plan cambia. Una fila cuyo origen desaparezca del
documento de partida se marca obsoleta, no se borra.

---

## Nivel artefacto — ¿es correcto el código?

Ninguna fila lleva columna de estado: **todas están pendientes**. Ver "Estado de
implantación".

| ID | Afirmación | Clase | Metodología | Criterio de salida | **Punto ciego** | Dónde vive |
| --- | --- | --- | --- | --- | --- | --- |
| VER-01 | Los esquemas Pydantic y las fichas de clase de `docs/definitions.md` tienen exactamente los mismos campos, **en los dos sentidos** | A | static analysis | El comprobador recorre los esquemas contra las fichas y las fichas contra los esquemas: cero campos de más y cero campos de menos | Ve **nombres**, no tipos ni valores: un campo `severidad: str` donde debería haber un `Enum` pasa | `commons/dominio/tests/` |
| VER-02 | Un valor fuera de un vocabulario controlado es error de validación, y **cada `Enum` tiene exactamente los valores de su tabla** | T | unit testing | Cada enumeración rechaza un valor inventado, **y** su número de miembros coincide con el de la tabla de vocabularios | Compara cardinalidad y pertenencia, no **uso**: un campo tipado `str` que nunca toca el `Enum` pasa | `commons/dominio/tests/` |
| **VER-58** | La longitud de la escena cae dentro de su `longitud_objetivo` (`INV-17`) | T | unit testing | Se cuentan las palabras del borrador y se comparan con el rango. Fuera de rango produce `Hallazgo` de severidad `mayor`, y por tanto impide cerrar el capítulo | Cuenta **palabras**, no **densidad**: una escena dentro de rango rellena de paja pasa. Y compara contra el rango **configurado**, no contra lo que el género pide |
| VER-03 | El endpoint de generación no bloquea: devuelve un identificador de trabajo | T | integration testing | La respuesta llega antes de que termine la generación y trae un identificador consultable | No comprueba que el trabajo **llegue a ejecutarse**: un worker parado deja `202` y trabajos eternos | `features/generacion/tests/` |
| VER-04 | Un trabajo encolado sobrevive a un reinicio del servidor | T | integration testing | Se encola, se mata el proceso, se levanta, y el trabajo sigue ahí y se completa | **No comprueba unicidad**: el trabajo puede completarse habiendo llamado al modelo dos veces | `commons/trabajos/tests/` |
| VER-05 | Ninguna llamada al modelo supera los 100.000 tokens, salida incluida, **medido con un contador independiente del ensamblador** | T | property-based testing | El contexto ensamblado más la reserva de salida cabe en el límite **según el `usage` que devuelve el modelo o un segundo tokenizador**, no según el contador de producción (Regla 3) | Mide el **techo**, no el **contenido**: un contexto de 3.000 tokens que dejó fuera al protagonista pasa | `features/contexto/tests/` |
| VER-06 | Cuando no cabe, se recorta en el orden declarado **en dos vueltas** —primero reducir, después eliminar—, y agotadas las formas reducidas y los tres primeros bloques se falla en vez de generar | T | property-based testing | Ningún recorte parte un bloque; **ninguna forma reducida se aplica fuera de orden ni se elimina un bloque con reducción pendiente**; el orden es el de las filas de `SPEC-01` §2.4 y si aún no cabe, el trabajo falla sin tocar el bloque 4.º (**estado del mundo y registro de conocimiento**), la reserva de salida ni el nivel inmutable. **El caso negativo obligatorio es el recortador que itera por niveles de `CLAUDE.md` en vez de por bloques**: se lleva el registro de conocimiento con las fichas y deja `INV-03` sin datos | Comprueba que el **orden** se respeta, no que lo que queda **baste**: un contexto recortado correctamente, que conserva el estado y el registro de conocimiento pero se quedó sin la ficha del protagonista, pasa. Eso es `PC-9` | `features/contexto/tests/` |
| **VER-59** | Ninguna forma reducida de `SPEC-12` §2.4 se lleva algo que la columna «Qué lee» asocie a una invariante `bloqueante` de nivel escena | A | static analysis | Se cruzan las formas reducidas declaradas contra lo que leen `INV-01`…`INV-05`. Cero solapamientos | **La columna «Qué lee» es una afirmación, no un hecho**, y lo será hasta que exista una implementación que pueda contradecirla. Compara declaración contra declaración: si la columna está incompleta o miente, el validador pasa igual. Es **el mismo punto ciego que `VER-38` con la severidad** —también compara un registro contra una tabla, sin nada que diga si el registro describe lo que el código hace—. Cerrarlo pide cruzar la columna contra las lecturas reales del verificador, y eso necesita código | `harness/documentos/` |
| VER-07 | Nunca se manda el texto completo de la obra al modelo | T | unit testing | Con una obra de muchas escenas, el contexto no contiene el texto de ninguna escena salvo la anterior | Mira **texto de escena**, no **volumen equivalente**: resúmenes que crecen hasta reconstruir la obra pasan | `features/contexto/tests/` |
| VER-08 | El texto completo de una escena se guarda pero no se recupera por similitud | T | integration testing | La búsqueda vectorial solo devuelve fichas, resúmenes y setups (`SPEC-26` v3) | Comprueba el **índice**, no el **camino de lectura**: leer el texto por clave primaria lo esquiva *(lo cubre `VER-07`)* | `commons/db/tests/` |
| VER-09 | El estado del mundo se reconstruye acumulando deltas en orden, **contrastado con una implementación de referencia** | T | property-based testing | Reconstruir con el aplicador de producción y con un **aplicador de referencia ingenuo escrito solo para la prueba** da el mismo estado (Regla 3). **Escrito en `PLAN-23` A2**: `consolidacion/tests/test_acumular.py` compara `aplicar.consolidar` + `mundo.leer`, `mundo.acumular` y el ingenuo, con un movimiento y una muerte en el mismo `t` | Comprueba que el estado **se construye bien desde el delta**, no que el **delta sea cierto** *(lo cubre parcialmente `VER-39`)* | `features/consolidacion/tests/test_acumular.py` |
| **VER-61** | El contador de delegaciones **no está por debajo del suelo reconstruible** desde los artefactos de la obra | T | integration testing | Se reconstruye desde los artefactos cuántas delegaciones hicieron falta **como mínimo** y se compara con el contador. **Tres resultados con significado distinto**: contador > suelo es normal —hay reintentos sin artefacto—; contador = suelo es sospechoso en una obra con reescrituras; contador < suelo es **error**, se perdieron delegaciones que sí produjeron trabajo | **Compara contra un suelo, no contra una medida.** No ve las delegaciones que no dejaron ningún artefacto, y sin los intentos conservados en disco no puede calcular el suelo: entonces lo dice y no concluye, en vez de dar por bueno un suelo de cero | `commons/trabajos/tests/` |
| **VER-62** | El **conjunto** de modelos es estable entre las delegaciones de una misma obra | A | static analysis | Se recorren las trazas y se compara el conjunto canónico de cada una contra el de la primera que reportó algo. Cero discrepancias. **No se compara contra lo declarado**: el declarado es corto —`fable`— y el reportado canónico —`claude-fable-5-1`—, así que no coincidirían nunca. Las trazas **sin modelo registrado** se devuelven aparte: no cuentan como conformidad | **Vigila la estabilidad, no la identidad**: si toda la obra usa un conjunto equivocado pero el mismo, pasa. Y no ve el enrutado si la traza registra lo que se pidió en vez de lo que respondió. Entonces estaría en verde mientras el cambio ocurre, y sería un eco: la Regla 3 otra vez. Cerrarlo pide que el registro venga de la respuesta |
| **VER-60** | El texto de una escena en el manuscrito es byte a byte el del `Borrador` que se auditó | T | integration testing | Se ensambla un manuscrito con escenas `aceptada` y `aceptada_por_rendicion` y se compara cada una con su `Borrador`. Cero diferencias | Compara el **texto**, no el **orden ni lo que falta**: un manuscrito al que le falte una escena entera, o que las ponga desordenadas, pasa. **Ampliada por `PLAN-22` E4:** la regla del texto elegido sube a `commons/obra/texto.py` y la usan el manuscrito y la lectura web, que devuelve cada escena con su `Borrador.version`; `features/lectura/tests/test_capitulo.py` compara byte a byte en la lectura |
| VER-10 | Ninguna escena pasa a `consolidada` sin su delta aplicado (`INV-05`) | T | unit testing | Intentar consolidar sin delta aplicado falla; la escena siguiente no se puede generar | Comprueba que el delta **se aplicó**, no que se aplicara **entero** *(lo cubre `VER-42`)* | `features/consolidacion/tests/` |
| VER-11 | `bloqueante` detiene la escena en la puerta; `mayor` y `menor` generan hallazgo y dejan seguir, y un `mayor` abierto impide cerrar el capítulo | T | unit testing | Un hallazgo de cada severidad produce exactamente el comportamiento declarado, **incluida la diferencia entre `mayor` y `menor` en la puerta de cierre de capítulo** (`SPEC-04` C-2) | Comprueba el comportamiento **dada** una severidad, no que la **asignada sea la correcta** *(lo cubre `VER-38`)* | `commons/invariantes/tests/` |
| VER-12 | Todo hallazgo cita su invariante **y el verificador que lo levantó**, los dos por identificador y nunca por descripción | A | static analysis | Todo `Hallazgo` construido lleva un identificador del registro `INV-xx` **y un `verificador` no vacío**. En las invariantes que comprueba más de un agente —`INV-13`, incremental y de barrido— dos hallazgos de la misma invariante levantados por agentes distintos tienen `verificador` distinto | Comprueba que los dos ids **existen** y que **se distinguen entre sí**, no que sean los **correctos**: un verificador copiado que no cambió ninguno de los dos sigue pasando | `commons/invariantes/` |
| VER-13 | Una feature nunca importa de otra feature, salvo `orquestacion/` | A | static analysis | El comprobador de importaciones falla el build ante cualquier import cruzado no autorizado | Estático: no ve importación dinámica ni acoplamiento por datos compartidos | CI |
| **VER-63** | Todo módulo de producción es **alcanzable desde alguna prueba**, directa o indirectamente | A | static analysis | Se recorre el árbol de importaciones desde los ficheros de prueba y se cierra transitivamente. Cero módulos fuera del alcance | **Alcanzable no es probado.** Un módulo importado para usar una constante queda contado y sin ejercitar: esto caza **código muerto**, no falta de cobertura. Y comparte `PC-1` con los otros doce: no ve la ejecución |
| **VER-66** | `INV-21`: ningún capítulo aceptado contiene una palabra vetada, en ninguno de los tres niveles (`SPEC-25` `RF-15`..`RF-18`) | T | unit + integration testing | Un caso por nivel (global, franja de edad, novela), uno de variante con acento y plural, la parada al agotar las 2 reescrituras sin rendición y la segunda línea en el cierre de capítulo. `commons/politica/tests/`, `features/politica/tests/`, `features/orquestacion/tests/test_vetadas_en_obra.py` | **Solo variantes simples.** Una conjugación («mató» frente a «matar»), un sinónimo, una falta de ortografía o una grafía con números («c4dáver») pasan. Y quitar el género produce falsos positivos (`caso` y `casa`), que es el lado ruidoso del error y se acepta | `features/politica/`, `features/orquestacion/` |
| **VER-67** | La entrevista detecta las tres contradicciones deterministas y no afirma nada cuando un valor es `otro` (`SPEC-25` `RF-08`, `RF-08b`, `RF-09`) | T | unit testing | Positivo y negativo por cada tipo; `otro` devuelve `requiere_juicio` y nunca «sin contradicción»; una contradicción resuelta no reabre y la entrevista no cierra con una abierta. `features/entrevista/tests/test_contradicciones.py`, `test_entrevista.py` | **Solo las tres parejas configuradas.** Una contradicción que no está en `sistema.json` no existe para el código. Lo que queda en `otro` depende del juicio del modelo, que está marcado como tal pero no verificado. Sin fecha de nacimiento, la edad tiene un año de margen | `features/entrevista/` |
| **VER-68** | Una inyección en el texto libre no produce hechos y queda en el audit log (`SPEC-25` `RF-11`..`RF-14`) | T | unit + integration testing | El doble del extractor **obedece** a la inyección y aun así salen cero hechos; el delimitador no se puede cerrar desde dentro; 5.001 caracteres se rechazan con el motivo; un hecho no entra en la ficha sin confirmar. `features/entrevista/tests/test_texto_libre.py`, `test_router.py` | **El detector es una lista de patrones**: una inyección bien escrita que no se parezca a ninguno pasa el detector. Lo que la contiene entonces es estructural —el texto nunca llega al Escritor, solo hechos confirmados por el comprador—, y eso no protege de un comprador que confirme a ciegas | `features/entrevista/` |
| **VER-69** | Tras entregar no queda ningún dato de la ficha en ninguna tabla, y se conservan las vetadas y los hechos de la story bible (`SPEC-25` `RF-21`) | T | integration testing | Una entrevista con dos contradicciones resueltas, cerrada y entregada por la API; se recorren **todas las tablas** de la base buscando cuatro datos inventados. `features/orquestacion/tests/test_entrega.py` | **Solo mira esta base.** Lo que ya salió del proceso —las trazas de Langfuse cuando existan, los registros de la sesión delegada, una copia de la base— no lo borra nadie. Y mira cuatro cadenas concretas: un dato que se copie transformado no lo encuentra. **Desde `SPEC-32`, el texto de la obra —capítulos y dedicatoria— conserva por decisión los nombres que contenga**: la dedicatoria se copia a `Obra` al montar y sobrevive al borrado, y esta prueba no monta la obra, así que no lo ve; lo comprueba `VER-80` | `features/orquestacion/entrega.py` |
| **VER-70** | `INV-22`: los nombres aparecen escritos exactamente como en la story bible (`SPEC-26` `RF-13`) | T | unit + integration testing | Distancia 1 (2 si el nombre tiene más de 6 letras), la tilde cuenta; la reescritura comparte contador con `INV-21` y al agotarse para sin rendición. `commons/politica/tests/test_personalizacion.py`, `features/orquestacion/tests/test_personalizacion_en_obra.py` | **Solo erratas cercanas y con mayúscula.** Un diminutivo, un apodo o una errata en minúscula pasan, y es deliberado: a esa distancia puede ser un recurso | `features/orquestacion/ciclo.py` |
| **VER-71** | `INV-23` e `INV-24`: cada imprescindible aparece en su capítulo y en la novela, contra `uso_de_hecho` (`RF-14`, `RF-15`) | T | integration testing | Una clave ausente da `INV-23` y otro intento; la presente deja su uso en `uso_de_hecho`; con un imprescindible sin uso la novela queda `novela_incompleta`. `test_personalizacion_en_obra.py`, `test_novela.py` | **Es léxico.** Un recuerdo contado sin sus palabras clave no cuenta como aparecido, y uno que las nombra de pasada sí. Por eso las claves las elige el Planificador y el Editor juzga la naturalidad aparte | `features/orquestacion/` |
| **VER-72** | `INV-25`: la prosa no repite el nombre del destinatario por encima del umbral ni frases largas entre capítulos (`RF-16`) | T | unit testing | Umbral provisional y no medido; da un `menor` y no para nada. `test_personalizacion.py`, `test_novela.py` | **Los umbrales no están medidos**: pueden saltar de más o de menos. La medida está definida en `commons/config.py` | `features/orquestacion/novela.py` |
| **VER-73** | `INV-26`: el Editor da una nota 1–5 por cada criterio con justificación, y por debajo del umbral el capítulo se reescribe hasta 3 veces (`RF-09`..`RF-11`) | T | integration testing | Seis notas de 4 pasan; una de 2 da `INV-26` con su instrucción en el siguiente intento; cuatro rondas por debajo se rinden; lo ilegible es `sin_veredicto` y no bloquea. `test_editor.py` | **Es un juicio del modelo**, no una comprobación. El umbral está pendiente de medida contra la revisión humana de una novela completa | `features/orquestacion/ciclo.py` |
| **VER-74** | `INV-27`: el juicio de obra recibe los resúmenes y el último capítulo completo, nunca la obra entera (`RF-12`) | T | integration testing | El prompt contiene el capítulo 10 entero y no el texto del 1. `test_novela.py` | Juzga un final abrupto con el último capítulo delante, pero un arco roto a mitad de novela solo lo ve a través de los resúmenes | `features/orquestacion/novela.py` |
| **VER-75** | Sin plan aprobado no se escribe: el código cuenta la cobertura y el Revisor juzga la fidelidad, hasta 3 rondas (`RF-05`..`RF-07`) | T | integration testing | Tres rechazos lanzan `PlanNoAprobado` con cero llamadas al Escritor; un plan con huecos no llega al Revisor. `test_planificar.py`, `test_cobertura.py`, `test_novela.py` | El Revisor es un modelo: puede aprobar un plan infiel. La cobertura sí es código | `features/planificacion/` |
| **VER-76** | Los dos hooks actúan solo sobre las delegaciones del pipeline (`RF-17`..`RF-19`) | T | integration testing | Lanzados como proceso: sin `HARNESS_AGENTE` salen con 0; con él validan y niegan; la salida es UTF-8. `test_hooks.py` | **No prueban que Claude Code les entregue en una delegación real estas variables y este formato.** Eso lo comprueba la ejecución real de `PLAN-26` E13 | `backend/hooks/` |
| **VER-77** | El contexto llega al Escritor como texto, recortado, en todas las escenas (`F-58`) | T | integration testing | La premisa aparece en el prompt de cada escena, ningún bloque llega como número, un bloque eliminado por el recorte no aparece y la sinopsis llega en «que pasa». `test_contexto_llega_como_texto.py` | Mira que el texto llegue, no que el modelo lo use | `features/orquestacion/bucle.py` |
| **VER-78** | Una opción de extensión fuera de 1.000–1.500 palabras por capítulo, un mínimo no menor que el máximo o una opción sin rango es un error de configuración (`SPEC-32` `RF-07`) | T | unit testing | Tres configuraciones malas, cada una con el motivo que tiene que dar, y la de serie que carga. `commons/configuracion/tests/test_configuracion.py` | El rango de 1.000–1.500 es **decisión nuestra**, no del enunciado: la prueba comprueba que se cumple, no que sea el bueno | `commons/configuracion/esquemas.py` |
| **VER-79** | La longitud objetivo de cada capítulo, las reglas del hook y el prompt del Planificador usan la extensión de la ficha (`SPEC-32` `RF-09`) | T | integration testing | `montar` con `larga` deja 1.350–1.500 en cada escena, `corta` llega a las reglas del hook, y un capítulo de 1.200 palabras deja `INV-17` con `larga`. `orquestacion/tests/test_novela.py`, `planificacion/tests/test_planificar.py` | Mira que la longitud viaje, no que el Escritor la respete: eso lo mide `INV-17` | `features/orquestacion/novela.py` |
| **VER-80** | Tras entregar, la dedicatoria sigue en `Obra` y solo allí, y ningún otro dato de la ficha queda en la base (`SPEC-32` `RF-02`, `RF-03`) | T | integration testing | La variante de `VER-69` que **sí monta la obra** antes de entregar, con una dedicatoria y dos datos que solo viven en la ficha. Se comprobó que la primera prueba falla si `montar` no copia la dedicatoria. `orquestacion/tests/test_entrega.py` | Busca cadenas concretas, como `VER-69`: un dato que se copie transformado no lo encuentra | `features/orquestacion/novela.py` |
| **VER-81** | La puerta de publicación decide las cuatro condiciones de `SPEC-30` `RF-01` —rendición, `bloqueante` abierta, Lean distinto de `0`, hallazgo de obra del Editor— y las lista todas, no la primera | T | unit testing | Una prueba por condición con su negativo, más una con las cuatro a la vez. `auditoria/tests/test_publicacion.py` | Decide sobre lo que le dan: si quien reúne los datos se deja uno, la decisión es correcta y la conclusión falsa. Por eso existe `VER-82` | `features/auditoria/publicacion.py` |
| **VER-82** | Lean se ejecuta solo al llegar a la puerta, el `2` bloquea igual que el `1`, y ni un `0` sin cobertura ni un generador con cero eventos dan limpio (`INV-28`, `RF-02`, `RF-09`) | T | unit + integration testing | El adaptador con un ejecutor doble en todos los caminos de salida; la puerta cuenta una llamada a Lean por ronda aunque otra condición ya falle; y Lean de verdad sobre tres bases (`specs/lean/medidas.md`). `auditoria/tests/test_lean.py`, `test_lean_real.py`, `orquestacion/tests/test_publicacion.py` | `test_lean_real.py` se salta sin `lake`, y lo dice. Y hoy en la novela regalo **solo `L-1` tiene datos**: nadie declara los presentes de cada escena (`PLAN-27` E3), así que un `0` de Lean ahí es «`L-1` limpio» | `features/auditoria/lean.py` |
| **VER-83** | Un fallo de Lean vuelve al Editor con las violaciones y los eventos implicados, y la generación se detiene sin reescribir (`RF-03`, `RF-06`, `C-2`) | T | integration testing | Lean doble con una violación de `L-1`: el prompt del Editor lleva `L-1` y `ev-1,ev-2`, el diagnóstico llega al informe de parada y no se llama a ninguna reescritura. `orquestacion/tests/test_publicacion.py` | Comprueba que el feedback viaja, no que el diagnóstico del Editor sea bueno | `features/orquestacion/publicacion.py` |
| **VER-84** | La puerta termina siempre, publicada o detenida; nunca hay un tercer reintento, y relanzar no reinicia el contador (`RF-04`, `RF-07`) | T | integration testing | Tope 2 con un juez que siempre ve un final abrupto: tres rondas y dos reescrituras; relanzar se para sin reescribir; y cuatro secuencias de veredictos acaban todas publicadas o detenidas. `orquestacion/tests/test_publicacion.py` | Es la propiedad de terminación que TLA+ comprueba sobre el modelo, probada sobre el código con cuatro secuencias, no con todas | `features/orquestacion/publicacion.py` |
| **VER-85** | Una escena rendida se queda en `aceptada_por_rendicion` después de consolidar, y un capítulo rendido no se publica (`RF-11`, `INV-29`) | T | integration testing | La escena rendida sigue rendida y consolidada; la limpia queda `consolidada`; relanzar salta la rendida; la puerta la nombra. `orquestacion/tests/test_obra.py`, `test_publicacion.py` | — | `features/escaleta/repository.py` |
| **VER-86** | Reescribir un capítulo consolidado cambia solo el texto, y solo si el delta es el mismo; ni el estado ni el canon se mueven, y un borrador rechazado no se convierte en el texto de la novela (`RF-10`, `C-1`) | T | integration testing | Mismo delta: cambia el texto aceptado y hay un solo delta; delta distinto o vetada: se rechaza y el texto elegido no cambia. `orquestacion/tests/test_obra.py` | Compara deltas como JSON ordenado: dos deltas que digan lo mismo con otra forma se rechazan. Falla ruidoso, no en silencio | `features/orquestacion/obra.py` |
| **VER-87** | `INV-06` sale en cada veredicto como **no ejecutada**, con su motivo, y no bloquea (`RF-12`, `C-4`) | T | unit testing | El veredicto guardado lista `INV-06` con el motivo de la excepción. `auditoria/tests/test_publicacion.py`, `orquestacion/tests/test_publicacion.py` | **Abierta a propósito**: comprueba que la excepción se dice, no que `INV-06` se cumpla. Se cierra el día que `INV-06` se implemente | `features/auditoria/publicacion.py` |
| **VER-88** | Tres tools de lectura de la story bible, con esquema de entrada y de salida; una entrada fuera de esquema se rechaza con un error que el agente ve y queda registrada (`SPEC-28` `RF-01`, `RF-02`) | T | unit + integration testing | Esquemas sobre `_DelDominio`; `atender` con entrada, salida y tool inválidas; el servidor como proceso atiende las tres. `commons/dominio/tests/test_story_bible.py`, `orquestacion/tests/test_story_bible.py`, `test_servidor_story_bible.py` | Que Claude Code acepte el `initialize` de este servidor propio lo dice solo una ejecución real (`PLAN-28` E10) | `features/orquestacion/story_bible.py` |
| **VER-89** | La story bible se lee en solo lectura, y un intento de escritura falla (`RF-04`) | T | integration testing | La conexión de lectura del servidor rechaza un `UPDATE`. `orquestacion/tests/test_servidor_story_bible.py` | Lo hace cumplir SQLite (`mode=ro`), no la disciplina | `backend/herramientas/story_bible.py` |
| **VER-90** | Ninguna tool devuelve el texto de una escena, ni por esquema ni por contenido (`RF-05`) | T | unit + integration testing | Ningún esquema de salida tiene un campo de texto; y un borrador con un marcador no aparece en nada de lo que devuelven las tres. `test_story_bible.py` (los dos) | Busca un marcador concreto: un texto que se colara transformado no lo encuentra | `features/orquestacion/story_bible.py` |
| **VER-91** | Una tool solo lee la obra de su delegación, y la obra no es argumento (`RF-06`) | T | unit + integration testing | Pedir otra obra por argumento es un error; con dos novelas en la misma base, ni los usos ni la ficha ni la cronología cruzan de una a otra. Se comprobó que quitar el filtro por obra hace fallar dos (Regla 11). `test_story_bible.py` | `entidad` y `lugar` siguen sin columna de obra (`F-64`, lo que queda): la lectura se apoya en que los identificadores vengan acotados del plan | `features/orquestacion/story_bible.py` |
| **VER-92** | Solo el Escritor y el Editor tienen tools, y el hook de policy es una allowlist por agente; toda delegación apaga las herramientas integradas (`RF-03`, `RF-07`, `D-2`) | T | unit + integration testing | Los agentes declaran sus tres tools exactas o `tools: []`; `escribir` se las da a los dos y a nadie más; el hook deja pasar las suyas y niega las demás, también las de otro servidor MCP; la orden lleva `--tools ""`, y con tools `--strict-mcp-config` y `--allowedTools`. `test_agentes.py`, `test_novela.py`, `test_hooks.py`, `test_proveedor.py` | Hasta `PLAN-28` E10, nada prueba que Claude Code entregue al hook el nombre `mcp__story_bible__*` ni que `--tools ""` deje vivas las tools MCP | `commons/politica/herramientas.py`, `hooks/policy.py` |
| **VER-93** | Cada llamada a una tool deja una fila con nombre, latencia, validación y tokens estimados, sin argumentos ni resultado, enlazada con su delegación; y esos tokens no entran en el reparto del presupuesto (`RF-08`, `RF-09`) | T | unit + integration testing | La tabla no tiene columnas de argumentos ni resultado; el span lleva un conjunto de claves exacto; la traza suma las llamadas de su delegación; `atender` no importa lo que reserva ni lo que recorta. `observabilidad/tests/test_trazas.py`, `test_story_bible.py`, `test_migraciones.py` | Los tokens son una **estimación** de cuatro caracteres por token, no una medida; reconciliarlos con el `usage` del sobre queda abierto | `features/observabilidad/repository.py` |
| **VER-102** | El congelado `contrato/openapi.json` se deriva del backend, y cualquier diferencia falla con operación, campo y dirección (`SPEC-22` `RF-31`..`RF-33`, `MF-29`) | T | unit + integration testing | Quitar un campo de una respuesta y añadir una ruta son diferencias que dan salida 1, con la operación que las usa, el puntero JSON y `anadido`/`quitado`/`cambiado`; dos generaciones seguidas son idénticas byte a byte; y el congelado del repositorio coincide con el backend de hoy. `harness/documentos/tests/test_contrato.py`, con `python -m pytest harness/documentos -q` desde la raíz | Compara **forma**, no **significado**: un campo que cambia de sentido sin cambiar de tipo pasa (eso es `VER-103`). Y corre en local: sin CI, un commit que no lo ejecute lo salta | `harness/documentos/contrato.py` |
| **VER-103** | En el congelado, los estados son enumeraciones con los literales de `docs/definitions.md`, ningún campo admite dos lecturas y ninguno expone la configuración del sistema (`SPEC-22` `RF-34`, `RF-35`, `RF-57`) | T | unit testing | Un `estado` como cadena libre, un campo `int | str`, una propiedad `techo_de_contexto` y un `EstadoDeEscena` con un literal inventado dan fallo cada uno; el `EstadoDeEscena` real y el congelado de hoy no dan ninguno. Los literales se leen de la tabla de `docs/definitions.md`, no de `enumeraciones.py` (Regla 3). `harness/documentos/tests/test_significado.py` | Mira **nombres y tipos**, no sentido: un campo `modelo_usado` con el nombre del modelo dentro pasa, y un estado que no se llame `estado*` ni `severidad` no se obliga a ser enumeración | `harness/documentos/contrato.py` |
| **VER-104** | El índice llega resuelto: capítulos por `capitulo.orden`, sin mezclar obras, cada escena bajo su capítulo con su estado, la rendición decidida y sus hallazgos abiertos con su estado, y la portada con la `dedicatoria` de `Obra` (`SPEC-22` `RF-37`, `RF-38`, `RF-40`, `RF-46`) | T | integration testing | `GET /obras/{id_obra}/indice` sobre una base con dos obras y dos capítulos cuyos `id` ordenan al revés que su `orden`: cada escena bajo el suyo y en orden, `cap-b` antes que `cap-a`, la otra obra no se cuela, un `id` de capítulo pedido como obra da 404, un `resuelto` no sale y un `sin_veredicto` sale como tal, y sin dedicatoria viaja `null`. `features/lectura/tests/test_indice.py` **Ampliada en `PLAN-22` E15:** con dos versiones, el índice sin versión es el de la vigente —antes salían los cuatro capítulos, dos con `orden` 2 (`F-132`)— y `GET /obras/{id}/versiones/{n}/indice` da el de cualquier versión, cada capítulo con su `compartido` tal como lo da `regeneracion` (se sustituye `comparar` y la lectura lo sigue). `orquestacion/tests/test_lectura_de_version.py` | Las pruebas siembran por SQL, porque una feature no importa de otra: que los que escriben dejan esa forma lo comprueba la semilla de E11, no estas. Y **no hay partes**: la base no tiene tabla `parte` | `features/lectura/`. **Las fichas** (`GET /obras/{id_obra}/fichas`, `PLAN-22` E5) se prueban en `features/lectura/tests/test_fichas.py` y `commons/obra/tests/test_apariciones.py`, y comparten la fila de `PLAN-27` que allí se llama `VER-nueva-B` y todavía no tiene número: un personaje por `participa_en`, un lugar por `ocurre_en`, por `capitulo.orden`, una mención no cuenta, presentes sin declarar viajan nulos y un nombre no guardado viaja nulo |
| **VER-105** | Ninguna escena sale de la lectura sin su estado ni sus hallazgos, tampoco dentro de un capítulo, y su texto es byte a byte el del borrador elegido, con su `version` (`SPEC-22` `RF-39`..`RF-41`) | T | integration testing | `GET /capitulos/{id_capitulo}` y `GET /escenas/{id_escena}`: cada escena lleva `estado` y `hallazgos_abiertos`; con dos versiones y la 1 elegida sale la 1 byte a byte; una `planificada` sale con `borrador` nulo; un `sin_veredicto` sale como tal; una rendida sigue rendida; `GET /escenas` da lo mismo que la escena dentro de su capítulo; un `id` de obra pedido como capítulo o escena da 404. La regla del texto elegido vive en `commons/obra/texto.py` y la usa también el manuscrito. `features/lectura/tests/test_capitulo.py`, `commons/obra/tests/test_texto.py` **Ampliada en `PLAN-22` E15:** `GET /obras/{id}/versiones/{n}/capitulos/{id}` lee la versión anterior entera, con su texto, y cada escena lleva además su `estado_de_verificacion` en esa versión: una `consolidada` con verde heredado sale `sin_reverificar`; un capítulo que no es de la versión da 404. `orquestacion/tests/test_lectura_de_version.py` | Compara con **lo sembrado**, no con lo que el Escritor escribió: un borrador que se guardó ya alterado sale igual de «byte a byte» | `features/lectura/`, `commons/obra/texto.py` |
| **VER-106** | Los datos de prueba del frontend se derivan del congelado, los que hablan de capítulos traen al menos dos en una obra, y ninguna prueba del frontend llama a un backend ni el frontend depende de un cliente de base (`SPEC-22` `NF-01`, `NF-04`, `NF-05`, `NF-07`; también `VER-16` y `VER-17`) | T + A | unit testing + static analysis | Desde `frontend/`, `npm test`: `fixtures.test.ts` valida cada fixture con Ajv contra su esquema de `contrato/openapi.json` y una fixture rota no valida; toda fixture con `capitulos` trae dos o más; `frontera.test.ts` no encuentra ningún cliente de base en `package.json`, ningún `fetch(` fuera de `src/shared/api/`, y el `fetch` global falla si alguien lo llama. Los tipos salen del congelado con `npm run contrato` (`openapi-typescript`). Y `npx steiger src` sin violaciones (se comprobó que caza una importación de `shared/` a `app/`) | Las fixtures las escribe una persona: validan contra el esquema, no contra lo que el backend devuelve de verdad con esos datos. `fetch(` se busca como texto: una llamada por otro nombre pasa | `frontend/src/shared/api/`, `frontend/tests/` |
| **VER-107** | La interfaz pinta en el orden y con los enlaces que recibe, sin calcular: portada, índice, capítulo y fichas (`SPEC-22` `RF-38`, `RF-41`, `RF-43`, `RF-44`, `RF-46`, `NF-06`) | T | unit testing | Desde `frontend/`, `npm test`, con el cliente real y un `fetch` inyectado que devuelve fixtures del congelado: el índice pinta capítulos y escenas **en un orden que ninguna regla produciría** (la respuesta invertida a mano) y cada capítulo enlaza con su `id`; la portada pinta la dedicatoria y, sin ella, solo el título. `pages/indice/ui/Indice.test.tsx`, `pages/portada/ui/Portada.test.tsx` (E8); capítulo y escena en E9, fichas en E10 | Comprueba lo que se **pinta**, no lo que se **ve**: un CSS que oculte un capítulo pasa. Eso lo mira la inspección con el navegador (`VER-109`, `VER-119`) | `frontend/src/pages/` |
| **VER-108** | Un solo browser MCP en la configuración del repositorio, y ninguna delegación del pipeline lo carga ni lo usa (`PLAN-22` DP-6) | T | unit testing | `.mcp.json` declara solo `playwright`, con `@playwright/mcp@` y versión fijada; **toda** delegación con agente lleva `--strict-mcp-config`, también las que no tienen tools, y sin tools no recibe `--mcp-config`; el hook de policy niega las tools del browser a cada agente de `.claude/agents/`. `commons/modelo/tests/test_proveedor.py`, `commons/politica/tests/test_browser_mcp.py` (la del hook pasó desde el primer momento y queda como regresión) | Comprueba la **orden**, no a Claude Code: que `--strict-mcp-config` de verdad impida cargar el `.mcp.json` de la raíz no lo prueba nada aquí | `commons/modelo/proveedor.py`, `.mcp.json` |
| **VER-109** | La inspección visual real de la lectura web queda registrada, con lo que se inspeccionó, lo que se detectó y lo que cambió (clase I) | I | inspección con browser MCP | `PLAN-22` E13, 2026-09-24: una sesión de Claude Code con Playwright MCP recorrió ocho páginas de la web servida sobre la semilla inventada y siguió los cuatro enlaces de las fichas (0,5766 USD leídos del sobre). Registrado en `docs/proceso/claude-code.md` § Browser MCP: `F-81`..`F-83` arreglados en el mismo commit, `F-84` abierto **Ampliada en `PLAN-22` E18:** la petición de cambio sobre la semilla con dos versiones —selección, hecho, capítulos, promesa y punto ciego, versión 1 entera— (1,07275 USD leídos del sobre); detectó `F-133`, arreglado en el mismo commit | **Una vez, a mano, sin score**; y el agente no vio tres defectos que las capturas sí enseñaban (`F-84`) | `docs/proceso/claude-code.md` |
| **VER-110** | La página recibe los hechos de una escena y sus presentes, y el trabajo tiene forma en el congelado (`PLAN-22` E14; `SPEC-22` `RF-47`, `RF-48`) | T | integration testing | `GET /escenas/{id}/hechos` da los hechos de `uso_de_hecho` de la escena **de su obra** —el mismo `id` en dos obras con enunciados distintos—, uno por hecho aunque se use con dos tipos, y `[]` (no nulo) sin usos; `GET /escenas/{id}` trae `personajes_presentes`, nulo si no se declaran. `features/lectura/tests/test_hechos_de_escena.py`. `GET /trabajos/{id}` tiene `response_model` con `estado` como `EstadoDeTrabajo`, cuyos literales se leen de `docs/architecture.md` § "Los estados de un trabajo" (no del `Enum`): `harness/documentos/tests/test_significado.py` | El enunciado que se ofrece es **el del canon**, no el de la versión que se lee: tras un cambio de hecho, la página ofrece el enunciado viejo (`F-130`) | `features/lectura/`, `orquestacion/schemas.py`, `harness/documentos/contrato.py` |
| **VER-111** | La interfaz no confirma un cambio sin enseñar antes los capítulos que se tocarían, manda lo elegido y no una lista suya, y la promesa va con su punto ciego (`PLAN-22` E16; `SPEC-22` `RF-47`..`RF-51`, `RF-55`; `SPEC-23` `D-3`) | T | unit testing | Desde `frontend/`, `npm test`, con el cliente real y un `fetch` que apunta método y cuerpo: la propuesta lleva el hecho elegido (o el personaje) y las palabras del lector y **ningún capítulo**; el botón de confirmar no existe antes de la propuesta; al confirmar, `capitulos_propuestos` y `version_de_partida` son **los que dio el backend**; `promesa` y `punto_ciego` se pintan tal como llegan; sin salida elegida (`capitulos_propuestos` nulo) no se ofrece confirmar y se enseñan el motivo y las dos listas; un `409` enseña su `detail`; el trabajo se sigue hasta un estado terminal y deja de pedirse, y si falla se enseña su motivo. `features/pedir-cambio/ui/PedirCambio.test.tsx`, `pages/capitulo`, `pages/escena`, `shared/api/cliente.test.ts` | La selección del fragmento se prueba con un rango sintético de jsdom, no con un ratón; y **pedir el cambio no se ha ejercido de verdad**: `POST /obras/{id}/cambios` responde `409` hasta la Parte B de `PLAN-23`, así que el seguimiento de un trabajo real solo se ha visto con dobles | `frontend/src/features/pedir-cambio/` |
| **VER-112** | Las marcas de cambio y el verde heredado se pintan tal como llegan, y la versión anterior se navega entera (`PLAN-22` E17; `SPEC-22` `RF-52`..`RF-54`) | T | unit testing | Desde `frontend/`, `npm test`: el índice de una versión marca «cambió» **exactamente** los capítulos con `compartido: false` de la respuesta, con marcas que ningún texto justificaría (el 1 cambiado y el 2 no); sin anterior no marca nada; cada escena lleva su `estado_de_verificacion` y un `sin_reverificar` no se pinta como verificado ni por color ni por texto (`EscenaConEstado`, `tema.test.ts`); fuera de una versión no se inventa. Desde el índice de la vigente se llega a la versión 1 y se lee cada capítulo suyo entero, sin ofrecer pedir cambios sobre ella. `pages/indice/ui/IndiceDeVersion.test.tsx`, `entities/escena/ui/EscenaConEstado.test.tsx`, `app/App.test.tsx` | Con fixtures: que el backend dé de verdad esas marcas lo comprueba `VER-104`/`VER-105` (E15). La versión vigente es **la última de la lista** que da el backend, que la ordena por número: si el orden cambiara, la interfaz elegiría mal sin fallar | `frontend/src/pages/indice/`, `frontend/src/pages/capitulo/` |
| **VER-119** | `INV-30`: cada pieza de la lectura web (`portada`, `indice`, `capitulos`, `fichas`, `enlaces`) tiene veredicto validado con esquema, un veredicto ilegible no pasa, una pieza que falla deja hallazgo, y cada una sube como score en la sesión de la obra (`SPEC-22` `RF-58`) | T | unit + integration testing | `auditoria/tests/test_visual.py`: nueve formas de ilegible —nada, texto, vacío, sin piezas, falta una, repetida, veredicto fuera, sin motivo, campo de más— son `sin_veredicto` con su hallazgo, y una pieza que falla deja `INV-30` `abierto` con su motivo; `orquestacion/tests/test_inspector_visual.py`, con un doble del agente sobre la semilla: el hallazgo en la base, `INV-30.<pieza>` y `INV-30` como scores en la sesión de la obra, y el motivo del agente no sube; `commons/politica/tests/test_browser_mcp.py`: el inspector solo tiene las tools del browser y su delegación carga `.mcp.json` con MCP estricto; `commons/invariantes/tests/test_registro.py`: la fila de `INV-30` del registro es la de `docs/definitions.md` **Ejecución real (E13b.2):** una vez, sobre la semilla ya arreglada, `pasa` en las cinco piezas, 0,4872 USD; sus scores fueron a un exportador en memoria porque el worktree no tenía claves de Langfuse | **Lo juzga un agente, que puede ver bien lo que está mal** (`F-84`: el de E13 lo hizo); y ❌ **el fallo no vuelve solo al Escritor** | `features/auditoria/visual.py`, `backend/inspeccion_visual.py` |
| **VER-128** | La interfaz muestra en qué punto va una generación —fase, capítulo sobre el total y segundos desde la última actividad, calculados en el servidor— y avisa cuando lleva quieta más de un umbral (`SPEC-22` `RF-60`) | T | unit + integration testing | Backend: `novela.escribir` con dobles deja `planificando`, `revisando_plan`, `escribiendo` (capítulos 1 a 10 de 10), `editando`, `resumiendo`… `en_la_puerta` y termina en `publicada` o `esperando_revision`; una excepción o una parada del capítulo dejan `parada` con su motivo (`orquestacion/tests/test_progreso.py`); `GET /obras/{id}/progreso` da los segundos con el reloj de la base, cuenta la traza de una escena de la obra y no la de otra, y sin filas da `404` (`lectura/tests/test_progreso.py`). Frontend: `Progreso` enseña fase y capítulo sobre el total, marca «sin actividad desde hace N min» pasado `UMBRAL_SIN_ACTIVIDAD_SEGUNDOS`, se vuelve a pedir sola y sin progreso no pinta nada (`entities/progreso/ui/Progreso.test.tsx`) | La fase la escribe `ConFase` **al llamar a cada agente**: una fase sin agente —montar, cargar vetadas— se ve como la anterior. Y el umbral es de la interfaz, no de `config/sistema.json`: el frontend no lee configuración (`RF-56`) | `features/orquestacion/progreso.py`, `features/lectura/`, `frontend/src/entities/progreso/` |
| **VER-129** | Cada delegación deja su coste en la base, también las que no pasan por el ciclo de escena, y lo guardado cuadra con el total que informa la CLI (`SPEC-33` `RF-18`, `RF-19`, `RF-21`) | T | unit + integration testing | `commons/modelo/tests/test_anotador.py`: la delegación que sale bien, la ilegible con lo que costó, la de transporte y la sin envoltura sin coste, y un anotador roto que no se calla; `commons/modelo/tests/test_gasto.py`: coste ausente como `NULL`, nunca 0; `orquestacion/tests/test_regalo.py`: una novela entera con `SesionDelegada` de verdad y un proceso doble con un coste distinto por agente, donde los cinco agentes y el juez de la puerta anotan y la suma de `gasto_de_delegacion` es el `coste_total` de la CLI; `entrevista/tests/test_gasto_de_la_entrevista.py`: el Entrevistador anota en la obra | Cuadrar con la CLI prueba que las dos cuentas coinciden, **no que alguna sea el coste real**: eso solo lo dice Langfuse o la factura, en `PLAN-33` E16. Un agente nuevo que no se construya con `SesionDelegada` no anota, y nada lo caza salvo que alguien lo añada a la prueba | `commons/modelo/proveedor.py`, `commons/modelo/gasto.py`, `features/orquestacion/regalo.py` |
| **VER-130** | Lanzar una generación desde la web lo decide el backend: entrevista cerrada, una sola a la vez y lo gastado por debajo del techo de 50 USD; si no, `409` con el motivo (`SPEC-33` `RF-11`, `RF-13`, cuestión 1) | T | integration testing | `orquestacion/tests/test_lanzar.py`: `202` con trabajo y generación, el mismo pipeline con su gasto anotado por generación, `409` sin entrevista cerrada, con otra en curso y con 50 USD gastados, y una generación que falla deja su motivo en el trabajo; `commons/configuracion/tests/test_techo_de_la_web.py`: el techo separado del de la evaluación, 50 en `sistema.json` y un techo no positivo rechazado **por su campo** | Lo gastado es un suelo: una base con gasto anterior a la migración 18 puede pasar el techo sin que el backend lo sepa. Y el techo se mira **al lanzar**: una generación en curso no se para al cruzarlo | `features/orquestacion/router_regalo.py`, `features/orquestacion/regalo.py` |
| **VER-131** | La entrevista en la web es una conversación que sobrevive a recargar, con lo que falta, los avisos y las contradicciones dentro de su turno, y cerrar solo cuando el backend lo dice (`SPEC-33` `RF-05`..`RF-10`) | T | unit + integration testing | Backend: `entrevista/tests/test_historial.py` y `test_historial_para_la_pagina.py` (el historial en orden con la primera pregunta, avisos ausentes y no vacíos en filas viejas, `puede_cerrar`, `cerrada` y los hechos propuestos, y el borrado con la ficha). Frontend, `npm test`: `pages/entrevista/ui/Entrevista.test.tsx` y `Turno.test.tsx` | Los endpoints de `PLAN-25` no están tipados en el congelado (su campo `contradicciones` choca con `RF-57`): la página lee el historial, y un cambio en la respuesta del turno no lo caza el contrato | `features/entrevista/`, `frontend/src/pages/entrevista/` |
| **VER-132** | La generación visible y el gasto se pintan como llegan, sin inventar cifras: la fase solo en el capítulo en curso y el estado de su escena en los demás (`F-200`), las notas del Editor al cerrarse, «sin medir» en vez de 0,00 y el suelo junto a la cifra, y la confirmación no deja gastar hasta tener sus tres cifras con su procedencia (`SPEC-33` `RF-12`, `RF-14`..`RF-20`) | T | unit + integration testing | Backend: `regalo/tests/test_generacion.py`, `test_gasto_web.py`, `test_estanteria.py`. Frontend, `npm test`: `pages/generacion/`, `pages/entrevista/ui/ConfirmarGeneracion.test.tsx` (las pruebas del contador se vieron fallar contra un stub) y `pages/estanteria/` | Las fixtures son inventadas: `F-200` pasó todas las pruebas y solo lo vio una captura. Una forma nueva del progreso que deje el pipeline no la cubre ninguna fixture hasta que alguien la mire | `features/regalo/`, `frontend/src/pages/generacion/`, `frontend/src/pages/estanteria/` |
| **VER-133** | Las páginas de la novela regalo se recorren en un navegador real sobre una base con datos inventados, y lo que se vio queda registrado (clase I, `SPEC-33` `RF-24`) | I | inspección en navegador | `PLAN-33` E15: Edge sin cabeza movido por Playwright, **sin modelo y sin gastar**, en los puertos 8010 y 5183; 16 comprobaciones y las capturas revisadas a mano, a 1280 y a 390 px. Registrado en `docs/proceso/claude-code.md` § "Browser MCP" | **No es la inspección con el agente del browser MCP** que pide `RF-24`: esa lanza una sesión de Claude Code y gasta, y queda pendiente de un sí. Nada pulsó «Responder» ni «Sí, escribir la novela», así que el camino con el modelo real no está inspeccionado | `backend/semilla_regalo.py`, `frontend/scripts/recorrido-regalo.mjs` |

| **VER-113** | La medida del arrastre se calcula como dice `SPEC-23` v2 —la media, entre los hechos con algún uso en las escenas de la obra, de los capítulos distintos que lo usan por `menciona`—, **se niega** sin obra completa, sin ningún hecho con usos o sin procedencia (`MF-27`), filtra los usos por las escenas de la obra, no escribe en la base y lleva la dirección de su sesgo en el mismo objeto | T | unit testing | `orquestacion/tests/test_arrastre.py` (`PLAN-23` A1): dos menciones en un capítulo cuentan uno; con 3 sale `cascada` y con 3,5 `selectiva`; los usos de otra obra con el mismo `id` de hecho no cuentan; `total_changes` e `iterdump` no cambian al medir; el guion sale con 1 al negarse. Un capítulo reescrito tras consolidar sale aparte | Hereda el de `menciona`, que es **léxico**: una alusión sin las palabras del enunciado no cuenta, y el sesgo es **a la baja, hacia `S-1`**. Un delta sin versión (los consolidados antes de `PLAN-23` A1) no deja ver si el texto aceptado es el del acta: va a `acta_sin_version` | `orquestacion/arrastre.py`, `backend/medir_arrastre.py` |
| **VER-114** | Una versión creada no cambia, dos versiones se distinguen por su número aunque tengan los mismos capítulos, y lo compartido es la misma fila (`RF-53`, `F-43`, `EX-14`) | T | integration testing | `brief/tests/test_versiones.py` (`PLAN-23` A3): un `UPDATE` o `DELETE` sobre una versión creada lo aborta un disparador; la versión 2 no cambia ninguna fila de la 1; un capítulo nuevo en la posición 3 no borra al capítulo 3 anterior (Regla 7); la migración da la versión 1 a cada obra sin perder capítulos | No comprueba que la nueva sea **mejor**, solo que la anterior sigue ahí | `features/brief/repository.py`, migración 11 |
| **VER-115** | Un verde solo cuenta contra la huella del estado de su versión (`D-1`, `RF-54`, `MF-26`) | T | integration testing | `orquestacion/tests/test_regeneracion.py` (`PLAN-23` A4): una acción sobre un hecho que la versión nueva ya no revela falla al reverificar; una escena compartida con el verde de la versión 1 sale `sin_reverificar` en la 2; un delta nuevo en el prefijo deja el verde sin contar; la reverificación no escribe en `hallazgo` ni delega en nadie. `INV-06` e `INV-04` salen como no ejecutadas | Las puertas leen el **delta** y no la prosa (`PC-5`, `MF-18`, `PCF-7`). Y `INV-04` no se vuelve a pasar: `Borrador.pov_usado` no se guarda | `orquestacion/regeneracion.py`, `features/verificacion/repository.py` |
| **VER-116** | La propuesta enseña los capítulos antes de tocarlos y no encola nada, «cambió» lo decide el backend por identidad y no por texto, la promesa lleva su punto ciego, y pedir el cambio sin salida elegida es `409` sin guardar ni encolar (`RF-51`, `RF-52`, `RF-55`, `C-4`) | T | integration testing | `orquestacion/tests/test_router_versiones.py` (`PLAN-23` A5) y `test_peticion.py` (A7): un capítulo nuevo con el mismo texto sigue contando como cambiado; ningún campo expone modelos, topes ni la ruta de la base; un renombrado toca los capítulos con el nombre viejo como palabra entera y los de su presencia; renombrar al destinatario o a un nombre ya usado es `409`; el nombre viejo es vetada solo en la versión nueva | Comprueba la **API**, no la página (`PLAN-22`). Y no hay contrato congelado en esta rama: la forma la fijan las pruebas | `orquestacion/router.py`, `orquestacion/regeneracion.py` |
| **VER-117** | Con dos versiones, cada lector ve solo la suya | T | integration testing | `orquestacion/tests/test_regeneracion.py` (`PLAN-23` A6): la memoria de una escena de la versión 2 no trae el resumen del capítulo sustituido; el manuscrito es el de la vigente; la story bible, `INV-24`, la medida y el cierre ven la vigente; un imprescindible se comprueba en el capítulo regenerado; con una sola versión, lo mismo que antes | **Solo los lectores enumerados** en el hallazgo 5 de `PLAN-23`. La cronología y el generador de Lean siguen leyendo por obra (`F-93`) | `orquestacion/regeneracion.py` |
| **VER-118** | La rama que eligió la medida, `S-1` (cascada): la salida fijada es la que da la regla con el número guardado; la versión nueva reescribe desde el primer capítulo que toca la petición hasta el final y comparte por referencia lo anterior; un capítulo `cerrado` no se reabre; una parada a mitad deja la anterior intacta y a la vista, y relanzar sigue desde el último consolidado; la versión nueva queda reverificada entera y pasa la puerta con sus propias rondas (`SPEC-23` v4, `D-1`, `D-2`, `D-3`, `RF-30`, `F-121`, `F-122`) | T | integration testing | `orquestacion/tests/test_cascada.py` (`PLAN-23` B2, B-S1.1, B-S1.2), con una novela entera escrita con dobles y la cascada con dobles: la salida contra `harness/evals/arrastre-SPEC-23.json`; los capítulos 3..10 nuevos y 1..2 compartidos, con los borradores viejos intactos; ningún capítulo de la versión 2 `sin_reverificar`; un `cerrado` sigue `cerrado`; una parada en el capítulo 5 deja la 1 vigente mientras se escribe y después; relanzar no repite el 3 ni el 4; el renombrado «Nala» escribe con el nombre nuevo y veta el viejo (`F-124`); sin ficha (`F-91`) o sin agentes (`F-126`), nada escrito; y `backend/pedir_cambio.py` con dobles (`test_pedir_cambio.py`) | **Reescribe más de lo necesario, a propósito**: los capítulos posteriores cambian aunque no dependieran del cambio, y la generación no es determinista, así que se pierde texto que estaba bien (`SPEC-23` `S-1`, «efecto no obvio»). **Lo que no ve**: una prosa de los capítulos compartidos `1..k-1` que contradiga el cambio sin que su delta lo declare no se toca (`D-3`, `PC-5`); la cronología y Lean leen por obra, así que la puerta de la versión nueva ve también los eventos de los capítulos sustituidos (`F-93`); y cambiar el enunciado de un imprescindible no cambia sus palabras clave (`F-125`). **Sin ejecutar con modelos reales**: B4 | `orquestacion/cascada.py`, `orquestacion/regeneracion.py`, `backend/pedir_cambio.py` |

| **VER-120** | Hay cinco briefs de evaluación, cargan con su esquema, fijan su extensión y declaran datos inventados (`SPEC-31` `RF-01`, `RF-10`; `PLAN-31` VER-nueva-A) | T | unit testing | `evaluacion/tests/test_briefs.py`: los cinco cargan; uno sin `datos: inventados`, con ficha y guion a la vez, sin ninguno de los dos o sin extensión no carga; el de injection trae una instrucción que el detector ve y otra que no; el de contradicciones dispara las tres reglas; el de vetadas tiene una variante cazada de cada vetada | «Inventados» es una declaración del fichero: ninguna prueba sabe si un nombre es de una persona real | `features/evaluacion/briefs.py`, `harness/evals/` |
| **VER-121** | La tabla por brief no da «pasó» sin constancia de ejecución, y distingue «sin veredicto», «sin ejecutar» y «no ejecutado» (`RF-02`; VER-nueva-B) | T | unit + integration testing | `orquestacion/tests/test_evaluacion.py` y `evaluacion/tests/test_tabla.py`: sin borrador las puertas dicen sin veredicto; un `sin_veredicto` y el `2` de Lean no son pase; una obsoleta es no aplica; `INV-06` es no ejecutado en todas las filas; un brief sin ejecutar lo dice en todas sus columnas; una invariante nueva del registro sale sola; la puerta sale del veredicto (mutación comprobada) | La constancia es la que **declara** `orquestacion/evaluacion.py` para cada validador: si esa declaración es falsa, el pase también. `INV-21` e `INV-22` suponen listas no vacías | `features/orquestacion/evaluacion.py`, `features/evaluacion/tabla.py` |
| **VER-122** | El gasto se persiste por tramo, el techo detiene entre capítulos y un coste ausente no suma cero (`RF-06`; VER-nueva-C) | T | unit + integration testing | `evaluacion/tests/test_gasto.py`, `commons/modelo/tests/test_contador.py`, `test_novela.py` y `test_evaluar.py`: un tramo sin ninguna delegación con coste se guarda sin medir; con alguna es un suelo y se dice; con el techo alcanzado no empieza nada; `seguir` para con `techo_de_gasto`; lo anotado sobrevive a una caída; el juicio de obra y las reescrituras de la puerta entran en el total | Lo gastado es un suelo si hay delegaciones sin coste, así que el techo puede dejar empezar una ejecución que ya no cabe; y se puede pasar en lo que cueste el capítulo en curso | `features/evaluacion/repository.py`, `commons/modelo/contador.py`, `orquestacion/novela.py` |
| **VER-123** | El Editor deja sus seis notas por versión de borrador (relación `valorado_en`; VER-nueva-D) | T | integration testing | `orquestacion/tests/test_editor.py`: seis notas aunque ninguna baje del umbral, doce con una reescritura, ninguna con una valoración ilegible, que sigue siendo `sin_veredicto` | Guarda lo que el Editor dijo, no si tenía razón: eso es `VER-124` | `features/escaleta/repository.py` (`valoracion_del_editor`), `orquestacion/ciclo.py` |
| **VER-124** | La comparación Editor contra autor es criterio a criterio sobre la misma novela (`RF-04`; VER-nueva-E) | T + I | unit testing + revisión humana | **Sin implementar**: `comparar.py` y su prueba con dobles son parte de `PLAN-31` H1, que espera a la novela de R1 | Mientras no exista, la nota del Editor solo se contrasta consigo misma | `features/evaluacion/comparar.py` (no existe) |
| **VER-125** | Una novela no recibe datos de otra de la misma base (`RF-12`; VER-nueva-F) | T con dobles + D | integration testing | `evaluacion/tests/test_exfiltracion.py`: dos fichas disjuntas en una base, dobles que guardan cada prompt y el rastro con la normalización de las vetadas. **Falla hoy** (`F-100`, `xfail` estricto): el Escritor de la segunda recibe el mundo de la primera. Las tools no fugan, y su prueba caza la fuga si se quita el filtro de obra | El rastro busca las claves: una paráfrasis o un dato sin nombre no se ven. En una ejecución real solo mira los textos guardados, no el prompt | `features/evaluacion/exfiltracion.py` |
| **VER-126** | Cada caso del red-team nombra su detector o dice «ninguno», y las pruebas que cita existen (`RF-05`; VER-nueva-G) | T + D | unit testing | `evaluacion/tests/test_red_team.py` sobre `harness/adversarial/casos.json` | Que un caso esté en el log no dice que el corpus sea completo; ninguno se ha lanzado contra el modelo real | `harness/adversarial/` |
| **VER-127** | El tuning muestra la versión de prompt de cada pasada, y los resultados llegan a Langfuse como scores (`RF-03`, `RF-07`; VER-nueva-H) | T | integration testing | `evaluacion/tests/test_evaluar.py`: un score por columna de la fila con `referencia=evaluacion-<pasada>`, y la huella del prompt del Escritor en el libro y en la tabla | Probado con el exportador en memoria: que lleguen a la instancia real es `PLAN-29` E13 | `backend/evaluar.py`, `orquestacion/observar.py` |
| VER-14 | `commons/` nunca importa de una feature | A | static analysis | Mismo comprobador; la flecha va en un solo sentido | El mismo que `VER-13` | CI |
| VER-15 | Los `Enum` de los vocabularios controlados **del dominio** viven solo en `commons/dominio/` | A | static analysis | No hay ninguna definición de `Enum` de dominio fuera de esa carpeta. Los vocabularios de infraestructura —hoy los estados de un trabajo, que `docs/architecture.md` declara— viven con su infraestructura y no cuentan | **Sin punto ciego relevante**: regla estructural sobre artefacto estático. El único escape sería crear un `Enum` en tiempo de ejecución a propósito | CI |
| VER-16 | El frontend nunca accede a la base de datos ni calcula estado de dominio | A | static analysis | El frontend no depende de ningún cliente de base de datos | Comprueba **dependencias**, no **lógica**: deducir el estado de una escena a partir de campos sueltos pasa | CI |
| VER-17 | Un módulo del frontend solo importa de capas FSD estrictamente inferiores | A | static analysis | El comprobador de FSD pasa sin violaciones | Comprueba **capas**, no **responsabilidades**: lógica de negocio dentro de `shared/` pasa | CI. **Hoy, en local:** `npx steiger src` desde `frontend/` (`PLAN-22` DP-4, E6); no hay CI |
| VER-18 | Una escena nunca se muestra sin su estado y sus hallazgos abiertos | T | unit testing | El componente no renderiza texto si falta el estado o la lista de hallazgos | Comprueba el **componente**, no la **API**; y presencia, no que sean los de estado `abierto` | **Cerrada por `PLAN-22` E7:** `frontend/src/entities/escena/ui/EscenaConEstado.test.tsx`. Sin `estado` o sin `hallazgos_abiertos` no se pinta el texto y se dice; una rendida se pinta distinta de una aceptada con `se_acepto_rindiendose`; un `sin_veredicto` se pinta como tal. El punto ciego de la API lo cubre `VER-105` |
| VER-19 | Un dato sin medir se muestra "sin medir", nunca como cero | T | unit testing | Con el valor ausente la interfaz pinta "sin medir"; con cero, pinta cero | Comprueba la **presentación**, no el **origen**: si el backend devuelve `0` donde no midió, la interfaz acierta y el dato miente | `frontend/.../tests/` |
| VER-20 | El Escritor devuelve texto y delta en la misma respuesta | T | contract testing | Una respuesta sin delta, o con delta fuera de esquema, se rechaza sin llegar a las puertas | Comprueba **forma**, no **correspondencia con el texto** *(lo cubre parcialmente `VER-39`)* | `features/generacion/tests/` |
| VER-21 | Todo cambio de un atributo obligatorio en `docs/definitions.md` lleva su migración en el mismo commit | A | CI/CD integration | Un commit que toque un atributo obligatorio sin añadir migración falla en CI | Comprueba que la migración **existe**, no que sea **correcta**: una migración vacía pasa | CI |
| VER-22 | Cada invariante `INV-01`…`INV-16` tiene un caso negativo **que de verdad la caza** | A | mutation testing | **Se desactiva la comprobación de la invariante y su caso negativo tiene que fallar.** Si sigue pasando, el caso no estaba probando nada | Prueba la **eficacia del caso**, no su **representatividad**: un solo caso negativo no cubre todas las formas de violar la invariante | CI |
| VER-23 | Ningún identificador publicado se reutiliza ni se renumera | A | static analysis | El conjunto de identificadores solo crece; lo retirado queda marcado obsoleto | Ve el **conjunto**, no el **significado**: cambiar el enunciado de `INV-14` manteniendo el número pasa | CI |
| **VER-38** | La severidad, el nivel y el tipo declarados en el código para cada invariante coinciden con la tabla de `docs/definitions.md` | A | static analysis | Se parsea la tabla de invariantes y se compara con el registro de `commons/invariantes/`: identificador, nivel, severidad y tipo, los cuatro iguales | Compara **declaración contra declaración**: no comprueba que el verificador respete la severidad que declara *(lo cubre `VER-11`)* | `commons/invariantes/tests/` |
| **VER-39** | Toda entidad que el delta declara aparece mencionada en el texto de la escena, y todo personaje mencionado está en `personajes_presentes[]` | T | contract testing | Por cada entrada del delta, su `nombre_canonico` o algún `alias` aparece en el texto; y ningún nombre del texto queda fuera de `personajes_presentes[]` | **Compara superficie léxica, no sentido**: un delta equivocado sobre alguien que **sí** está mencionado pasa | `features/consolidacion/tests/` |
| **VER-42** | Aplicar un delta es atómico: o entran todas sus entradas o ninguna | T | integration testing | Se fuerza un fallo a mitad de la aplicación y el estado queda exactamente como antes; el número de cambios aplicados coincide con el de entradas del delta | Cuenta **entradas aplicadas**, no la **corrección de cada una** *(lo cubre parcialmente `VER-39`)* | `features/consolidacion/tests/` |
| ~~**VER-57**~~ | ~~El presupuesto reservado por un trabajo que no vuelve se libera al expirar el margen~~ | — | — | **RETIRADO con `MF-25`** por `SPEC-14` C-1: comprobaba la liberación de una reserva que ya no existe. Identificador quemado | — | — |
| **VER-43** | Una escena no supera un número acotado de ciclos de regeneración | T | unit testing | Con un tope configurado, la escena número `tope+1` se detiene y se marca en vez de volver a generarse. **El valor del tope sale de medir, no se fija aquí** | Cuenta **ciclos**, no **diagnostica la causa**: detecta que gira, no por qué | `features/orquestacion/tests/` |
| **VER-45** | Toda ruta citada en un documento **existe tal cual está escrita, o encaja en la estructura declarada de `docs/architecture.md`** | A | static analysis | Se recorren las rutas entre acentos graves de todos los documentos del proyecto. Cada una existe en disco **o** corresponde a una carpeta del árbol de `docs/architecture.md`, resuelta contra su raíz declarada `backend/app/`. Cero rutas que no cumplan ninguna de las dos. Se eximen solo las ignoradas por git | Ve **existencia y forma**, no **corrección**: una ruta que existe, o que encaja en el árbol, pero que no es donde vive de verdad ese test, pasa igual. Y sigue sin comprobar que el documento citado diga lo que el citante cree | `harness/documentos/` |
| **VER-46** | Todo literal de enumeración citado en un documento se escribe como en la tabla de `docs/definitions.md` | A | static analysis | Se normalizan los tokens con forma de identificador —minúsculas, sin tildes, sin guiones bajos— y se comparan con los valores de la tabla: si dos normalizan igual y se escriben distinto, es una variante | No ve los valores que son **palabras sueltas y comunes** (`obra`, `escena`, `regla`, `vivo`): comprobarlas daría ruido sin señal, así que `nivel_de_evaluacion`, `severidad` y `tipo_de_verificador` quedan fuera de alcance | `harness/documentos/` |
| **VER-56** | Ninguna afirmación marcada con una condición de caducidad tiene su condición ya cumplida | A | static analysis | Se recorren las marcas `Caduca con:` de todos los documentos **y del código**, **salvo las que están dentro de un bloque de código en un fichero Markdown**: ahí una marca es una demostración del formato, no una afirmación, y `SPEC-05` haría fallar el build por definir su propia convención. En un fichero de código fuente sí cuentan, que es donde viven las dos de `commons/config.py`. Para cada marca real, la ruta **no** existe en disco, o la sección citada sigue diciendo lo que la marca supone. Cero marcas con su condición cumplida. **Falla el build**: un aviso que nadie mira es el mismo problema con otra cara | **Ve la condición, no el argumento.** Que `backend/` no exista no garantiza que la afirmación siga siendo cierta por el motivo que declara. Y **no detecta una afirmación condicional sin marcar**, que sigue siendo lectura humana: es `PC-13`. En código tampoco ve una marca que un refactor separó de su valor: ve que la marca existe, no que siga pegada a lo que describe | `harness/documentos/` |
| **VER-47** | La escena entregada es la que la escaleta pidió | T | contract testing | Tres comparaciones, las tres exactas: **(a)** toda entidad que el delta toca está entre los `personajes_presentes` declarados; **(b)** el `cambio_de_valor` del delta es el que la escaleta previó, par `{eje, signo}` contra par; **(c)** el `pov_usado` del borrador coincide en `persona` y `tiempo_verbal` con el `pov` asignado | Compara **lo que el agente declara**, no lo que el texto hace. Un Escritor que declare el POV correcto y escriba otro pasa: para eso está `VER-48` | `features/generacion/tests/` |
| **VER-53** | Todo hecho que un resumen cita fue establecido o revelado en la escena que resume | T | contract testing | `Resumen.hechos_clave` es una lista de identificadores de `HechoCanonico`: la intersección con los que la escena estableció o reveló tiene que ser total | Ve **identificadores citados**, no un resumen que **parafrasee** un hecho sin citarlo. Ese punto ciego es distinto del léxico de `VER-39`, que es lo que permitió admitirlo | `features/consolidacion/tests/` |
| **VER-54** | Los accesos de un `Lugar` no cambian sin que un `HechoCanonico` lo establezca | T | integration testing | `Lugar.accesos_y_salidas` es una lista de referencias: cualquier diferencia entre dos momentos `t` exige un `HechoCanonico` con su `escena_de_establecimiento` | Ve el **conjunto declarado**, no el texto: una puerta que la prosa describe y que nadie declaró sigue sin verse | `features/consolidacion/tests/` |
| **VER-49** | Un trabajo produce como máximo una llamada al modelo por intento registrado | T | integration testing | El intento se registra **antes** de llamar; al reanudar tras un reinicio, un intento ya registrado no vuelve a llamar. Se cuenta llamadas por `(trabajo, intento)` y nunca hay dos | Depende de que el registro **preceda** a la llamada. Si alguien invierte el orden, el validador deja de ver justo el caso que existe para cazar | `commons/trabajos/tests/` |
| **VER-52** | Todo recuento de hallazgos se expresa también como densidad por diez mil palabras | T | unit testing | Ninguna respuesta de la API que devuelva un recuento de hallazgos lo hace sin su densidad al lado | Normaliza por **longitud**, no por **dificultad**: un capítulo de acción y uno de transición no son comparables aunque tengan la misma densidad | `features/lectura/tests/` |

**Nota sobre la Regla 3 aplicada a `VER-05` y `VER-09`.** Eran las dos filas que
compartían implementación con lo que validaban.

- **`VER-05`** contaba tokens con el contador del ensamblador, así que un
  tokenizador equivocado se validaba a sí mismo y la llamada real se pasaba del
  límite sin que nadie lo viera. Ahora su referencia es **externa**: el `usage`
  que devuelve el modelo, o un segundo tokenizador independiente. Esto lo ata a
  `VER-41`, que es quien hace la reconciliación.
- **`VER-09`** comparaba dos ejecuciones del mismo aplicador de deltas, que
  coinciden incluso cuando las dos están mal. Ahora su referencia es una
  **implementación ingenua escrita solo para la prueba**: lenta, sin optimizar y
  legible de un vistazo. Cuando la de producción y la ingenua coinciden, el
  acuerdo significa algo.

---

## Nivel proceso — ¿se comportan los agentes de forma fiable?

| ID | Afirmación | Clase | Metodología | Criterio de salida | **Punto ciego** | Dónde vive |
| --- | --- | --- | --- | --- | --- | --- |
| VER-24 | Cada llamada de agente deja una traza consultable después del hecho | T | runtime observability / tracing | Toda llamada emite traza con agente, escena, niveles de contexto enviados y tokens consumidos | Comprueba que la traza **existe**, no que su contenido sea **real** *(lo cubre `VER-41` para los tokens, no para los niveles)* | `commons/modelo/` |
| VER-25 | El Juez no recibe el prompt ni el razonamiento del Escritor | T | unit testing | El contexto que llega al Juez contiene texto y rúbrica, y nada del prompt del Escritor | Comprueba **una** fuga concreta, no todas: el `Borrador` completo lleva `modelo` y `prompt_hash` y entra por otro campo | `features/verificacion/tests/` |
| VER-26 | El Juez no aprueba sistemáticamente lo que una persona rechaza | T | evals | Existe una medición publicada de concordancia entre Juez y persona. **El umbral se fija después de la primera medición, no antes** | Mide **concordancia**, no **corrección**: si persona y Juez se equivocan igual, la concordancia es alta | `harness/evals/` |
| VER-27 | La salida de cada agente valida contra su esquema tipado, o se rechaza | T | guardrails | Una salida fuera de esquema no llega nunca a la capa siguiente; se rechaza y se registra | **Una salida vacía es esquema válido**: no distingue "no encontró nada" de "no funcionó" *(lo cubre `VER-40`)* | `commons/modelo/` |
| VER-28 | No existe ningún camino de `planificada` a `consolidada` que no pase por `en_verificacion` | A | model checking | Exploración exhaustiva de la máquina de estados: ningún camino alcanzable salta la puerta | Verifica el **modelo**, no la **implementación**: un `UPDATE` directo a la base lo esquiva | `features/orquestacion/tests/` |
| VER-29 | Ninguna ruta del worker lleva una escena a `aceptada` **ni un capítulo a `cerrado`** | A | static analysis | El worker no tiene ninguna ruta de código que produzca ninguna de las dos transiciones, **ni ninguna que devuelva un capítulo de `cerrado` a `abierto`** (`RF-30`: esa transición no existe) | Comprueba que **el worker no puede**, no que quien firma sea **una persona**: sin autenticación, un script llama al endpoint igual. Vale para las dos puertas | `features/orquestacion/tests/` |
| ~~VER-30~~ **(obsoleta: `SPEC-26` v3 `RF-21`)** | El Escritor no viola las reglas de la amenaza bajo los prompts adversarios del corpus | T | red-teaming / adversarial testing | El corpus adversario se ejecuta en CI y ningún caso conocido vuelve a fallar | Cubre lo que **ya está en el corpus**: no inventa ataques nuevos, para eso sigue haciendo falta una persona | `harness/adversarial/` |
| VER-31 | Los cambios generados por agente pasan por la misma tubería que los escritos a mano | A | CI/CD integration | No hay ninguna ruta que publique cambios saltándose CI | Comprueba que **se pasa** por CI, no que CI **compruebe algo útil** | CI |
| **VER-40** | El Juez caza el defecto conocido del canario en cada lote de verificación | T | guardrails | Cada lote incluye una escena con un defecto plantado; si el Juez no lo señala, el lote se marca como no fiable | Detecta que el Juez **funciona**, no que **acierte**. Y un canario fijo puede acabar acertándose por memorización | `harness/evals/` |
| ~~**VER-41**~~ | ~~Los tokens que registra la traza coinciden con los que declara el modelo~~ | — | — | **RETIRADO por `SPEC-14` C-2.** No hay `usage` que reconciliar: el harness delega en sesiones y los tokens los reporta quien delegó. **El identificador queda quemado** porque el validador que lo sustituye **afirma otra cosa** —que el contador no está por debajo del suelo reconstruible— y reutilizar el id haría mentir a su historial. Lo sustituye `VER-61` | — | — |
| **VER-48** | El texto generado usa de hecho la persona y el tiempo verbal que el borrador declara | T | property-based testing | Señal morfológica sobre el texto —proporción de terminaciones de pasado frente a presente, marcas de primera frente a tercera— **contrastada contra el `pov_usado` que declara el `Borrador`**. Las dos fuentes son independientes: una es el texto, otra la declaración del agente (Regla 3). **La serie se registra desde el primer día; el umbral sale de medirla** | Sigue siendo una **heurística, no un análisis gramatical**: una escena con mucho diálogo en presente dentro de una narración en pasado puede dar falso positivo, y un narrador que cita mucho lo diluye | `features/verificacion/tests/` |
| **VER-55** | Ningún tic prohibido por la `GuiaDeEstilo` aparece en el texto generado | T | unit testing | `tics_prohibidos` es una lista de cadenas literales: ninguna aparece en el texto de ningún borrador | Busca **cadenas exactas**: una variante morfológica del tic —el mismo giro en otro tiempo verbal— no la ve. Es el precio de que la comprobación cueste una línea | `features/verificacion/tests/` |
| **VER-50** | La puntuación del Juez y su lista de hallazgos son coherentes entre sí | T | guardrails | Si la puntuación no es el nivel máximo de su `Rubrica`, la lista de hallazgos no puede estar vacía | **Confía en que la puntuación sea honesta.** Un Juez que puntúa alto y se calla el problema pasa: detecta la incoherencia, no la connivencia | `features/verificacion/tests/` |
| **VER-51** | La deriva de voz se mide **por personaje**, no solo globalmente | T | evals | Las mismas cuatro métricas de estilo —longitud media de frase, ratio de diálogo, densidad de adverbios, riqueza léxica— se calculan agrupadas por hablante, y la **distancia entre personajes** se registra por escena. **El mínimo sale de medir la serie, no se fija aquí** | Depende de **atribuir el diálogo** a su hablante. Un narrador que parafrasea en vez de citar no deja diálogo que medir, y ahí la voz puede derivar sin que nadie la vea | `harness/evals/` |

**`VER-29` ha cambiado de metodología.** Declaraba `human-in-the-loop review` y
su criterio era *"el worker no tiene ninguna ruta de código"*, que es análisis
estático puro: la etiqueta decía juicio humano y el trabajo lo hacía un
comprobador. Ahora dice lo que hace. Que la aceptación la ejecute **una persona**
no lo verifica nadie, y eso está anotado en "Puntos ciegos asumidos".

---

## No verificable hoy

| ID | Afirmación | Qué falta exactamente | Decisión abierta que lo desbloquea |
| --- | --- | --- | --- |
| VER-32 | La distancia estilométrica a las anclas se mantiene bajo umbral (`INV-15`) | Medir la distancia sobre un corpus y fijar el número con esa medición | `docs/definitions.md` § Decisiones abiertas → **"Umbrales"** |
| ~~VER-33~~ **(obsoleta: `SPEC-26` v3 `RF-21`)** | La varianza de la curva de dread supera el mínimo fijado (`INV-16`) | Lo mismo: primero medir, después fijar | `docs/definitions.md` § Decisiones abiertas → **"Umbrales"** |
| VER-34 | Cada agente cabe en su parte del presupuesto de contexto | Instrumentar el consumo por agente durante varias escenas. **Depende de `VER-41`**: sin reconciliar, mediría sobre un dato sin validar | `docs/architecture.md` § Decisiones abiertas → "Reparto de tokens por agente" |
| VER-35 | Cuando el Juez marca `INV-03` y la regla de continuidad no ve nada, gana el correcto | Solo la decisión de quién gana. **Ya no es hipotética**: con `SPEC-04`, `INV-03` es de tipo `regla` y el Juez es su desempate, así que las dos comprobaciones se ejecutan sobre la misma escena y la discrepancia se puede contar desde el primer día | `docs/definitions.md` → "Desempate juez vs. regla" |
| VER-36 | El coste por escena es sostenible para una obra completa | Coste real de una escena con `A-03`. **Depende de `VER-41`** | Ninguna. Nace aquí |
| VER-64 | **Con qué frecuencia `INV-03` bloquea una generación larga** | **Primer dato utilizable, y el mecanismo por fin puede dispararse y no dispararse.** Dos ejecuciones reales con `SPEC-15`, `SPEC-16`, `SPEC-17` y `SPEC-18` aplicadas: **2 bloqueos en 7 escenas generadas** (paró en `e4` la primera y en `e3` la segunda), y las demás pasaron limpias. Los dos bloqueos fueron **detecciones correctas**: *per-marta actua sobre hec-sotano-cerrado* y *sobre hec-reloj-sin-cuerda*, dos hechos que el registro dice que no conoce —el segundo porque el plan declaró explícitamente que los `ignora`—. Ya no es el cero de `F-30` ni el `1 de 1` sesgado de `F-32`: es una frecuencia preliminar de **~29% de escenas**, con `n=7`, que hay que volver a medir sobre una obra entera | Ninguna. Nace aquí |
| VER-37 | El reparto por niveles de `CLAUDE.md` basta para una escena real | **Medido cinco veces, y la respuesta ya no es "no se sabe": sobra holgadamente.** Con el material circulando de verdad —resumenes, fichas, escena anterior, estado y conocimiento— el contexto **crece solo**: 256 → 1.253 → 1.339 tokens en tres escenas, y **ni un recorte**. Eso es el **1,3% del techo de 100.000**. La pregunta con la que nació la fila —*¿basta el reparto?*— se contesta que sí con mucho margen; la que queda viva es otra y hay que escribirla aparte: **si el reparto por niveles de `CLAUDE.md` describe un sistema que no existe**, porque sus cifras (15.000 inmutable, 25.000 local, 20.000 recuperado) están dos ordenes de magnitud por encima de lo que los bloques ocupan | Ninguna. Nace aquí |

`VER-32` y `VER-33` **son la decisión "Umbrales" con dos fichas**: quien la cierre
en `docs/definitions.md` tiene que cerrarlas aquí en el mismo commit, y al revés.
Ninguna de las seis lleva umbral provisional, porque un umbral inventado que
queda escrito deja de distinguirse de uno medido.

**Tres de las seis dependen de `VER-41`**, que es nuevo y barato. Es la razón de
que suba tan arriba en el orden de implantación.

---

## Puntos ciegos asumidos

Huecos que **hoy no tapa nadie** y que se asumen a sabiendas. Un hueco escrito no
es un problema; uno que nadie escribió, sí. Cada uno con el fallo concreto que
dejaría pasar.

| # | Punto ciego asumido | Fallo concreto que se cuela | Por qué se asume |
| --- | --- | --- | --- |
| **PC-1** | **Ninguna comprobación que no ejecute el sistema ve la ejecución.** Doce validadores lo comparten —nueve de análisis estático, `VER-28` de *model checking* y `VER-21` y `VER-31` de integración en CI—: `VER-01`, `VER-12`, `VER-13`, `VER-14`, `VER-15`, `VER-16`, `VER-17`, `VER-21`, `VER-23`, `VER-28`, `VER-31`, `VER-38` | Un script de mantenimiento hace `UPDATE escena SET estado='consolidada'` y salta la máquina de estados entera | Taparlo pide auditoría en tiempo de ejecución sobre la base. No es caro, pero **no hay estado del mundo que auditar todavía**: `commons/db/` existe desde `PLAN-01` A3, y su única migración crea el registro de versiones. **Caducó en `PLAN-01` C5**: `features/consolidacion/` escribe ya estado del mundo, así que **hay base que auditar y nadie la audita**. El punto ciego deja de ser condicional y pasa a ser un hueco activo |
| **PC-2** | **Nadie comprueba que quien acepta una escena sea una persona.** `VER-29` solo comprueba que el worker no puede | Un script llama a `POST /escenas/{id}/aceptar` en bucle y consolida la obra entera sin que nadie la lea | `SPEC-01` §2.5 excluye la autenticación de la v1. Sin identidad no hay nada que comprobar. **Caduca con:** `SPEC-01` §2.5 |
| **PC-3** | **La fiabilidad del Juez no está medida**, y `VER-26` en verde significa "se midió", no "es fiable" | Tras `SPEC-04` **ninguna invariante `bloqueante` es de tipo `juez_llm`**: la única que queda del Juez es `INV-10`, de severidad `mayor`. El Juez ya no detiene una escena por sí solo | **Encogido dos veces, no cerrado.** `SPEC-03` hizo escribible la parte determinista de `INV-03`; `SPEC-04` la reclasificó a `regla` y dejó al Juez como desempate. Siguen abiertas dos vías por las que un modelo sin fiabilidad medida decide: **(a)** el desempate de `INV-03` es una decisión abierta, y si el veredicto del Juez cuenta, sigue deteniendo escenas `bloqueante`; **(b)** `SPEC-04` le dio una consecuencia nueva a `mayor`, así que `INV-10` —del Juez— ahora **bloquea el cierre de capítulo**, que es una puerta humana. Esta segunda vía no existía antes: parte del punto ciego encogió y otra parte se movió una puerta más arriba. Cierra cuando `VER-26` y la medida de estabilidad den un número |
| **PC-4** | **Los resúmenes pueden crecer hasta reconstruir la obra.** Se propuso un validador de ratio de compresión —al que `REV-02` llegó a dar el número **`VER-44`**— y **se rechazó por la Regla 2**: su punto ciego, mide longitud y no calidad, ya lo tienen seis validadores. **`VER-44` queda quemado**: el identificador está publicado y no se reutiliza | El Resumidor devuelve resúmenes casi tan largos como la escena. `VER-07` está en verde porque no hay texto de escena en el contexto, y el contexto lleva la obra entera de todos modos | Se asume hasta encontrar una comprobación con un punto ciego propio |
| **PC-5** | **`VER-39` compara léxico, no sentido.** Estrecha `BC-4` —que nada contrasta el delta con el texto— pero **no lo cierra**, y es fácil darlo por resuelto | El delta dice que Marta coge el cuchillo y en el texto lo coge Luis. Los dos nombres están mencionados, así que `VER-39` pasa. El hueco de `BC-4` sigue abierto para todo delta que se equivoque sobre alguien **sí** mencionado | La comprobación semántica exige un juez, y un juez sin fiabilidad medida no mejora esto |
| **PC-6** | **`VER-12` comprueba que el identificador existe, no que sea el correcto** | Se copia un verificador y no se cambia el id: todos los hallazgos salen como `INV-01` y el recuento por invariante miente | `VER-38` valida el registro, no qué id usa cada verificador al construir el hallazgo |
| **PC-7** | **`VER-21` comprueba que la migración existe, no que sea correcta** | Se añade un atributo obligatorio y se commitea una migración vacía. CI verde y la columna no existe | Validar que una migración hace lo que dice exige ejecutarla contra un esquema de referencia |
| **PC-8** | **El contador se valida contra un suelo, no contra una medida** | Una delegación se emite, no deja ningún artefacto y no se anota. Es indistinguible de una que nunca ocurrió, así que el suelo no la ve y `VER-61` pasa | **Cambió de forma con `SPEC-14` C-2**, no desapareció. Antes era *"valida el contador contra el proveedor, no contra la verdad"*; con delegación no hay proveedor que declare nada. Se tapa en parte anotando **toda delegación emitida antes de interpretarla**, que es lo que `main` aprendió corrigiéndolo: su contador decía 59 donde habían sido 61 |
| **PC-9** | **Nadie detecta que un agente rellene con invención lo que el recorte dejó fuera** (`MF-07`) | El ensamblador recorta la ficha de Marta; el Escritor le atribuye unos rasgos físicos que nunca estuvieron en el canon. En el capítulo siete tendrá otros | Distinguir *"lo sabía"* de *"se lo inventó"* exige comparar la prosa con la ficha, y eso es un juez. ~~Lo que sí se hace y acota el daño: la traza de `VER-24` registra qué fichas quedaron fuera, así que el fallo pasa de invisible a atribuible~~ — **esa contención no existía** (`F-49`). La traza vive en memoria y muere con el proceso, y **el diagnóstico siempre se hace después**: cuando alguien va a preguntar qué se dejó fuera, no queda nada que mirar. Así que valía *dentro de la misma ejecución*, que para lo que este punto ciego necesita es **no valer**. Un punto ciego que se dio por acotado por un mecanismo que no sobrevive, y que por eso figuró como contenido durante semanas |
| ~~**PC-10**~~ | ~~El delta no puede expresar un cambio de topología~~ | ~~La casa tenía una sola salida en la escena 4 y en la 11 aparece una puerta trasera que nadie plantó~~ | **CERRADO por `SPEC-03`.** `Lugar.accesos_y_salidas` pasó a ser una lista de referencias, así que el cambio de topología es una diferencia de conjuntos. Lo comprueba `VER-54`. El identificador no se reutiliza |
| ~~**PC-11**~~ | ~~Nada contrasta un resumen con la escena que resume~~ | ~~Un resumen bien formado menciona un hecho que la escena no contiene y entra en el contexto como si fuera canon~~ | **CERRADO por `SPEC-03`.** `Resumen.hechos_clave` pasó a ser una lista de identificadores, así que la comprobación es una intersección de conjuntos y tiene punto ciego propio —no ve la paráfrasis sin cita—, que es lo que la Regla 2 exigía. Lo comprueba `VER-53` |
| **PC-12** | **Un validador puede marcar como defecto lo que es correcto, y ningún validador vigila a los validadores** (`MF-23`) | `VER-46` marcó como defecto la prosa de este documento que explicaba el defecto, porque no puede distinguir una cita de una demostración | **No se resuelve con una capa que vigile a los validadores, y conviene dejarlo escrito antes de que alguien lo intente: esa capa también fallaría, y la siguiente, y no hay torre que aguante.** Lo que lo contiene es el **caso negativo obligatorio**: si cada validador demuestra que falla cuando debe, su comportamiento está acotado por abajo sin necesidad de ninguna capa encima. Un validador sin caso negativo es el que de verdad deja `MF-23` suelto |
| **PC-13** | **Un criterio de salida puede remitir a algo que no existe, y no hay forma automática razonable de detectarlo** (`MF-24`) | `VER-23` dice *"el conjunto de identificadores solo crece"* y no dice respecto a qué. El validador se implementa, pasa, y la fila cuenta como cobertura de algo que nunca se acordó | **Se evaluó el validador y se rechazó por ruido.** La comprobación automatizable —*todo criterio cita al menos una referencia que resuelve*— marcaría **32 de los 54 criterios**, y casi todos son prosa perfectamente anclada: *"se encola, se mata el proceso, se levanta"* no cita nada y no le hace falta. Lo que distingue un criterio colgante es que usa un artículo definido sin antecedente —*"la prioridad declarada"*, *"solo crece"*— y eso es una propiedad del lenguaje, no de los tokens. **Lo que sí lo contiene es la lista de comprobación de la revisión**, que es como se encontraron los dos casos: una persona leyendo. Se añade allí *"ningún criterio remite a algo que no esté escrito"* |
| **PC-14** | *(quemado)* | Se propuso en sesión —«el Auditor solo ve resúmenes»— y **se retiró al comprobarlo**: `INV-09` compara filas de `Presagio`, no líneas de un resumen. El identificador no se reutiliza | — |
| **PC-15** | **Nadie comprueba el tope global de llamadas.** `SPEC-11` `C-1` lo declara y ningún validador lo mira | Un bucle mal cerrado encadena llamadas y el contador no frena porque nadie ha comprobado que se consulte antes de cada una | **Barato en cuanto exista la traza**: contar llamadas por escena y por obra y cruzarlas contra el tope. No se hace ahora porque no hay traza |
| ~~**PC-16**~~ | ~~Nadie comprueba que el modelo del Juez sea fijo dentro de una obra~~ | ~~El modelo cambia a mitad y las puntuaciones dejan de ser comparables~~ | **CERRADO por `VER-62`.** Dejó de ser opcional al elegir Fable como Escritor: **un modelo puede enrutar a otro por sus propias salvaguardas**, así que el cambio puede ocurrir dentro de una obra sin que nadie lo pida. Una regla que depende de que nadie la incumpla adrede no vale cuando el incumplimiento puede ser automático. El identificador no se reutiliza |
| **PC-17** | **Nadie comprueba que el Juez no vea las reglas del proyecto.** `SPEC-11` `C-4` lo declara, y es la regla que sostiene que su desempate valga algo | El prompt del Juez incluye los enunciados de las invariantes. Su veredicto pasa a confirmar lo que la regla ya dijo, y `VER-35` mide una concordancia que no significa nada | **No se puede comprobar hasta que haya un Juez.** Es el más caro de los tres y el que más sostiene: un desempate que ve lo mismo que la regla no desempata, confirma. **Y desde `SPEC-14` tiene un mecanismo concreto y frágil**: en esta arquitectura la regla se implementa con `omitClaudeMd: true` en la definición del subagente, que necesita una versión mínima de la herramienta y **lo que no reconoce lo ignora en silencio**. Una decisión de aislamiento que depende de una opción que puede fallar callando. **Y el mecanismo que se daba por hecho no funciona** (`F-20`): `omitClaudeMd` se ignora en silencio. El aislamiento pasa a conseguirse por directorio de trabajo, que sí se puede comprobar — pero nadie lo comprueba todavía, así que el punto ciego sigue abierto y ahora con un mecanismo distinto |
| **PC-18** | **El comprobador de `A-02` ve importaciones, no acoplamiento por datos** | Una feature lee la tabla de otra con un `JOIN` y no hay ningún `import` que delatarlo. Pasó en `PLAN-01` F3 (`F-28`) | Taparlo pide asociar cada tabla a su feature dueña y comprobar que nadie más la nombre en SQL. Es barato **en cuanto las tablas se declaren por feature**, y hoy cada repositorio crea las suyas sin decir de quién son |

---

## Juicio que en realidad es una comparación

`docs/definitions.md` clasificaba cuatro invariantes como `juez_llm` y este
documento tenía dos validadores etiquetados como juicio. Al revisarlos, varios
resultaron ser aritmética disfrazada. **`SPEC-04` reclasificó tres de las cuatro
invariantes**, así que hoy solo queda `INV-10` de tipo `juez_llm`.

| Caso | Qué dice que es | Qué es en realidad | Estado |
| --- | --- | --- | --- |
| **`VER-29`** | `human-in-the-loop review` | Análisis estático: *"el worker no tiene ninguna ruta de código"* | **Corregido en este documento** |
| **`VER-30`** | Inspección (clase `I`) | Regresión sobre un corpus, que es un test | **Corregido en este documento** |
| **`INV-14`** — *"cada deterioro es monótono, o su reversión está justificada"* | `juez_llm` | `Deterioro.serie_por_escena` es una serie numérica: la monotonía es una comparación. Solo la cláusula de la justificación necesita criterio | **Aplicado por `SPEC-04` C-4**: `tipo = regla`, con juez de desempate solo ante una reversión |
| **`INV-11`** — *"el grado de explicación acumulado no supera el fijado"* | `juez_llm` | Un conteo: cuántos `HechoCanonico` sobre la amenaza están revelados al lector frente a `grado_de_explicacion_permitido`. Solo lo implícito necesita juez | **Aplicado por `SPEC-04` C-5**: `tipo = regla`, con juez de desempate para las revelaciones implícitas |
| **`INV-03`** — *"ningún personaje actúa sobre un hecho que no conoce en `t`"* | `juez_llm` | Comparación de identificadores contra el registro de conocimiento en `t`, más el orden de `t_fabula` frente a `escena_de_establecimiento`. Solo decidir que el personaje **actúa sobre** el hecho se ve leyendo | **Aplicado por `SPEC-04` C-6**: `tipo = regla`, **severidad `bloqueante` intacta**, con juez de desempate |
| **`VER-26`** | `evals` con persona | La **estabilidad** del Juez se mide sin nadie: misma escena N veces, varianza del veredicto. Un juez que se contradice consigo mismo se descarta sin corpus | **Pendiente**: no cambia la fila, añade un paso previo |

`INV-10` admite un filtro determinista previo —comprobar si el delta registra
el coste de invocación cuando la escena invoca la amenaza— pero conserva una
parte central que solo el juez resuelve, así que sigue siendo `juez_llm` con
razón. `SPEC-04` la dejó fuera a propósito: no es una comparación disfrazada.

**Reclasificar no es ablandar.** `INV-03` conserva su severidad `bloqueante`.
Lo que cambió es **quién decide**, no **cuánto pesa**: sigue deteniendo la
escena en la puerta, y lo que ya no hace es detenerla apoyándose en un modelo
cuya fiabilidad nadie ha medido. Leer la reclasificación como una rebaja es el
error fácil, y sería el camino corto al anti-patrón de bajar una `bloqueante`
para desatascar.

---

## Casos negativos

Toda fila de clase `T` necesita una entrada que la viole a propósito. Una regla
que nunca ha fallado en las pruebas no está verificada, solo declarada.

| ID | Caso negativo | Qué debe cazarlo |
| --- | --- | --- |
| VER-02 | `rol_dramatico = "narrador"`, y un `Enum` al que le falta un valor de la tabla | Validación del esquema y la comparación de cardinalidad |
| VER-03 | Un generador que tarda mucho más que el tiempo de respuesta del endpoint | El test comprueba que la respuesta llega igualmente |
| VER-04 | Matar el proceso con un trabajo a medias | El worker lo retoma al arrancar |
| VER-05 | Un contexto que el contador de producción da por bueno y el `usage` real desmiente | La reconciliación falla |
| VER-06 | Un contexto que excede por poco, con el nivel inmutable al máximo | Se recorta por prioridad y ningún bloque queda partido |
| VER-06 | Un recortador que recorre los seis niveles del presupuesto en vez de los bloques de `SPEC-01` §2.4 | Falla: el registro de conocimiento desaparece junto a las fichas, y con él la entrada de `INV-03` |
| VER-07 | Una obra con cuarenta escenas consolidadas | El contexto no contiene el texto de la escena 3 |
| VER-08 | Una consulta de similitud cuyo vecino más próximo sea un texto completo | La consulta no puede devolverlo: no está indexado |
| VER-09 | Una secuencia de deltas con un movimiento y una muerte en el mismo `t` | El aplicador de producción y el de referencia coinciden |
| VER-10 | Consolidar una escena cuyo delta no se ha aplicado | La transición se rechaza |
| VER-11 | Un hallazgo `mayor` en una escena por lo demás correcta | La escena sigue; el hallazgo queda abierto y visible |
| VER-12 | El Auditor levanta un `INV-13` con el `verificador` del Verificador incremental | Falla: el recuento por agente mentiría y nadie se enteraría |
| VER-18 | Una respuesta de la API sin la lista de hallazgos | El componente no renderiza el texto |
| VER-19 | Un contador de tokens ausente frente a uno con valor cero | "sin medir" en el primero, `0` en el segundo |
| VER-20 | Una respuesta del Escritor con texto y sin delta | Se rechaza antes de las puertas |
| VER-24 | Una llamada que falla con excepción | También deja traza |
| VER-25 | Un contexto de Juez al que se le cuela el prompt del Escritor | El test lo detecta |
| VER-26 | Una escena mala que el Escritor considera buena | Se mide si el Juez la aprueba |
| VER-27 | Una salida del modelo con un campo de más | Se rechaza y se registra |
| VER-29 | Una ruta del worker que intente llevar una escena a `aceptada` | El comprobador estático la encuentra |
| VER-29 | Una ruta que devuelva un capítulo `cerrado` a `abierto` | El comprobador estático la encuentra: `estado_de_capitulo` no tiene esa transición |
| **~~VER-30~~ **(obsoleta: `SPEC-26` v3 `RF-21`)**** | Un prompt adversario del corpus que ya hizo fallar al Escritor una vez | Vuelve a comprobarse en cada ejecución de CI |
| **VER-39** | Un delta que declara una muerte de un personaje que no aparece en el texto | La comprobación de menciones falla |
| **VER-40** | Un Juez que devuelve lista vacía | El canario no aparece señalado y el lote se marca |
| **VER-41** | Una traza que registra el presupuesto planificado en vez del consumido | La diferencia con el `usage` no es cero |
| **VER-42** | Un fallo forzado a mitad de aplicar un delta de tres entradas | El estado queda como antes; no hay una entrada aplicada |
| **VER-43** | Una escena que falla la misma invariante `tope+1` veces | Se detiene y se marca en vez de regenerarse |
| **VER-45** | Un documento que cita la ruta del modelo de dominio con el typo que ya ocurrió —*defintions*, sin la segunda i— junto a la ruta correcta | El validador marca la rota y deja pasar la buena |
| **VER-46** | Un diagrama que escribe los estados en PascalCase, y una fuente del miedo escrita con tilde donde la tabla la tiene sin ella | El validador los marca y dice cuál es la escritura de la tabla |
| **VER-47** | Un delta que declara la muerte de un personaje que la escaleta no puso entre los presentes de esa escena | La comparación de alcance falla |
| **VER-48** | Una escena escrita en presente cuando la guía de estilo asignó pasado | La proporción de terminaciones se aleja de la serie registrada |
| **VER-49** | Matar el worker después de registrar el intento y antes de recibir la respuesta | Al reanudar no vuelve a llamar: el intento ya está registrado |
| **VER-50** | Una respuesta del Juez con puntuación intermedia y la lista de hallazgos vacía | La incoherencia se detecta y el lote se marca |
| **VER-51** | Dos personajes cuyas cuatro métricas de estilo convergen escena a escena | La distancia entre hablantes cae en la serie, aunque la deriva global de `INV-15` no se mueva |
| **VER-52** | Un informe que da un recuento de hallazgos sin su densidad al lado | El informe se rechaza |
| **VER-53** | Un resumen que cita un `HechoCanonico` que su escena ni estableció ni reveló | La intersección no es total |
| **VER-54** | Un `Lugar` que gana un acceso entre dos momentos `t` sin `HechoCanonico` que lo establezca | La diferencia de conjuntos se detecta |
| **VER-55** | Un texto que contiene una de las cadenas de `tics_prohibidos` | La búsqueda literal la encuentra |

> **Convención que imponen `VER-45` y `VER-46`, y que conviene conocer antes de
> editar este documento:** los acentos graves significan *"este es el literal"*.
> Una ruta rota o un literal mal escrito **no se citan entre acentos graves**,
> porque entonces el validador los lee como una cita y los marca. Para hablar de
> una escritura equivocada se usa cursiva o prosa. Los ejemplos concretos van en
> los ficheros de caso negativo del harness, que quedan fuera del alcance de los
> dos validadores precisamente por esto. Ver "Lo que se aprendió al implementar".

---

## Lo que se aprendió al implementar

Cinco validadores —`VER-23`, `VER-28`, `VER-38`, `VER-45`, `VER-46`— se
escribieron de verdad, con su caso negativo, y después se retiraron: la entrega
de esta fase es este documento y la spec, no el harness. **Lo que aprendieron sí
se queda**, porque es lo único de todo esto que no se puede reconstruir
leyendo.

### La convención de los acentos graves

Lo más práctico que salió, y afecta a cualquiera que edite este documento.

Al añadir las filas de caso negativo de `VER-45` y `VER-46`, **los dos
validadores fallaron contra la prosa de este mismo documento**: los ejemplos
rotos estaban citados entre acentos graves, y el acento grave significa *"este es
el literal"*.

No tiene arreglo técnico: **un validador que comprueba literales citados no puede
distinguir una cita de una demostración.** De ahí la convención —un literal
equivocado se escribe en cursiva o en prosa, nunca entre acentos graves— y de ahí
que los ejemplos concretos vivan en los ficheros de caso negativo, fuera del
alcance de los validadores. Conviene saberlo porque **el fallo aparece en un sitio
que no es el que se ha tocado**.

### `VER-45` y `VER-46` cazaron dos defectos reales en su primera ejecución

Se abrieron porque eran las dos únicas clases de defecto con historial en este
repositorio. Encontraron dos el primer día:

| Validador | Qué encontró | Estado |
| --- | --- | --- |
| `VER-45` | Una referencia a la carpeta de revisiones en su ubicación antigua, que quedó colgando al moverla de `specs/` a `docs/` en el commit anterior | **Corregido**. Es exactamente la clase de defecto que el validador existe para cazar, y ocurrió otra vez mientras se escribía |
| `VER-45` | Una ruta de la columna "Dónde vive" que no corresponde a ninguna carpeta del árbol de `docs/architecture.md` | Falla: no existe en disco y tampoco encaja en la estructura, que es justo el hueco por el que pasaban las rutas mal escritas |
| `VER-46` | El diagrama de §2.2.1 de `SPEC-01`, que sigue escribiendo los estados en PascalCase | **Abierto**: es `D1-2` de `REV-01` y su corrección va junto con la decisión `C-1`, así que no tocaba arreglarlo |
| `VER-56` | Se crea `backend/` con una afirmación marcada `Caduca con:` `backend/` todavía en pie | Falla: la condición que sostenía la afirmación ya se cumplió |
| `VER-56` | El ejemplo del formato dentro del bloque de código de `SPEC-05` | **No** falla: es una demostración, no una afirmación |
| `VER-57` | Un worker muere con la reserva del Escritor tomada y otro trabajo esperando | El segundo no arranca hasta el margen; si arranca antes, el techo se repartió mal |
| `VER-58` | Una escena de 944 palabras con `longitud_objetivo` de 1.200 a 2.200 | Falla, aunque los jueces la aprueben: es el caso de la Regla 2 |
| `VER-59` | Una forma reducida del bloque de estado que retire `ubicaciones` | Falla: `INV-02` la lee y es `bloqueante` de escena |
| `VER-60` | Un ensamblador que normaliza comillas al montar el manuscrito | Falla: el informe describiría un texto que ya no es el entregado |
| `VER-61` | Una delegación que se emitió, dejó artefacto y no se anotó | Falla: el contador queda por debajo del suelo |
| `VER-62` | La escena 3 usa `{fable, haiku}` y la 40 usa `{opus}` | Falla: dos puntuaciones de conjuntos distintos no son comparables |
| `VER-63` | Un módulo nuevo que ninguna prueba importa ni directa ni indirectamente | Falla y lo nombra |

### Lo que el documento no aguantó al llevarlo a código

Quince hallazgos. Los que siguen abiertos son decisiones pendientes sobre este
documento, no sobre el código.

| # | Hallazgo | Estado |
| --- | --- | --- |
| F-1 | **`VER-38` no puede contrastar la severidad**, que es un tercio de su enunciado: solo existe en `docs/definitions.md`, no hay segunda fuente. Se implementó cruzando nivel y tipo, y comprobando la severidad contra su propio vocabulario | Abierto |
| F-2 | El "Dónde vive" de `VER-01` y `VER-38` apuntaba a `harness/esquema/`, que no era ninguna carpeta de la estructura del harness | **Cerrado por `SPEC-06`.** `VER-01`, `VER-38` y `VER-22` se movieron al backend y a `CI`. Al reubicar las seis restantes salió la causa real: **no existía ninguna estructura declarada del harness**, se borró con la carpeta, así que ninguna ruta bajo `harness/` se podía juzgar. `docs/architecture.md` § "El harness" la declara ahora —`documentos/`, `evals/`, `adversarial/`— con el criterio que decide si un validador va al backend o al harness |
| F-3 | **`VER-01` no se puede implementar ni a medias**: compara esquemas Pydantic con fichas de clase y sin `backend/` falta la mitad de la comparación | Abierto |
| F-4 | No había fila para "las rutas citadas existen" | **Cerrado**: es `VER-45` |
| F-5 | No había fila para "los literales citados son los de la tabla" | **Cerrado**: es `VER-46` |
| F-6 | **`VER-28` no dice de qué documento sale la máquina de estados.** Está en la tabla de `docs/architecture.md` y en el diagrama de `docs/domain-knowledge.md`; se eligió la tabla, y la eligió el implementador | Abierto |
| F-7 | **`VER-23` no dice contra qué compara.** Se eligió el commit anterior, con un agujero conocido: un borrado en dos commits ya no se ve | Abierto |
| F-8 | **`VER-23` se salta en vez de fallar cuando no hay historial**, así que puede aparecer en verde sin haber comprobado nada. El documento no dice qué hacer | Abierto |
| F-9 | No se pudo escribir la comprobación de que el nivel del agente cuadre con el de la invariante: `docs/architecture.md` no declara el nivel de cada agente, y decidirlo es de dominio. Sigue asignando invariantes de obra al Verificador de reglas | Abierto |
| F-10 | **Los datos normativos en prosa se rompen al editar.** La skill `harness-invariantes` afirma el reparto de tipos en letra; es parseable y cualquier reescritura del párrafo lo rompe | Abierto |
| F-11 | **El escape de guion bajo de las tablas markdown es una trampa**: en una tabla y en el texto son cadenas distintas para la misma cosa, y todo comparador tiene que normalizarlo antes | Abierto |
| F-12 | **`VER-44` estaba quemado y casi se reutiliza.** `REV-02` se lo asignó al validador de ratio de compresión que luego se rechazó. **`VER-23` no lo habría cazado**: su punto ciego es que ve el conjunto de identificadores, no su significado | Cerrado por el número elegido |
| F-13 | **La columna "Dónde vive" es un plan, no una referencia**, y el documento no lo dice en ninguna parte. Un lector razonable toma esas rutas por existentes | Abierto |
| F-14 | **`VER-46` no ve los valores que son palabras comunes.** Si alguien escribe la severidad con mayúscula inicial en un documento, no lo detecta | Abierto, y consta como punto ciego de la fila |
| F-15 | **Un validador que no distingue una cita de un uso marca el texto que explica el defecto.** Vale para los dos soportes, y se descubrió en cada uno por separado: en documentos son los **acentos graves** —un literal equivocado citado entre ellos lo marcan `VER-45` y `VER-46`, así que se cita en cursiva—; en código son los **docstrings** —la prueba que impide el eco de `VER-41` buscaba la cadena `tokens_declarados` y falló contra el docstring que explica por qué ese módulo no la escribe, así que busca **escrituras**—. Era un patrón, no una curiosidad del markdown | Cerrado por convención en los dos |
| F-29 | **La lista de identificadores se derivaba del registro de conocimiento, que solo crece revelando, que necesita la lista.** Un punto muerto que hizo `INV-03` inejecutable sin que nada fallara. Al arreglar `F-21` puse en el prompt `hechos = los del registro de conocimiento`, y el registro **arranca vacío**: el prompt decía *"hechos: (ninguno)"*, el modelo no citaba ninguno —correctamente—, no había revelaciones, y el registro seguía vacío. Seis escenas reales: **conocimiento 0, fichas 0**. Funcionó en `E5b` solo porque el fixture lo sembraba a mano. La causa de fondo es que **confundí "qué hechos existen" con "quién los sabe"**: lo primero lo declara el plan de la obra —los `HechoCanonico`— y lo segundo es lo que `INV-03` comprueba | Cerrado por `SPEC-15`: los hechos se declaran y el prompt los lleva. **El efecto todavía no se puede medir**, porque arreglarlo destapó `F-31` |
| F-30 | **Cero bloqueos no es una medida de cero.** La ejecución de seis escenas terminó sin que `INV-03` bloqueara ni una vez, y ese número **no vale**: el mecanismo nunca se ejerció, porque no hubo una sola revelación que comprobar (`F-29`). Apuntarlo como *"`INV-03` bloquea 0 de 6"* sería exactamente el verde falso que este documento persigue — **un validador que no puede dispararse no está midiendo cero** | Abierto: `VER-64` sigue sin dato |
| F-53 | **La forma de la obra vivia dentro del guion, y por eso nadie pudo verla para discutirla.** Durante diez capitulos el harness estuvo modelando **diez obras** en vez de una con diez capitulos, y la diferencia no se noto porque **no habia ningun sitio donde mirarla**: estaba repartida en un bucle que llamaba diez veces a `guardar_escaleta` con un identificador distinto. Las consecuencias se fueron descubriendo de una en una y ninguna apuntaba a la causa —`escena.capitulo` siempre `NULL`, el cierre evaluando obras de seis escenas, la cronologia sin ningun corte que cruzar, los diez `HechoCanonico` colisionando bajo `cap-10` y dejando a siete capitulos generando con *"hechos: (ninguno)"*—. **Un fichero de configuracion con `capitulos: 10` dentro de `forma` lo habria hecho visible el primer dia.** No es la familia de los datos ausentes: es una forma **no expresada**, y lo que no se puede leer en un sitio no se puede discutir, ni corregir | **Cerrado**: `config/brief.json` y `config/sistema.json`, validados con esquemas Pydantic y `extra=forbid`. La huella del brief se guarda junto al commit en `procedencia`, asi que una tanda se repite exactamente en vez de aproximadamente |
| F-54 | **La salvaguarda de `F-52` funcionaba por accidente del caso, no por diseño: con la tabla presente y vacía, Lean aprobaba.** En `F-52` el generador murió con `no such table` y eso evitó el verde falso. Pero **una tabla vacía es un escenario más probable que una ausente** —en cuanto `evento_cronologico` exista y una obra no la llene, se está en ese caso— y ahí no moría nadie: cero eventos, cero violaciones, *«la versión puede publicarse»*. El veredicto era correcto y la conclusión falsa. Lo único que avisaba era el informe de cobertura del generador, que **depende de que alguien lo lea**, y una salvaguarda que depende de eso no es una salvaguarda | **Cerrado.** Es la Regla 8 aplicada al propio validador formal, que es donde no la habíamos aplicado: `verificar-real` tiene ahora **tres salidas y no dos** — `0` limpio, `1` con violaciones y `2` **sin veredicto**. El generador emite una `Cobertura` junto a la obra y Lean decide con ella, así que **la negativa a dar veredicto vive en el verificador** y no en un informe que hay que acordarse de mirar. Medido en las dos direcciones: con la base de demo, cinco eventos y salida 1; con una base cuyas tablas existen y están vacías, *«SIN VEREDICTO: cero eventos convertidos»* y salida 2. Es la misma decisión que la sesión de backend tomó en la puerta de capítulo con `sin_fecha_legible`, tomada aquí un día después y por el mismo motivo |
| F-55 | **El orden del discurso se deducía ordenando `capitulo` como cadena, y eso se rompe exactamente al llegar al décimo capítulo.** Con `cap-1`…`cap-9` el orden alfabético coincide con el numérico y todo parece correcto; con diez, `cap-10` se coloca **entre `cap-1` y `cap-2`**. El eje del discurso queda mal y `INV-08` / `L-1` levantan inversiones que no existen. Y en el repositorio **conviven las dos convenciones** —`cap-01` y `cap-1`—, así que no se puede confiar en el relleno con ceros. Lo destapó el cambio anunciado del guion, que pasa a modelar **una obra de diez capítulos** en vez de diez obras: hasta ahora ninguna obra había llegado al décimo | **Corregido en `specs/lean/`**, que extrae el número final del identificador; un capítulo del que no se deduce el orden **no se adivina**: se cuenta y deja la obra `sin veredicto` (`F-54`). **Abierto en el backend:** `cronologia/consultas.py` → `orden_temporal` ordena con `key=lambda e: (e["capitulo"], e["id"])` y tiene el mismo defecto, así que la puerta de capítulo levantaría el fallo en la primera obra de diez. **Es el escenario que `F-50` describe llegando por otra puerta**: inversiones falsas en la primera obra, y alguien concluyendo que `INV-08` da falsos positivos y relajándola. Avisado a la sesión de backend antes de la repetición |
| F-56 | **Diez capítulos modelados como diez obras, y con eso nada de lo construido significaba nada.** El guion daba de alta cada capítulo como una `Obra` distinta. `escena.capitulo` no tenía a qué apuntar —quedaba `NULL` en las sesenta—, el cierre de capítulo evaluaba una obra de seis escenas, la cronología no cruzaba ningún corte, y los diez `HechoCanonico`, declarados diez veces, colisionaban en la misma clave. **`F-39` solo mordió porque eran diez obras**: con una, no hay dos claves que choquen. Nadie lo notó durante diez capítulos porque **la forma de la obra vivía dentro del guion**, donde no se puede discutir: un fichero de configuración la habría hecho visible el primer día | **Cerrado**: una obra con sus diez capítulos registrados en `capitulo` con su orden, los hechos declarados una vez, y `generar_obra(capitulo=...)` para poder generar y reintentar capítulo a capítulo sin perder la puerta de capítulo. La configuración sale del guion en trabajo aparte |
| F-57 | **Dos marcas `Caduca con:` caducaron en silencio el día que la ruta que nombran empezó a existir.** `TOPE_INTENTOS_ESCENA` y `TOPE_DELEGACIONES_OBRA`, en `commons/config.py`, llevan `Caduca con: harness/evals/`, y esa carpeta existe desde que la Fase 0 del examen fusionó `verificacion-formal` (commit `b6732e7`). La convención de `SPEC-05` dice que entonces el build falla y hay que volver a mirar los números; **no falló nada**, porque ninguna prueba recorre las marcas del código: `test_cola.py` solo cuenta cuántas hay. Es `VER-56` sin implementar en el lado del código, y el mismo patrón que `F-54`: una salvaguarda que existe en el documento y no en la ejecución | **Abierto.** Falta la prueba que recorra las marcas `Caduca con:` de `backend/app/` y falle si la ruta existe; y, con ella en rojo, revisar los dos números a la luz de lo que ya se ha medido. Encontrado al preparar `PLAN-25` E3 |
| F-58 | **El Escritor nunca ha visto el contexto que el harness le monta: recibe los tamaños de los bloques, no su texto, y en todas las escenas.** `obra.generar_obra` monta los bloques (`ensamblado.montar`), calcula sus tokens (`ensamblado.tamanos`) y pasaba **los tamaños** a `ciclo.ejecutar`; `bucle._prompt` los volcaba en «LO QUE YA ES VERDAD EN LA FICCION» como `[["inmutable", 66], ["escena_anterior", 0], ...]`. **Cuidado con la lectura**: no es que falte contexto por ser el primer capítulo. Que la escena anterior esté a cero en el capítulo 1 es correcto; que el bloque inmutable sea un número no lo es nunca, y en el capítulo 7 llegaba igual `inmutable: 66` en vez de la premisa. Así que la premisa, el estilo, el género, el tono, la escena anterior, los resúmenes y las fichas **no llegaban al modelo**. La sinopsis de cada capítulo tampoco: `guardar_escaleta` no la guardaba y el objetivo era solo `{eje, signo}`. Y `aplicar_recorte`, que recorta el texto según el plan de presupuesto, **existía y nadie la llamaba** fuera de sus pruebas. Aparece desde `9089f52`. Consecuencias: la obra de diez capítulos se escribió sin premisa ni memoria, y el contexto «medido» de 1.339 tokens de `CLAUDE.md` medía lo que se montaba, no lo que se enviaba: sesgaba hacia la conclusión cómoda de que sobra presupuesto. No falló nada porque ninguna prueba miraba el contenido del prompt, solo que el recorte cuadrara | **Cerrado**: el texto de los bloques, ya recortado con `aplicar_recorte`, llega al prompt en el orden de `SPEC-01` 2.4, un bloque vacío se dice vacío y, sin textos, el prompt lo dice en vez de mandar números; la sinopsis del plan viaja como texto del beat y llega en «que pasa». Lo sostienen `test_contexto_llega_como_texto.py`, que mira el **contenido** del prompt en todas las escenas, y `test_escribir_encadena_plan_montaje_y_generacion`, que fue xfail estricto y se puso en rojo sola al arreglarlo. **Queda sin medir** el contexto enviado de verdad: `CLAUDE.md` lo dice |
| F-59 | **La palabra vetada «coño» se normalizaba en «con», así que vetaba la preposición y ningún capítulo podía pasar `INV-21`.** `normalizar.raiz` quita los acentos con NFD —la ñ pierde la tilde y queda n— y después el género gramatical en palabras de más de tres letras: «coño» → «cono» → «con». Y «con» se queda en «con». En la primera ejecución real (`PLAN-26` E13) el capítulo 1 se reescribió dos veces por «con» y se paró por `parada_por_vetada`, con 47 filas de coincidencia en el audit log. Las pruebas de E2 no lo cazaron porque ninguna vetada de prueba llevaba ñ ni se quedaba en una palabra funcional al recortarla: es el falso positivo de `caso`/`casa` que el módulo aceptaba como «el lado ruidoso», pero en su peor forma | **Cerrado**: `sin_acentos` conserva la ñ, que no es una tilde sino otra letra, así que «coño» se reduce a «coñ» y ya no a «con». Y una prueba **barre la lista real** de `config/vetadas.json` contra las palabras más frecuentes del español: es la que habría cazado esto antes de gastar una ejecución real, y cazará el siguiente caso parecido |
| F-60 | **`INV-03` bloquea al destinatario por usar sus propios recuerdos.** Los imprescindibles se declaran como `HechoCanonico` (`imp-01`, `imp-02`) pero nadie declara que el destinatario —ni Lucía, en el recuerdo del picnic— los conoce **antes** del capítulo 1, así que cuando el Escritor hace que Tomás actúe sobre su propio recuerdo, `INV-03` dice que no consta que lo conozca. Es el caso de `F-32` otra vez: lo que un personaje sabe desde antes del relato va en `conocimiento_inicial`, y `montar` no lo rellena para los imprescindibles | **Cerrado**: `montar` siembra el conocimiento inicial: el destinatario conoce todos sus imprescindibles y cada persona o mascota imprescindible conoce los que la nombran. De paso, `montar` ignoraba el `conocimiento_inicial` que declara el propio plan, y ahora también lo siembra. Lo sostiene una prueba en la que el destinatario actúa sobre `imp-01` sin que salte `INV-03` |
| F-61 | **Los hooks no dejaron constancia en la ejecución real.** El Escritor completó tres sesiones y el registro de `HARNESS_REGISTRO_HOOKS` no se creó. Puede ser que Claude Code no cargue los hooks de `.claude/settings.json` en una sesión `-p` lanzada desde `backend/`, que el comando `python "$CLAUDE_PROJECT_DIR/..."` no funcione en la shell de Windows, o que la variable no llegue al proceso del hook. **La causa está sin diagnosticar**; lo único medido es la ausencia. Es exactamente lo que `VER-76` declaraba como su punto ciego | **Cerrado**: `SesionDelegada` arranca en la raíz del repositorio cuando no se le da una carpeta, así que Claude Code carga `.claude/settings.json` y sus hooks. El Editor y el Juez siguen en su directorio aislado, que ya lleva el hook de policy con ruta absoluta. El diagnóstico (dos sesiones mínimas idénticas: desde `backend/` sin registro, desde la raíz con él) costó 0,0919 + 0,0510 USD, medidos. Que se disparen en una generación real lo comprobará la siguiente ejecución. **La segunda ejecución real lo comprobó solo en parte**: `validar_capitulo.py` dejó 4 filas en el registro de hooks, las 4 con el Planificador o el Revisor y en la rama que sale sin comprobar nada (solo actúa con el Escritor, `validar_capitulo.py:76`); `policy.py` no dejó ninguna. Cargar, se cargan; validar un capítulo o interceptar una herramienta, no lo han hecho nunca, porque el Escritor no llegó a ejecutarse (`F-68`) |
| F-62 | **El Planificador devolvió dos planes fuera de esquema antes del bueno.** Ronda 1: un campo inventado (`notas_de_mundo`). Ronda 2: los accesos de un lugar como texto («verja sur, frente a la parada del 14») en vez de identificadores. El código los rechazó, como debía, y el tope de 3 rondas se quedó a una de agotarse. El prompt no dice que no se admiten campos extra ni que los accesos son `id` de lugares | **Cerrado**: el prompt del Planificador y su definición de agente dicen que un campo de más hace rechazar el plan entero y que los `accesos` son identificadores de lugares declarados (Regla 4). Una prueba lo exige en el prompt. Cuántas rondas ahorra está **sin medir** hasta la siguiente ejecución real |
| F-63 | **El ensamblador del manuscrito recorta los bordes, así que `VER-60` falla en los extremos y su prueba no lo ve.** `features/manuscrito/exportar.py:130` hace `"".join(partes).strip()`: sin títulos se come el blanco inicial de la primera escena, y con o sin títulos el final de la última. `test_el_texto_de_cada_escena_es_byte_a_byte_el_del_borrador` compara con `in`, y sus borradores no tienen blanco en los bordes, así que pasa por coincidencia (Regla 11). Lo encontró la preparación de `PLAN-27`, que no puede heredarlo en el PDF | **Cerrado** por `PLAN-27` E1: `exportar.capitulos_de` devuelve el texto elegido de cada escena sin tocarlo, y `manuscrito` junta con su separador sin recortar. Lo sostienen `test_el_borde_de_la_primera_y_la_ultima_escena_no_se_recorta` y `test_capitulos_de_devuelve_el_texto_byte_a_byte_con_comillas_y_rayas`, que compara con `==`; las dos, rojas antes |
| F-64 | **Dos novelas en la misma base se pisaban: la segunda fallaba, pero tarde, y la primera ya estaba corrupta.** El Planificador pone los mismos identificadores en todas las novelas (`cap-01`, `per-irene`, `lug-casa`) y `montar` numera las escenas `"{capitulo}-e1"`; `capitulo`, `escena`, `entidad` y `lugar` tienen clave global. `capitulo`, `entidad`, `lugar` y `conocimiento` se escriben con `INSERT OR REPLACE`, así que la segunda novela **se quedaba en silencio** los capítulos, personajes, lugares y conocimiento de la primera, y solo al insertar la escena reventaba con `UNIQUE constraint failed: escena.id` (Regla 7: la clave que omite el campo que distingue dos filas no falla, pisa). Y era fácil de provocar: `novela_regalo.py` usa `backend/regalo.db` por defecto. **Corrección de este mismo registro**: la primera versión decía que `hecho_canonico` también se pisaba; no es verdad, tiene clave `(obra, id)` desde `F-39`. Lo encontró la preparación de `PLAN-28` | **Cerrado.** Los identificadores del plan se acotan a su obra en el único punto por el que entran, al leerlo (`planificacion/ids.py`, `acotar_a_la_obra`), y todo lo de abajo usa los mismos. Y `alta_de_obra` falla **antes de escribir** si un capítulo ya es de otra obra (`CapituloDeOtraObra`). Lo sostienen `test_dos_novelas_con_el_mismo_plan_en_la_misma_base_no_se_pisan`, que fue rojo con el `UNIQUE constraint failed` de arriba, y `test_dar_de_alta_capitulos_que_son_de_otra_obra_falla_sin_tocar_nada`. **Lo que queda**: `entidad` y `lugar` siguen sin columna de obra, así que un camino que no pase por el plan —el guion de la obra de diez capítulos, la escaleta por la API— puede todavía pisarlos; y el Escritor ve identificadores más largos, cuyo efecto está sin medir hasta la siguiente ejecución real |
| F-65 | **`INV-24` contaba los usos de otras novelas: un imprescindible que faltaba pasaba si otra novela de la misma base lo había usado.** `novela.cerrar` preguntaba `usos_de_hecho(con, "imp-03")`, y `uso_de_hecho` no guarda la obra; todas las novelas llaman a sus imprescindibles `imp-01`, `imp-02`… Un verde falso en una `bloqueante`, que la puerta de publicación iba a heredar. Es un residuo de `F-64`: lo encontró la preparación de `PLAN-30` E8 al leer lo que la puerta compone | **Cerrado.** Solo cuentan los usos de las escenas de la obra, que desde `F-64` tienen identificador propio. Lo sostiene `test_un_imprescindible_usado_solo_en_otra_novela_sigue_faltando`, que fue rojo con el uso de otra obra contando como propio |
| F-66 | **La «escena anterior» del mismo capítulo es el último borrador, no el aceptado.** `obra.reunir_material` la busca con `ORDER BY b.version DESC LIMIT 1`, así que en la obra de varias escenas por capítulo una escena rendida pasa a la siguiente el último intento y no el menos malo que se eligió, y una reescritura rechazada pasaría la suya. La rama de `anterior_cruza_capitulo`, la que usa la novela regalo (una escena por capítulo), sí usa `_texto_elegido` y no lo sufre. Lo encontró `PLAN-30` E9 al leer qué contexto recibe una reescritura | **Abierto.** Fuera del encargo de `PLAN-30`: no afecta al producto, y el arreglo —pedir el texto elegido también en la primera rama— necesita su prueba en la obra de varias escenas |
| F-67 | **Reanudar la novela regalo volvía a planificar.** `novela.escribir` llamaba siempre al Planificador y al Revisor, aunque la obra ya tuviera un plan aprobado en `plan_de_obra`. Relanzar tras una caída pagaba otra vez la planificación, y el plan nuevo podía no cuadrar con la obra ya montada, que `montar` no reinicia: capítulos de un plan escritos con los imprescindibles de otro. Afecta al checkpoint que pide el enunciado (reanudar desde el último capítulo completado) | **Cerrado**: `planificacion.reanudar_o_planificar` devuelve el plan aprobado de la obra si existe, sin llamar a nadie, y solo planifica si no lo hay; el título y la premisa salen de la ficha (`SPEC-25` v3). Lo sostienen una prueba de servicio y otra que relanza `escribir` y comprueba que el Planificador y el Revisor no reciben llamadas nuevas. `novela_regalo.py` dice en su informe si el plan se reutilizó |
| F-68 | **Los errores de formato del Planificador gastan las rondas de revisión, y el plan no se aprueba.** Segunda ejecución real (`PLAN-26` E13, ficha real): ronda 1 rechazada por el esquema (el POV de un capítulo apunta a un personaje no declarado), ronda 2 rechazada por el esquema (un campo inventado, `personajes_nota`, pese al aviso de `F-62`) y ronda 3 rechazada por el Revisor con tres objeciones de fondo. Con el tope de 3 agotado, `PlanNoAprobado` y ningún capítulo. **El Revisor solo llegó a ver un plan**: dos de las tres rondas se fueron en fallos que el código detecta sin juicio. `F-62` (el aviso en el prompt) no bastó | **Cerrado** con el arreglo que aprobó el autor (2026-09-24): un plan fuera de esquema no cuenta como ronda de revisión y se repite, con el error como objeción, hasta `topes.reintentos_de_transporte` veces en toda la planificación (3), el mismo tope que un fallo de transporte; pasado, `PlanNoAprobado`. Las rondas las gastan solo la cobertura y el Revisor, que es lo que `SPEC-26` `RF-07` llama revisiones. El número de llamadas al Planificador queda acotado por `revisiones_de_plan + reintentos_de_transporte`, que es lo que la liveness de TLA+ tiene que modelar. Lo sostienen `test_un_plan_fuera_de_esquema_no_gasta_ronda_de_revision` y `test_los_planes_fuera_de_esquema_tienen_su_propio_tope`, rojas antes. Cuántas rondas ahorra en real está **sin medir** hasta la siguiente ejecución |
| F-69 | **`novela_regalo.py` muere sin informe cuando el plan no se aprueba, y el coste de esa ejecución se pierde.** `PlanNoAprobado` sube hasta el guion como excepción: no se imprime el coste, ni las rondas, ni los hooks. El coste del Planificador y del Revisor lo sumaba `Contador` en memoria, y no queda en ninguna tabla, así que el de la segunda ejecución real está **sin medir**. Es un dato ausente que no se ve: el guion termina con una traza, no con «sin medir» | **Cerrado.** `novela_regalo.py` captura `PlanNoAprobado`, imprime las rondas con su origen, el coste del Planificador y del Revisor —marcado como suelo si alguna delegación no lo dio—, el tiempo, los hooks y el estado de Langfuse, y sale con 1. Lo sostiene `test_sin_plan_aprobado_el_guion_informa_y_sale_con_1`, que lanza `main` con agentes que no llaman a nadie y fue rojo con la traza. **Lo que no arregla**: el coste de la segunda ejecución real se perdió y sigue **sin medir**; y ese coste sigue viviendo en memoria, así que un proceso que muera de otra forma lo pierde igual |
| F-70 | **La API no conserva nada entre peticiones: `app.state.ruta_db` solo se asigna en las pruebas.** `entrevista/router.py:41`, `brief/router.py:24` y `orquestacion/router.py:41` abren `getattr(request.app.state, "ruta_db", ":memory:")`, y fuera de las pruebas nadie asigna `ruta_db`, así que cada petición abre una base en memoria nueva. Reproducido con `TestClient(app)` y el arranque real: `POST /entrevistas` da 201 y el `GET /entrevistas/{id}` siguiente da 404. El camino que documenta `CLAUDE.md` —`uvicorn app.main:app` más `entrevista_cli.py`— no puede completar un turno. Las pruebas pasan porque fijan `ruta_db` a un fichero: el doble no tiene la forma de lo real. Lo encontró la auditoría contra `EXAMEN.md` del 2026-09-24 | **Cerrado.** El arranque fija la base antes de atender (`main.fijar_base`): `HARNESS_BASE` o, sin ella, `ruta_de_la_base` de `config/sistema.json`, y la prepara con sus migraciones. Si ya viene fijada —las pruebas—, se respeta. Lo sostiene `entrevista/tests/test_arranque.py`, que arranca la aplicación como `uvicorn`, sin fijar nada, y fue rojo con el 404 de arriba |
| F-71 | **`INV-22` toma palabras comunes por erratas de un nombre.** `nombres_mal_escritos` (`commons/politica/personalizacion.py`) marca toda palabra con mayúscula a distancia 1 de un nombre corto: con «Nala», el ejemplo del enunciado, «Nada» es una errata. Recalculado con el código de hoy sobre los tres borradores reales de `regalo-prueba.db`: **los tres dan positivo**, por «Nada» y «Nadie» a principio de frase. Con un personaje de nombre así, un capítulo que empiece una frase con esas palabras no pasa. Es, con toda probabilidad, lo que gastó la reescritura 2 de la primera ejecución real (`F-73`). Lo encontró la auditoría del 2026-09-24 | **Cerrado en parte.** Aplicado el arreglo que decidió el autor (2026-09-24): una palabra con mayúscula no es errata si el propio texto la usa en minúscula. Su punto ciego, dicho: una errata real que coincida con una palabra común usada en minúscula en el mismo capítulo pasa. Lo sostienen dos pruebas de `test_personalizacion.py` —«Nada» con «nada» en el texto no es errata; «Nela» junto a ellas sí—, rojas antes del arreglo. **Medido sobre los tres borradores reales**: los borradores 1 y 2 quedan limpios, y **el 3 sigue dando positivo por «Nadie»**, que solo aparece a principio de frase. **Cerrado** con la opción (a) del autor: una lista cerrada y a la vista de palabras comunes que nunca son errata (`personalizacion.PALABRAS_COMUNES`); la (b), ignorar el principio de frase, se descartó porque calla erratas reales. Su punto ciego es la propia lista: una errata que coincida con una de esas palabras pasa. Medido de nuevo: **los tres borradores reales quedan sin marcas**. Lo sostiene una prueba con «Nadie» junto a «Nadia», a distancia 1 como en el borrador real; la primera versión usaba «Nala», a distancia 3, y pasaba sin haber fallado (Regla 11) |
| F-72 | **El reintento por fallo de transporte está declarado y no existe.** `reintentos_de_transporte` (`commons/configuracion/esquemas.py:357`, 3 en `backend/config/sistema.json`) no lo lee ningún código, y `se_reintenta` y `quedan_intentos` (`commons/modelo/cliente.py:19`, `:25`) no tienen llamadas fuera de sus pruebas. En `orquestacion/obra.py` una delegación con cualquier `fallo` corta el bucle. El enunciado §3 pide *«Retries con límite»*, y el tope está en la configuración sin que nadie lo aplique. Lo encontró la auditoría del 2026-09-24 | **Cerrado**, con el tope que ya estaba en la configuración. El Escritor reintenta **el ciclo entero**, con el contexto montado de nuevo, como dice `commons/modelo/cliente.py`, y sin gastar intentos de calidad ni reescrituras; agotado, la escena para con motivo `transporte`. Los agentes que no montan contexto —Planificador, Revisor, Editor, Resumidor— reintentan su llamada con `cliente.ConReintentos`, porque su entrada es texto ya fijado y repetir la llamada es repetir el trabajo; agotado, el fallo sube y `novela_regalo.py` lo informa y sale con 1, y relanzar reanuda. Antes hacía falta `F-74`: el fallo real no lo capturaba nadie. Lo sostienen dos pruebas del ciclo (`test_personalizacion_en_obra.py`), dos de los demás agentes y una del guion (`test_novela.py`), rojas antes |
| F-73 | **La rama de `INV-22` no deja rastro en el audit log: ni la reescritura que pide ni la parada al agotar el tope.** En `orquestacion/obra.py`, `nombre_mal_escrito` suma al mismo contador que `INV-21`, no llama a `registrar_decision` y, al agotar el tope, hace `break` sin registrar la parada. En la primera ejecución real se ve el hueco: `decision_de_politica` tiene 29 coincidencias vetadas en la reescritura 0, ninguna en la 1, 17 en la 2 y una sola `reescritura_pedida`; el paso de la 1 a la 2 no lo registró nadie. §7 pide un audit log de las decisiones del policy engine | **Cerrado.** Dos valores nuevos de `tipo_de_decision_de_politica` en `docs/definitions.md`, como cambio documental que nombra algo que ya ocurría (decisión del autor, 2026-09-24): `nombre_mal_escrito` y `parada_por_nombre`. La rama de `INV-22` deja ahora el mismo rastro que la de `INV-21` —coincidencia, reescritura pedida o parada—, y el detalle dice **cuántos** nombres, nunca cuáles, porque el audit log sobrevive a la entrega. Sin migración: la columna no tiene `CHECK`. Lo sostienen dos pruebas de `test_personalizacion_en_obra.py`, rojas antes |
| F-74 | **Había dos clases `FalloDeTransporte` y el bucle capturaba la del doble, así que un fallo de transporte real no lo capturaba nadie.** `commons/modelo/doble.py` y `commons/modelo/proveedor.py` definían cada uno la suya, sin herencia entre ellas, y `orquestacion/bucle.py` importaba la del doble. Un `claude -p` que no responde o muere lanza la del proveedor: subía como traza por `generar_obra` y tumbaba la novela sin informe. Las pruebas no lo veían porque el doble lanzaba la suya: el doble no tenía la forma de lo real. Además, ningún otro agente —Planificador, Revisor, Editor, Resumidor, juicio de obra— captura ese fallo (`F-72`). Lo encontró el arreglo de `F-72` | **Cerrado.** Una sola clase, en `commons/modelo/cliente.py`, que es el módulo que decide qué se reintenta; `doble.py` y `proveedor.py` la importan. Lo sostiene `test_el_fallo_de_transporte_de_la_delegacion_real_se_captura`, que lanza la del proveedor y fue rojo con la traza |
| F-75 | **Con Langfuse caído, el SDK real no lanza: escribe el error en su log y `flush()` vuelve normal, así que el informe decía «enviado» con cero pérdidas.** Visto en `PLAN-29` E13(a) con un host que no responde (`http://127.0.0.1:9`): `vaciar` devolvió `True`, `perdidas` 0 y ninguna fila en `envio_perdido`, y el SDK solo dejó *«Unexpected error occurred»* en el logger `langfuse`. Los envíos de spans fallan igual por el logger `opentelemetry`. Las pruebas de E10 no lo veían porque el SDK doble lanzaba una excepción: el doble no tenía la forma de lo real. Es `SPEC-29` `RF-09` roto en silencio | **Cerrado.** `ExportadorLangfuse` cuenta los registros de nivel ERROR de los loggers `langfuse` y `opentelemetry`, sin guardar el mensaje, y un vaciado que deja errores nuevos es una pérdida, con clase `ErrorDelSDK` en `envio_perdido`. **Medido contra la red de verdad**: el host caído da ahora `False`, una pérdida y la fila, en 9 s; el host real, en un proceso limpio, `True` y ningún error en el log. **Punto ciego, dicho**: el contador escucha loggers de todo el proceso, así que con dos exportadores en un mismo proceso y uno caído, el sano también informa de pérdida. Sobra en vez de faltar. Lo sostienen tres pruebas de `test_langfuse.py` con un SDK doble que se porta como el real, rojas antes |
| F-76 | **El Editor no podía aprobar ni suspender nada con datos reales: el transporte añade `medidas` a toda respuesta y el esquema de su valoración prohíbe campos de más.** En la primera ejecución real aceptada (`obra-ejemplo`, capítulo 1) el Editor contestó y su delegación terminó `ok`, pero `ValoracionesDelEditor` rechazó la respuesta por `medidas` —*«Extra inputs are not permitted»*—, `INV-26` quedó `sin_veredicto` y, como es `mayor`, el capítulo se aceptó **sin que el validador semántico juzgara nada**. Con dobles no se veía porque el doble no añade `medidas`. Lo encontró la ejecución real `R0` | **Cerrado.** `ciclo._editar` valida la respuesta sin `medidas`, que es del transporte y no del Editor. Lo sostiene `test_el_editor_se_lee_aunque_el_transporte_anada_sus_medidas`, con un doble que añade `medidas` como el real, roja antes con el mismo `SIN_VEREDICTO`. **Comprobado contra el modelo real**, repitiendo solo la delegación del Editor sobre el mismo capítulo: seis notas legibles con su justificación —continuidad 4, tono 5, arco 4, coherencia 4, ritmo 3, personalización 4—, por 0,1384975 USD medidos |
| F-77 | **El Resumidor no resume: su definición le pide los identificadores «de la lista que te den» y el prompt no le da ninguna** (Regla 4). En `R0` falló por contrato y la escena quedó sin resumen, así que el capítulo 2 habría arrancado sin memoria del 1 y el juicio de obra (`INV-27`), sin los resúmenes que lee. Repetida esa delegación con el mismo texto —0,05233 USD, medidos—, el modelo contestó en prosa pidiendo *«la lista de hechos clave con sus identificadores»* | **Cerrado.** `ciclo.prompt_de_resumen` da la lista de hechos de la obra, con su enunciado, y dice qué hacer si no hay ninguno; llega desde `ciclo.ejecutar` y desde la rendición de `obra.py`. Lo sostienen dos pruebas de `test_ciclo.py`, rojas antes. **Comprobado contra el modelo real**, repitiendo solo esa delegación con el mismo texto y el prompt nuevo: respuesta legible, un resumen de 52 palabras y 3 `hechos_clave`, los tres de la lista. Costó 0,05208 USD, medidos |
| F-78 | **El informe de `novela_regalo.py` decía «sin medir» las palabras de un capítulo que tenía texto.** Una escena que sale limpia se consolida sin fijar `borrador_aceptado`, y el informe solo buscaba ese borrador. El capítulo 1 de `R0` tiene 1.102 palabras, dentro del rango `corta`. El manuscrito ya usaba la regla buena: el aceptado o, si no consta, el último | **Cerrado.** `novela_regalo.palabras_de` cuenta el texto con la regla del manuscrito (`exportar._texto_de`). Lo sostiene `test_el_informe_cuenta_las_palabras_del_texto_elegido_aunque_no_se_marcara`, roja antes |
| F-79 | **`INV-02` hacía imposible un reencuentro: comprobaba que cada presente estuviera vivo antes de aplicar el delta de la propia escena.** La novela de ejemplo (`R1`) se paró en el capítulo 9: el gato se perdió en el 8 —el delta lo dejó `desaparecido`— y el plan lo hacía reaparecer en el 9, que es justo la escena que cambia su estado. Es la familia de `SPEC-16`: allí `INV-03` no dejaba aprender en la escena en que se aprende; aquí `INV-02` no dejaba volver en la escena en que se vuelve. **Una invariante no puede bloquear el acto que cambia lo que ella vigila** | **Cerrado** con la decisión del autor (2026-09-24): el estado vital que cuenta es el que deja la propia escena, con los `cambios_de_estado_vital` de su delta aplicados. Punto ciego, de la misma familia que `PC-5` y `MF-18`: **las puertas leen el delta, no la prosa**, así que una vuelta que el delta declara y el texto no cuenta pasa. Lo sostiene `test_inv02_un_desaparecido_que_la_escena_devuelve_a_vivo_puede_estar_presente`, roja antes, con su control, que un desaparecido que la escena no devuelve sigue bloqueando. **Incompleto**: la formulación solo miraba la reaparición, y bloqueaba su espejo, la desaparición dentro de la escena; lo cerró `F-148` |
| F-90 | **`escena.capitulo` no es clave entre obras, y `escenas_de_capitulo` sin obra mezcla escenas de dos obras con un capítulo del mismo identificador.** Lo destapó `PLAN-23` A6: `orquestacion/tests/test_contexto_llega_como_texto.py` monta dos obras en la misma base con escenas en `cap-1`, y al leer las escenas de la versión por capítulo la segunda obra recibía las de la primera. `CapituloDeOtraObra` impide dos filas de `capitulo` con el mismo `id`, pero **nada impide que una escena apunte a un capítulo de otra obra**. `PLAN-23` A6 pasa la obra en los lectores de versión (`escenas_de_capitulo(con, capitulo, obra)`); siguen sin pasarla `POST /capitulos/{id}/cerrar` y `generar_obra`/`evaluar_cierre` con `capitulo` | Abierto para los lectores por capítulo que no son de versión |
| F-91 | **Tras entregar la novela no consta quién es el destinatario, y la regeneración lo necesita dos veces.** `SPEC-25` `RF-21` borra la ficha al entregar. `PLAN-23` `C-4` no admite renombrar al destinatario, y sin ficha no hay con qué comprobarlo: A7 **niega el renombrado** con ese motivo en vez de admitirlo a ciegas. Y la Parte B lo va a encontrar otra vez: `novela.inmutable(ficha, premisa)` —el bloque 1 del Escritor— se monta con la ficha (género, tono, nombre y edad del destinatario). **Regenerar una novela entregada no tiene hoy con qué montar el contexto del Escritor.** Decidir qué se conserva al entregar es de una spec (`SPEC-25`). **Primera de dos veces** que una salvaguarda de privacidad bloquea una función del producto; la segunda es `F-146`, y las dos van juntas a la presentación como limitación declarada (`docs/proceso/trade-offs.md` `T-11`). La terminal lo sortea con `--ficha` (`F-147`); la web, no | Abierto |
| F-92 | **Cerrar un capítulo no se guarda: `capitulo.estado` no lo escribe nadie.** `POST /capitulos/{id}/cerrar` devuelve `cerrado` y ninguna línea del backend hace `UPDATE capitulo` (buscado con `grep -rn "UPDATE capitulo"` sobre `backend/`). `PLAN-23` `C-5` y `RF-30` hablan de capítulos compartidos **cerrados**, y `GET /obras/{id}/versiones/{numero}` enseña el estado de la tabla, que es siempre `abierto`. Además, `brief.alta_de_obra` escribe con `INSERT OR REPLACE ... 'abierto'`: el día que el cierre se guarde, relanzar el alta **reabriría** un capítulo cerrado | Abierto |
| F-93 | **La cronología y el generador de Lean leen por obra, no por versión.** `evento_cronologico` tiene `obra` y no versión; `cronologia/consultas.py` (`orden_temporal`, ubicuidad), `story_bible.leer_cronologia` y `specs/lean/generar_lean.py` leen todos los eventos de la obra. Con dos versiones, el evento del capítulo sustituido y el del nuevo entran juntos. Es el punto ciego declarado de `VER-117` (solo los lectores enumerados en el hallazgo 5 de `PLAN-23`) | Abierto: la puerta de publicación por versión está fuera de `PLAN-23` |

| F-100 | **La segunda novela de una base recibe el mundo de la primera: personajes, lugares y conocimiento.** `consolidacion/mundo.leer(con)` no filtra por obra y `orquestacion/obra.reunir_material` (`obra.py:160`) se lo pasa entero al Escritor: en el prompt de la B aparecen `personajes: obra-a-per-canela, obra-a-per-rodrigo, …`, `vivos`, `donde` y `obra-a-per-rodrigo sabe imp-01`. `F-64` acoto los identificadores con la obra y con eso dejaron de **pisarse**, pero no de **verse**: `entidad`, `lugar` y `conocimiento` no tienen columna de obra, y el identificador lleva el nombre que puso el Planificador, asi que lo que cruza es el nombre del destinatario de otra persona. Lo reproduce `test_la_segunda_novela_no_recibe_nada_de_la_primera` (`PLAN-31` E7, dos fichas disjuntas de `harness/adversarial/` con dobles que guardan cada prompt), que queda como `xfail` estricto. Las tres tools de la story bible **no** fugan: estan acotadas (`SPEC-28` `RF-06`) y su prueba caza la fuga si se quita el filtro a proposito | **Cerrado con la salida (a) del autor** (2026-09-24): en la novela regalo, `mundo.leer(con, obra)` solo lee las entidades, los lugares y el conocimiento cuyo identificador lleva el prefijo de la obra, que es como los deja `acotar_a_la_obra` desde `F-64`; lo activan `novela._escribir` y la reescritura de la puerta. La prueba de las dos novelas era un `xfail` estricto y dio `XPASS`: ahora es una prueba normal. **Anotada la salida (b)**, una columna `obra` en `entidad`, `lugar` y `conocimiento` con su migración: el prefijo solo vale donde los identificadores están acotados, y la obra de terror y otros caminos no lo están. Hasta (b), `evaluar.py` sigue exigiendo una base por ejecución |
| F-101 | **La traza del Resumidor de una reescritura a delta fijo no se guarda nunca.** `orquestacion/obra.reescribir_capitulo` llama a `ciclo.guardar_trazas(con, c)` **antes** de delegar en el Resumidor, y la traza de esa delegación se añade después a `c.trazas` sin que nadie la escriba en `traza_de_delegacion`. Su coste sí entra ahora en el total (`PLAN-31` E5, `coste_del_cierre`), pero quien reconstruya el gasto desde `traza_de_delegacion` ve una delegación de menos por cada capítulo que la puerta reescribió: sesga hacia abajo. Encontrado al sumar el coste de las reescrituras | **Abierto.** Arreglo pequeño —guardar las trazas al volver—, pero fuera del encargo de `PLAN-31` |
| F-80 | **La skill de FSD manda instalar un paquete que no existe.** `.agents/skills/feature-sliced-design/SKILL.md` § 5-4 y `references/migration-guide.md` dicen `npm install -D @feature-sliced/steiger`; el registro contesta `E404 Not Found` (medido con `npm view` el 2026-09-24, `PLAN-22` E6). El linter se instala como `steiger` más `@feature-sliced/steiger-plugin` y se configura en `steiger.config.ts`, que es como lo hace `frontend/`. No se corrige la skill: es vendorizada de upstream y el arreglo va allí | Abierto: se registra aquí y el frontend usa los nombres que el registro sí tiene |
| F-81 | **En las fichas, el aviso «(sin nombre guardado)» iba dentro del título y lo partía en dos líneas.** Visto en la captura de la primera inspección real con Playwright MCP (`PLAN-22` E13), no por el agente que la hizo | Cerrado en `PLAN-22` E13: el aviso sale fuera del `h3`, con su prueba roja en `Fichas.test.tsx` |
| F-82 | **En la página de un capítulo, la etiqueta de estado quedaba pegada a la primera tarjeta.** Visto en la captura de E13 | Cerrado en E13 con un margen en `estilos.css`. Sin prueba roja: jsdom no calcula el layout; lo comprobó una segunda captura |
| F-83 | **Cada página daba un `404` en `/favicon.ico` en la consola.** Estaba en los registros de consola que deja el MCP en `.playwright-mcp/`, y el agente no lo contó | Cerrado en E13: icono declarado en `index.html`, con prueba roja en `tests/frontera.test.ts` |
| F-84 | **El agente inspector dio por buena la web con `F-81`..`F-83` dentro.** Juzgó «en la inspección del árbol de accesibilidad»: un solapamiento o un salto de línea feo no están en ese árbol, y un error de consola solo se ve si se piden los mensajes. **Un agente con navegador puede ver bien lo que está mal**, que es el punto ciego que `VER-119` ya declaraba, ahora con un caso | Abierto: es el punto ciego de `VER-109` y de `INV-30`. El prompt del inspector de E13b pide mirar las capturas y la consola, y eso no garantiza que lo haga |
| F-110 | **Relanzar tras una caída reinicia todos los contadores de reintento: los topes valen por ejecución, no por capítulo ni por plan.** `obra._intentar` guarda `numero`, `reescrituras` y `reintentos_de_transporte` en variables locales (`orquestacion/obra.py:445-450`), y `planificar` hace lo mismo con `rondas`, `fallos_de_formato` y `version` (`planificacion/service.py:130`). Al relanzar, `generar_obra` no salta un capítulo que no esté en `YA_HECHAS` (`obra.py:223`) y `_intentar` empieza de cero; sin plan aprobado, `planificar` también. El dato para no hacerlo existe: `repo.intentos_de` cuenta los borradores de la escena —su docstring: *«No hay contador aparte: es cuantos borradores hay»* (`escaleta/repository.py:316-319`)— y solo lo usa `reescribir_capitulo` (`obra.py:710`). La puerta de publicación ya lo hace bien: lee su tope de `veredicto_de_publicacion` (`orquestacion/publicacion.py:183`) y lo dice, *«relanzar no lo reinicia: la leccion de `CE-4`»*. Lo encontró TLC (`specs/tla/`, `CE-7`, invariante `ReintentosAcotados`): con `revisiones_de_plan = 2`, rechazo, caída, relanzar, rechazo, rechazo — tres rondas de revisión. **Y arreglar solo el contador no basta** (`CE-6`, del propio modelo): con el contador en la base, un capítulo que ya agotó sus intentos al caer se volvía a escribir en vez de rendirse. El checkpoint tiene que rehacer la decisión, no solo recordar el número. Cuántas veces ha pasado en una ejecución real: **sin medir** | **Abierto.** No se toca `backend/` desde la sesión de TLA+. Lo que la corrección tiene que cumplir está en `specs/tla/HarnessBackend.cfg` (`ReanudarReiniciaContadores = FALSE`, y la rendición al reanudar de `Montar`) |
| F-111 | **Relanzar la planificación pisa las filas de `plan_de_obra` de la ejecución anterior** (Regla 7). `planificar` vuelve a numerar desde `version = 0` (`planificacion/service.py:130-132`) y `repository.guardar` escribe con `INSERT OR REPLACE` sobre la clave `(obra, version)` (`planificacion/repository.py:24`, `:36`). El camino es el que el propio informe recomienda: el Planificador o el Revisor agotan sus reintentos de transporte (`ConReintentos`, `novela.py:302-304`), `FalloDeTransporte` sube hasta `novela_regalo.main`, que imprime *«relanzar reanuda»* (`novela_regalo.py:193-208`); al relanzar no hay plan aprobado y la ronda 1 nueva **sustituye** a la ronda 1 vieja. Lo que se pierde son las objeciones de las rondas anteriores, que es lo primero que `F-69` y `SPEC-26` `RF-07` quieren poder leer cuando un plan no se aprueba. No falla nada. TLC: `CE-8`, invariante `RondasDePlanConservadas` | **Cerrado.** `planificar` numera desde la última versión guardada de la obra, así que relanzar añade rondas en vez de pisarlas. Lo sostiene `test_relanzar_la_planificacion_no_pisa_las_versiones_anteriores`, roja antes con `[1, 2, 3]` donde tenían que quedar cuatro. **Lo que no arregla**: el tope de rondas sigue siendo por ejecución (`F-110`) |
| F-112 | **Consolidar son dos transacciones, y una caída entre ellas deja el capítulo en un estado que la reanudación no sabe leer.** Dos ventanas, en órdenes contrarios. **(a)** Camino limpio: `aplicar.consolidar` confirma el delta y la fila de `escena_consolidada` (`consolidacion/aplicar.py:137-164`) y después `repo.marcar_consolidada` abre otro `with con:` (`escaleta/repository.py:290-292`), las dos llamadas seguidas en `ciclo.py:344-346`. Si el proceso muere entre ellas, la escena sigue `generada` con su delta en el canon. Al relanzar, `generar_obra` no la salta, paga un ciclo entero, y al consolidar `YaConsolidada` (`aplicar.py:131-135`) la convierte en parada (`ciclo.py:348-350`). **Cada relanzamiento termina igual: la novela no puede pasar de ese capítulo.** TLC: `CE-9`, `NoReescribeCerrados`. **(b)** Rendición: `repo.rendir_escena` confirma `aceptada_por_rendicion` (`obra.py:275`, `escaleta/repository.py:311-313`) antes de `consolidar_y_resumir` (`obra.py:277`). Si muere entre las dos, el capítulo ya está en `YA_HECHAS`, se salta, y su delta no se aplica nunca: los capítulos siguientes se escriben contra un mundo al que le falta, que es `INV-05` roto. La puerta lo pararía después por `INV-29`, pero lo escrito entre medias ya está pagado. TLC: `CE-10`, `SoloSobreConsolidadas`. En las dos ventanas **no hay ninguna llamada a un modelo**, así que solo cabe una muerte del proceso; con qué frecuencia: **sin medir** | **Abierto.** Lo que la corrección tiene que cumplir: `DosTransaccionesAlConsolidar = FALSE` en `specs/tla/HarnessBackend.cfg`, el estado de la escena en la misma transacción que su delta |
| F-113 | **La puerta de publicación publica un capítulo que el Editor no llegó a juzgar.** Si el Editor no devuelve una valoración legible, `ciclo._editar` guarda un `INV-26` `sin_veredicto` y **no** lo añade a los hallazgos del intento (`ciclo.py:174-179`), así que la escena se consolida como limpia (`ciclo.py:328-331`). En la puerta, `decidir` (`auditoria/publicacion.py:92-104`) cuenta `sin_veredicto` como abierto (`:37`) pero solo bloquea las `bloqueante`, `INV-29`, `INV-28` e `INV-27`; `INV-26` es `mayor` (`commons/invariantes/registro.py:148`), así que no cierra nada. La reescritura de la puerta tiene el mismo hueco: `reescribir_capitulo` solo mira `c.generacion.hallazgos` (`obra.py:732`). El enunciado pide que nunca se publique un capítulo que no haya pasado todos los validadores, y el Editor es el único validador semántico del capítulo. **No es hipotético: es lo que `F-76` vio en `R0`** —*«el capítulo se aceptó sin que el validador semántico juzgara nada»*—; `F-76` arregló la causa de aquella respuesta ilegible, no el camino por el que pasa cualquier otra. TLC: `CE-11`, `NuncaPublicaSinValidar` | **Cerrado** con la decisión del autor (2026-09-24): un `INV-26` en `sin_veredicto` impide publicar (`SPEC-30` v5, `RF-01` condición 5), reintentable como `INV-27`, que es la corrección que ya modelaba TLC. Lo sostiene `test_un_capitulo_que_el_editor_no_juzgo_no_publica`, roja antes, con su control: un `INV-26` abierto —un juicio que se hizo y dio nota baja— sigue sin bloquear |
| F-114 | **`delegaciones_por_obra` no acota la obra: en la novela regalo no puede saltar nunca.** `generar_obra` crea su `Generacion` (`obra.py:204`) y compara `g.delegaciones` con el tope antes de cada escena (`obra.py:227`). `novela._escribir` llama a `generar_obra` una vez por capítulo (`novela.py:352-366`) y cada capítulo tiene una sola escena (`novela.montar`, `novela.py:59-70`), así que lo que se compara es **siempre cero**. `total.delegaciones` se suma (`novela.py:370`) y no se compara con nada. El freno que `rendicion.queda_presupuesto` describe como *«el freno de mano de la obra entera»* (`rendicion.py:87-94`) está enchufado donde no puede tirar. TLC: `CE-12`, `DelegacionesAcotadas` | **Cerrado**, con la corrección que modeló TLC (`TopePorCapitulo = FALSE`): `generar_obra` recibe las delegaciones que la novela ya gastó (`delegaciones_previas`) y compara el total de la obra, no el del capítulo. Lo sostiene `test_el_tope_de_delegaciones_acota_la_novela_entera_y_no_cada_capitulo`, roja antes con 35 delegaciones contra un tope de 5 y ninguna parada |
| F-115 | **Una generación detenida sale con código 0, igual que una que terminó bien sin puerta.** `codigo_de_salida` devuelve 0 cuando no hay `publicacion` (`novela_regalo.py:149-153`), y `_escribir` solo evalúa la puerta si no hubo parada (`novela.py:378`). Una novela entera parada por una `bloqueante`, una vetada, el tope, el transporte del Escritor o un contrato roto sale con 0. La prueba `test_el_codigo_de_salida_es_1_si_la_puerta_no_abre` fija ese 0 con el comentario *«sin puerta (--capitulos), no es un fallo»* (`orquestacion/tests/test_novela.py:462`): junta los dos casos. **No lo encontró TLC**: salió al escribir qué implementa `Terminado`. El modelo distingue `detenido` de `publicada`; el proceso, hacia fuera, no | **Cerrado** con la decisión del autor (2026-09-24): una generación parada sale con 1, llegue o no a la puerta; sin puerta y sin parada (`--capitulos`) sigue saliendo con 0. La prueba que fijaba el 0 (`test_el_codigo_de_salida_es_1_si_la_puerta_no_abre`) **se reescribió, no se borró**, y fue roja antes |
| F-116 | **Una caída después de consolidar y antes de resumir deja el capítulo sin memoria para siempre, y el informe no lo dice.** El Resumidor se llama después de consolidar (`ciclo.py:352`), y el resumen, los imprescindibles, los hechos establecidos y las fichas se escriben después de que el ciclo vuelva (`obra.py:293-310`). El Resumidor va envuelto en `ConReintentos` (`novela.py:302-304`) y `_delegar` solo captura `RespuestaIlegible` (`ciclo.py:193-195`), así que un fallo de transporte agotado sube hasta el guion. Al relanzar, el capítulo está en `YA_HECHAS` y se salta (`obra.py:223-225`): no se resume nunca, `_registrar_imprescindibles` no corre y **no entra en `sin_resumen`**, que solo se llena en `obra.py:306`. Lo que falta después: el resumen en la memoria de los capítulos siguientes y en el juicio de obra (`INV-27`, `novela.py:140`). Si además falla `INV-24` depende del texto —el acta también registra `menciona` por el enunciado (`cronologia/extraccion.py:115-117`)—: **sin medir**. TLC: `CE-13`, `MemoriaCompleta` | **Abierto.** Corrección modelada: al reanudar, un capítulo hecho sin su memoria es trabajo pendiente (`ReanudarSaltaLaMemoria = FALSE`) |
| F-117 | **El libro de gasto perdía el coste del capítulo que se paraba a medias.** `evaluar.py` anotaba cada capítulo en `seguir`, al terminarlo, y un capítulo parado no llega a terminar. En `R1` el libro sumó 12,9389 USD en 28 delegaciones, y Langfuse, sobre la misma sesión, 29 generaciones y **14,0463 USD**: faltaba el intento del Escritor en el capítulo 9, 1,1074 USD. El informe imprimía el total del libro como el coste de la ejecución | **Cerrado.** Al terminar con una parada, `evaluar.py` anota lo que la generación gastó y `seguir` no vio, como tramo de su capítulo; al reanudar, ese capítulo tendrá dos filas, el intento parado y el que termina. Lo sostiene `test_el_coste_de_un_capitulo_parado_entra_en_el_libro`, roja antes con 6 delegaciones en el libro contra 7 en la base |
| F-118 | **Un `bloqueante` de un intento descartado seguía abierto aunque la escena se reescribiera y pasara, y la puerta de publicación no publicaba nunca.** Los hallazgos van por escena y no por borrador, y nada cerraba los de un intento anterior cuando la versión aceptada pasaba la misma puerta. Visto con dobles antes de reanudar `R1`: parada por `INV-04` en el capítulo 3, reanudación, capítulo 3 reescrito sin fallos… y veredicto de la puerta `publica = 0` por ese `INV-04`. Habría pasado igual con el `INV-02` del capítulo 9 de la novela de ejemplo | **Cerrado.** Al consolidar, `ciclo._conciliar_la_puerta` cierra como resueltos, con motivo, los hallazgos de las invariantes de la puerta de escena que la versión aceptada ya no produce; es `publicacion._conciliar`, un nivel abajo. Los del Editor o de obra no se tocan. Lo sostienen dos pruebas de `test_ciclo.py` —la del cierre, roja antes, y su control— y `test_reanudar_sigue_la_misma_ejecucion_en_su_base_y_publica` |
| F-119 | **Al reanudar, el libro de gasto pisó con ceros los capítulos ya hechos: 11,9010 USD medidos desaparecieron sin que fallara nada.** `seguir` se llamaba también para los capítulos saltados, con coste 0, y `anotar` sustituía el tramo (`INSERT OR REPLACE`). En la reanudación real de `R1`, a las 19:28:48, los capítulos 1 a 8 pasaron a `usd = NULL` y 0 delegaciones, y el libro dejó de decir cuánto cuesta una novela: habría dado unos 5 USD, **el sesgo hacia lo barato** que convierte un número malo en un argumento. Y la sustitución también desmentía `F-117`: el intento parado y el que termina no eran dos filas, la segunda pisaba a la primera. La prueba de `--reanudar` no lo vio porque no miraba el coste de cada capítulo (Regla 11) | **Cerrado.** Lo que no gastó nada no se anota, y anotar otra vez un tramo **suma**: cada anotación es gasto nuevo de otra ejecución. Una prueba vieja fijaba lo contrario («anotar dos veces no suma») y **se reescribió, no se borró**. Lo sostiene `test_reanudar_no_pisa_lo_anotado_y_suma_el_intento_parado`, roja antes con `(None, 0)` donde había `(0.03, 3)`. **El libro de `R1` se reparó** con `anotar`, no con un `UPDATE`: los ocho tramos con los valores leídos de él antes de la reanudación, y el intento parado del capítulo 9 con el coste de su generación en Langfuse. Queda en 16,8905 USD y 36 delegaciones, lo mismo que Langfuse para la sesión |
| F-120 | **Al reanudar, la traza del intento parado se pisa.** `traza_de_delegacion` tiene clave `(escena, agente, prompt_hash)` y se escribe con `INSERT OR REPLACE` (`observabilidad/repository.py`); en la reanudación de `R1`, el capítulo 9 conserva solo las tres trazas del intento que terminó, y la del Escritor del intento parado —la que dejó el `INV-02`— ya no está. Con dobles, el capítulo 3 tiene 4 delegaciones en el libro y 3 trazas. El coste no se pierde —el libro y Langfuse lo tienen—, pero sí la traza del intento que hay que diagnosticar. Es la Regla 7 otra vez | **Abierto.** El arreglo toca la clave de la tabla y su migración. Hasta entonces, `test_reanudar_no_pisa_lo_anotado_y_suma_el_intento_parado` comprueba el libro y no las trazas, y lo dice |
| F-121 | **La versión vigente era la última creada, y la cascada crea la versión antes de escribirla: el lector veía una versión que nadie había escrito ni validado.** `brief.version_vigente` devolvía `MAX(numero)`, y `manuscrito/exportar.py` repetía la misma regla por SQL. En el paso siguiente a pedir el cambio, `brief.leer`, el manuscrito, el PDF y todo lo que lee «la vigente» desde A6 veían la versión `n+1` con capítulos sin escribir, y si la regeneración paraba a mitad se quedaba así. Lo encontró TLC (`CE-14`, `LectorVeLoPublicado`, `specs/tla/README.md` `D-5`) sobre el diseño de `PLAN-23`, antes de que existiera la rama `S-1` | **Cerrado** (`PLAN-23` B-S1.1): la vigente es **la última publicada** —una ronda de la puerta con `publica` en esa versión (`F-122`)— y, si no hay ninguna, la 1. La regla vive una sola vez en `commons/obra/vigente.py` y la llaman `brief/` y `manuscrito/` (el PDF exporta el veredicto de la versión vigente, no la última ronda de cualquiera). Quien escribe una versión nueva **la dice**: el bucle, la memoria, la puerta y la story bible reciben la versión, porque «sin decirla» ya no es la que se escribe. `test_la_version_vigente_es_la_ultima_publicada_y_no_la_ultima_creada`, `test_sin_ninguna_version_publicada_la_vigente_es_la_1` |
| F-122 | **Las rondas de la puerta de publicación eran de la obra, así que la versión 2 heredaba las que la 1 gastó para publicarse.** `veredicto_de_publicacion` tenía clave `(obra, ronda)` y `rondas(con, obra)` contaba todas: una versión 2 con tope 1 se detenía por tope tras una sola ronda propia. Lo encontró TLC (`CE-15`, `CadaVersionTieneSuTope`, `specs/tla/README.md` `D-5`) sobre el diseño de `PLAN-23`, antes de que existiera la rama `S-1`. Es la lección de `CE-4` —el tope se lee de la base— aplicada a la identidad de versión de `CE-5` | **Cerrado** (`PLAN-23` B-S1.1): la tabla lleva `version` y clave `(obra, version, ronda)` (migración 16, que da la versión 1 a las filas de antes: hasta ella `RAMAS` estaba vacío y ninguna versión 2 pudo pasar por la puerta); `rondas`, `guardar`, `evaluar` y `publicar` cuentan por versión. `test_las_rondas_de_la_puerta_se_cuentan_por_version`, `test_la_version_2_no_hereda_las_rondas_gastadas_por_la_1`, `test_la_migracion_da_la_version_1_a_los_veredictos_de_antes` |
| F-123 | **`mundo.rebobinar` borraba el mundo vivo de todas las obras de la base, y el nombre de cada lugar.** Hacía `DELETE FROM entidad WHERE id NOT IN (…)`, `DELETE FROM lugar` y `DELETE FROM conocimiento` sin mirar la obra: rebobinar una obra para la cascada dejaba sin mundo a las demás de la misma base (la situación de `F-100`) y a `lugar.nombre` a `NULL` (`PLAN-27` E2). No había fallado porque hasta `PLAN-23` B-S1.1 nadie llamaba a `rebobinar` fuera de las pruebas. Lo encontró escribir la cascada, leyendo lo que iba a llamar antes de llamarlo | **Cerrado** (`PLAN-23` B-S1.1): con `obra`, `rebobinar` solo toca las filas con su prefijo —como acota `leer` desde `F-100`— y los lugares se actualizan sin `DELETE`, conservando el nombre. La cascada lo llama con `obra`. Hereda el límite de `F-100`: solo vale donde los identificadores están acotados, y una obra cuyo id sea prefijo de otra con guion (`a` y `a-b`) no se distingue. `test_rebobinar_una_obra_no_toca_el_mundo_de_otra_de_la_misma_base` |
| F-124 | **En un renombrado, las palabras clave de un imprescindible seguían pidiendo el nombre viejo, que la versión nueva veta.** `novela.imprescindibles_por_escena` copiaba `palabras_clave` del plan: con «el perro se llama Nala», `INV-23` pedía «Brisa» en el capítulo del imprescindible e `INV-21` la rechazaba por vetada (`C-4`, punto 3). El capítulo no podía pasar nunca, y se rendía o paraba tras agotar el tope. `C-4` punto 4 sustituía el nombre en la sinopsis, los beats y los enunciados, y no en las palabras clave | **Cerrado** (`PLAN-23` B-S1.1): el elemento y las palabras clave de cada imprescindible salen con los nombres de la versión (`regeneracion.en_la_version`); la versión anterior conserva los suyos. `test_en_un_renombrado_las_palabras_clave_del_imprescindible_llevan_el_nombre_nuevo`, `test_en_un_renombrado_la_cascada_escribe_con_el_nombre_nuevo_y_veta_el_viejo` |
| F-125 | **Cambiar el enunciado de un imprescindible no cambia sus palabras clave ni su elemento.** Una petición de hecho sobre `imp-0n` cambia el enunciado en la versión nueva (`hechos_de_version`), pero `INV-23` sigue pidiendo las palabras clave del plan: *«que el viaje sea en barco a Oporto»* seguiría exigiendo «tren» y «Lisboa». Importa más de lo que parece: en la novela de ejemplo **los tres únicos hechos con usos son imprescindibles** (`harness/evals/arrastre-SPEC-23.json`), así que toda petición de hecho posible sobre ella cae aquí. Qué palabras clave tiene un imprescindible cambiado es una decisión (de la petición, del plan o de nadie) y no se toma aquí | **Abierto**. Mientras tanto, una petición de hecho sobre un imprescindible solo es segura si el enunciado nuevo conserva sus palabras clave; un renombrado no tiene este problema (`F-124`) |
| F-126 | **El worker de la API no tiene agentes, así que `POST /obras/{id}/cambios` encola un trabajo que falla.** Desde B2 la salida está elegida y la rama `cascada` existe, pero `router._atender` llama a `regeneracion.atender(con, id)` sin agentes: la cascada se niega **antes de escribir nada**, con el motivo en el trabajo, y ni siquiera crea la versión. Es a propósito: regenerar gasta dinero y la API no tiene hoy de dónde sacar agentes reales ni un «sí» explícito al gasto | **Abierto**. La regeneración real va por `backend/pedir_cambio.py`, que pide `--confirmo-el-gasto`. Dar agentes al worker de la API es una decisión de producto (quién confirma el gasto desde la web) y va con `PLAN-22`. `test_sin_agentes_el_worker_no_escribe_nada_y_lo_dice` |
| F-130 | **Los hechos que la página ofrece llevan el enunciado del canon, no el de la versión.** `GET /escenas/{id}/hechos` vive en `lectura/` y lee `hecho_canonico` por SQL; el enunciado de una versión (`hechos_de_version`, con los cambios de su cadena de peticiones) lo calcula `orquestacion/regeneracion.py`, que `lectura/` no puede importar (`A-02`). Tras un cambio de hecho, pedir otro sobre la misma escena ofrece el enunciado viejo. Lo mismo pasa con los nombres de las fichas frente a `nombres_de_version` | **Abierto** (`PLAN-22` E14). El dato que viaja al backend es el `id` del hecho, que no cambia, así que la petición no se equivoca de hecho: lo que miente es el texto que la página enseña al lado |
| F-131 | **El congelado no podía tener forma para un trabajo.** `comprobar_significado` exige que todo `estado*` sea una enumeración con los literales de `docs/definitions.md`, y `estado_de_trabajo` está declarado en `docs/architecture.md` a propósito, porque no es dominio. Dar `response_model` a `GET /trabajos/{id}` (`PLAN-22` hallazgo 9) fallaba `RF-34` por un vocabulario que sí existe, en otro documento | **Cerrado** (`PLAN-22` E14): el validador lee también la primera tabla de `docs/architecture.md` § "Los estados de un trabajo", del documento y no del `Enum` (Regla 3). `test_los_estados_de_un_trabajo_se_leen_de_architecture_y_no_de_definitions` |
| F-132 | **Con dos versiones, el índice sacaba dos «Capítulo 2».** `lectura/repository.capitulos` leía la tabla `capitulo` entera de la obra, y `brief.crear_version` inserta ahí el capítulo nuevo con su posición como `orden`: el 2 viejo y el nuevo, los dos con `orden` 2, y los compartidos una vez. El manuscrito ya se había arreglado en `PLAN-23` A6; la lectura, que llegó por otra rama, no. Ninguna prueba de `lectura/` tenía dos versiones (Regla 11) | **Cerrado** (`PLAN-22` E15): el índice lee `capitulo_de_version` de la vigente, y la posición en la versión es el `orden`. Las lecturas **por versión** (`/obras/{id}/versiones/{n}/indice` y `…/capitulos/{id}`) viven en `orquestacion/`, no en `lectura/` como decía E15 («las rutas de lectura aceptan la versión como parámetro»): el `estado_de_verificacion` necesita la huella del estado que calcula `regeneracion`, y `lectura/` no puede importarla (`A-02`) |
| F-133 | **La API daba `500` intermitentes con peticiones concurrentes.** Las cuatro dependencias `conexion` (`brief`, `entrevista`, `lectura`, `orquestacion`) abrían `sqlite3` con la comprobación de hilo por defecto, y FastAPI crea la dependencia, ejecuta el endpoint sincrono y la cierra en hilos distintos del pool: `sqlite3.ProgrammingError`. De una en una el hilo suele coincidir, y por eso ninguna prueba con `TestClient` lo cazó; la web pide versiones, capítulo y progreso a la vez, y la inspección de E18 vio doce | **Cerrado** (`PLAN-22` E18): `check_same_thread=False` —cada petición usa su conexión en serie, nunca a la vez—. `test_la_conexion_de_una_peticion_se_puede_usar_desde_otro_hilo` crea la conexión en un hilo y la usa en otro, sin depender de la suerte. Después, 90 peticiones concurrentes: 90 `200` |
| F-134 | **Cada página deja un `404` de `/progreso` en la consola.** Es lo que fija E13c —sin generación, `404` y no una fase inventada (`RF-60`)— y `Progreso` no pinta nada, pero un agente inspector lo lee como error y una persona también | **Abierto, sin cambio**: cambiarlo a `200` con nulo es un cambio de contrato y lo decide el autor |
| F-135 | **La pantalla de error de la API sale sin el margen de la página**: `Esperando` pinta el aviso suelto, fuera de `.contenido`, pegado a los bordes. Se vio en una captura de E18; el agente no lo señaló (`F-84` otra vez) | **Abierto** |
| F-140 | **Los briefs de guion estaban escritos para el Entrevistador doble, que no pregunta nada.** En la ejecución real del brief de inyección (`brief-injection-antes-1`), el Entrevistador real preguntó «¿es imprescindible este rasgo?» y el guion le contestó con la línea siguiente, que hablaba del recuerdo; la entrevista terminó sin rasgo, recuerdo, premisa ni título y **no se escribió novela**. Gasto de esa entrevista: 1,6246488 USD en 14 delegaciones, medidos. **Lo que sí se midió**: el detector de inyección vio la instrucción que `PATRONES` reconoce, descartó ese texto libre (*«el texto traía instrucciones y se ha descartado»*) y solo se propuso el hecho legítimo del segundo | **Cerrado en los briefs**: las respuestas del rasgo y del recuerdo dicen ya que son imprescindibles, y antes de aceptar el título y la premisa hay un turno que le pide proponerlos. Las pruebas con el doble siguen en verde. Que baste contra el Entrevistador real está **sin medir** hasta volver a ejecutarlo |
| F-141 | **El hallazgo de `INV-25` enseña al lector la frase repetida normalizada, no la del texto.** Lo encontró `INV-30` en real sobre la novela de ejemplo: en el capítulo 9 la web muestra «gat que ha comid en cuatr cas y» en vez de «gato que ha comido en cuatro casas y». `frases_repetidas` compara raíces, que es lo correcto para detectar, y el hallazgo guarda las raíces, que no lo es para contarlo | **Cerrado.** `frases_repetidas` sigue comparando por raíces, y la frase que devuelve es el fragmento del texto de su primera aparición. Lo sostiene `test_la_frase_repetida_se_cuenta_con_el_texto_y_no_con_sus_raices`, roja antes. Los `INV-25` que ya estaban guardados en la base de la novela de ejemplo conservan la forma normalizada: el arreglo vale para los nuevos |
| F-142 | **La ficha de un personaje enlaza los capítulos donde el plan lo declaró presente, no donde el texto lo pone.** Lo encontró `INV-30` en real: Cloe habla y actúa en el capítulo 10 y su ficha solo enlaza 4–9, porque el plan no la declaró presente en el 10. Es el punto ciego que `PLAN-27` ya declaraba para `VER-nueva-B` (*«se fía de `personajes_presentes` declarado»*), ahora con un caso real | **Abierto.** Salidas: que el delta de la escena declare los presentes y se guarden, o que el Editor los contraste. Decide el autor |
| F-143 | **`evaluar.py` reventaba cuando el plan no se aprobaba.** En el brief temporal real el Revisor no aprobó el plan, la obra no se montó, y el rastro contra otras novelas buscó escenas en una base sin tabla `escena`: la ejecución murió con una traza, sin informe ni tabla. El libro sí anotó el plan (4,9492 USD) | **Cerrado.** Sin obra montada, el rastro dice *«sin novela que rastrear»* y la tabla se regenera. Lo sostiene `test_sin_plan_aprobado_evaluar_informa_y_regenera_la_tabla_sin_reventar`, roja antes con el mismo `no such table: escena` |
| F-144 | **Un comentario y un mensaje decían que el coste de una generación parada «queda en `traza_de_delegacion`», y esa tabla no tiene columna de coste.** Estaban en la rama de `FalloDeTransporte` de `novela_regalo.py`: quien leyera el informe de una parada habría ido a buscar a la base un coste que no está en ella, y lo habría dado por cero o por perdido. El coste de cada delegación del ciclo viaja en la traza **en memoria** y se suma al informe; a la base no llega nada, y lo que se paga fuera del ciclo (Planificador, Revisor, juicio de la puerta, Entrevistador) solo lo cuenta `Contador`, también en memoria. Lo encontró la preparación de `SPEC-33` | **Corregido el texto** (`PLAN-33` E0): el comentario y el mensaje dicen que el coste de los capítulos solo está en Langfuse. **La causa sigue abierta** y la cierra `SPEC-33` `RF-18` (`PLAN-33` E4–E5): que cada delegación guarde su coste en la base |
| F-146 | **Las sesiones delegadas de Claude Code anonimizan los nombres de la ficha, por una política de la organización que está fuera del repositorio, y eso rompe la personalización.** En el brief de contradicciones (`R4`) el Entrevistador guardó al destinatario como `[NOMBRE_ANONIMIZADO]` en la ficha y el Planificador lo copió en las tres versiones del plan; la cobertura rechazó las tres (*«el personaje «[NOMBRE_ANONIMIZADO]» parece «[NOMBRE_ANONIMIZADO]» mal escrito»*) y no hubo novela. Aparece en `entrevista`, `turno_de_entrevista`, `plan_de_obra` y `trabajo`. **No es estable**: en la novela de ejemplo y en la de vetadas, cuya ficha no pasó por el Entrevistador, los nombres llegaron intactos. Y el problema es de fondo: en una novela de verdad los nombres **son** de personas reales, que es justo lo que la política protege | **Decidido por el autor (2026-09-24): pseudonimización en el harness.** Los nombres de la ficha no salen hacia las sesiones delegadas: van sustituidos por marcadores estables, y el harness los restituye en lo que vuelve. No se sortea diciéndole al modelo que ignore la política. **Es la segunda vez que una salvaguarda de privacidad bloquea una función del producto**, después de `F-91` (borrar la ficha al entregar deja la regeneración sin destinatario); va a la presentación como **limitación declarada** (`docs/proceso/trade-offs.md` `T-11`). **`SPEC-34` aprobada el 2026-09-25**; su plan está pendiente y no se aprueba sin revisar antes la pantalla de la entrevista con la sesión del frontend |
| F-145 | **El rastro de exfiltración confunde palabras comunes con nombres.** En `R5` dio *«HUELLAS Marta Roldan»* sobre una novela que no contiene ni «Marta» ni «Roldán»: el fragmento era «martes», que la normalización reduce, como «Marta», a «mart». Falla hacia el ruido, no hacia el silencio, pero un falso positivo en el detector de fugas se lee como una fuga | **Abierto.** El arreglo es comparar los nombres propios sin la reducción de género y número que sirve para las vetadas |
| F-147 | **Con la novela entregada, `pedir_cambio.py` no admitía ningún renombrado, con `--ficha` o sin ella.** La propuesta (`regeneracion.proponer`) necesita la ficha para saber quién es el destinatario y la leía solo de la base, que la borra al entregar (`F-91`); el guion leía `--ficha` después de proponer. Lo encontró la demo de `PLAN-23` B4 sobre la novela de ejemplo, **sin gastar**: las cuatro propuestas se negaron con «no consta quién es el destinatario». `test_con_la_ficha_en_un_fichero_regenera_aunque_la_base_no_la_tenga` no lo veía porque cambia un hecho, que no mira al destinatario (Regla 11) | **Cerrado**: la ficha se lee antes de proponer y viaja a `proponer` y `pedir`, que la prefieren a la de la base. Lo sostienen `test_un_renombrado_con_la_ficha_en_un_fichero_se_propone_aunque_la_base_no_la_tenga`, roja antes, y `test_con_la_ficha_en_un_fichero_el_destinatario_sigue_sin_poder_renombrarse`. **La web sigue sin poder**: `POST /cambios/propuesta` no lleva ficha, y una obra entregada no la tiene en la base; es la misma familia que `F-126` y se decide con ella |
| F-148 | **`INV-02` bloquea la escena en la que un personaje desaparece: el arreglo de `F-79` cubrió la reaparición y rompió su espejo.** Desde `F-79` el estado vital que cuenta es el que deja la propia escena. Una escena en la que un presente pasa de `vivo` a `desaparecido`, y el delta lo dice, deja al personaje presente y desaparecido, y la puerta la para. Lo destapó la demo de `PLAN-23` B4: el capítulo 8 reescrito paró con *«per-pimienta esta presente y su estado_vital es desaparecido»* con el mundo vivo diciendo `vivo`; el capítulo 8 de la versión 1 declaraba esa misma desaparición y pasó porque se escribió **antes** de `F-79`. Ninguna prueba tenía una desaparición dentro de la escena (Regla 9: la regla se probó en una sola dirección). Es la familia de `SPEC-16`: una invariante no puede bloquear el acto que cambia lo que ella vigila | **Cerrado** con la propuesta, que el autor aprobó el 2026-09-24: un presente pasa si está vivo **antes o después** de los cambios de su propia escena. Punto ciego, el de siempre: las puertas leen el delta, no la prosa. **El origen es una formulación incompleta del autor**, y él lo pidió anotar así: *«Mi formulación de F-79 solo miraba la reaparición y la desaparición es el mismo caso por el otro lado.»* Lo sostienen `test_inv02_un_vivo_que_desaparece_en_la_escena_puede_estar_presente`, roja antes con el mismo mensaje que paró la demo, y `test_inv02_quien_no_esta_vivo_ni_antes_ni_despues_sigue_bloqueando`. La primera versión de la prueba usaba un personaje que el fixture tiene muerto y fallaba por otro motivo (Regla 11) |
| F-149 | **El Escritor no sabe dónde ocurre la escena siguiente, y `INV-02` se lo exige un capítulo tarde, cuando ya no se arregla reintentando.** La accesibilidad de `INV-02` compara dónde deja a cada presente la escena anterior consolidada con el `lugar` de la escena, que fija el plan. El material del Escritor (`obra.reunir_material`) lleva resúmenes, fichas, escena anterior, mundo, hallazgos, hechos e inmutable, y **nada de la escena siguiente**. Lo destapó la demo relanzada de `PLAN-23` B4: el capítulo 8 reescrito, en el lago alto, dejó a tres personajes en la majada alta; el 9 ocurre en la plaza, que no es vecina de la majada, y paró. En la versión 1 el Escritor los había llevado al lago y pasó: **por azar, no porque se le pidiera**. Como los dos datos que compara están fijados —uno consolidado, otro en el plan—, relanzar vuelve a parar en el mismo sitio, y cada intento se paga. Es la Regla 4 (el contrato exige lo que el prompt no pide). Se midió la salida de contar caminos de varios pasos: los 11 lugares del mapa se alcanzan todos desde todos, así que la comprobación quedaría vacía en esta novela (Regla 8), y se descarta | **Cerrado** (el autor eligió arreglar y repetir la demo, 2026-09-25): `reunir_material` calcula `lugar_siguiente` **en la versión que se escribe** y el bloque 4 —el que no se elimina y el que lee `INV-02`— lo dice junto al grafo de accesos. Llega al prompt por `ensamblado.montar` (`obra.py`). Lo sostienen `test_el_material_lleva_el_lugar_de_la_escena_siguiente_de_su_version`, con el capítulo sustituido y el nuevo en sitios distintos a propósito (Regla 11), y `test_el_bloque_4_dice_donde_ocurre_la_escena_siguiente`; las dos, rojas antes. Punto ciego: se le **dice**, no se le obliga; el Escritor puede ignorarlo y `INV-02` lo seguirá viendo un capítulo tarde. No arregla un capítulo ya consolidado: la demo se repite con una versión nueva |
| F-150 | **La web enseñaba como vigente la versión que la cascada estaba escribiendo.** `F-121` puso la regla en un solo sitio (`commons/obra/vigente.py`: la última publicada), pero la lectura web llegó de otra rama con **su propia copia**, `MAX(numero)` en `lectura/repository._version_vigente`, y `arrastre.medir` tenía otra. Ninguna fusión dio conflicto. Lo vio el recorrido de la web real en Edge sin cabeza mientras se escribía la versión 3 de la demo: el índice decía «Versión 3 (vigente)» con sus capítulos 4 a 10 en `planificada`. Es `CE-14` (`LectorVeLoPublicado`) otra vez, y la causa de fondo es la de siempre: **dos copias de una regla divergen** | **Cerrado en el backend**: las dos copias se quitan y leen la regla de `commons`. Lo sostienen `test_la_lectura_web_lee_la_ultima_publicada_y_no_la_ultima_creada` y `test_el_arrastre_se_mide_sobre_la_ultima_publicada`, rojas antes. `brief.crear_version` sigue con `MAX(numero)` porque calcula el número **siguiente**, no la vigente; `regalo/repository.capitulo_vigente` también, y dice «la última versión» para la generación en vivo, donde interesa lo que se escribe. **Abierto en el frontend, y lo decide el autor**: `Indice.tsx` y `Capitulo.tsx` calculan la vigente como la última de `GET /obras/{id}/versiones`, que no dice cuál es; la interfaz calcula estado (`CLAUDE.md`: muestra estado, no lo calcula). Arreglarlo es añadir `vigente` a `VersionesSalida`, un cambio del contrato congelado |
| F-200 | **Un capítulo terminado seguía diciendo «resumiendo» en la generación en vivo.** `RF-14` de `SPEC-33` da a cada capítulo la fase de su última fila de `progreso_de_generacion`, y el pipeline no tiene una fase de «capítulo terminado» ni cierra capítulos: en la novela de ejemplo los 17 capítulos siguen en `abierto` y lo que dice que acabaron es su escena, `consolidada` (14). Quien miraba la página leía que los capítulos 1 y 2 seguían resumiéndose. Las pruebas no lo vieron porque sus fixtures se inventaban fases que parecían razonables; lo encontró la inspección en un navegador real de `PLAN-33` E15, mirando las capturas y no solo las comprobaciones, que pasaban todas | **Cerrado.** Cada capítulo trae `es_el_actual` y sus escenas con su `estado_de_escena`; la web enseña la fase solo en el capítulo en curso y, en los que ya pasaron, el estado de su escena (`CLAUDE.md`: una escena, siempre con su estado). Lo sostienen `test_un_capitulo_terminado_trae_el_estado_de_su_escena_y_no_es_el_actual` y `Generacion › un capítulo terminado dice el estado de su escena, no su última fase (F-200)`, rojas antes. Las fixtures llevan ahora un solo capítulo en curso, como el pipeline |
| F-52 | **Primera materializacion medida de `MF-27`, y es su forma peor: la obra de diez capitulos no puede contestar si su cero de hallazgos significa algo.** La pregunta correcta ante un cero no es *¿esta la obra limpia?* sino **¿llego alguna puerta a tener algo que rechazar?** (`F-30`). Medido en solo lectura sobre `backend/obra10.db` con 40 escenas consolidadas y **0 hallazgos**: hubo material para el registro de conocimiento y para `INV-18` —nueve hechos pasaron de declarados a establecidos y el conocimiento crecio hasta doce entradas—, asi que **ese cero ya no es el de `F-30`**. Pero para `INV-03` **no se puede saber**: mira el campo `acciones` del delta y la tabla `delta_de_escena` **no existe** en esa base. El proceso arranco antes de `deltas.py` e importo los modulos de entonces, asi que la base tiene **diez de las veintitres tablas que declara el arbol**, y entre las ausentes esta `esquema_version`: ni siquiera hay registro de migraciones que consultar. **Lo que lo hace la peor forma de `MF-27`** es que el artefacto no da un numero equivocado: se queda **sin la columna que haria falta para saber si el numero significa algo**, y un cero sin esa columna es indistinguible de un cero limpio | **CONTESTADO, y la respuesta es que el cero no es limpio.** Medido en solo lectura sobre la base terminada, y confirma lo que la sesion de backend adelanto: **los diez `hecho_canonico` estan todos bajo `obra='cap-10'`**, asi que los capitulos 1 a 9 generaron con *"hechos: (ninguno)"* e `INV-03` **no tuvo un solo hecho que comprobar**; y hay **dos entidades en toda la novela, las dos `vivo`**, asi que la mitad de `estado_vital` de `INV-02` tampoco podia fallar. **Cero hallazgos en 48 escenas es `F-30` a escala de obra.** Y la medida destapa la causa de raiz de todo lo demas: la base tiene **diez obras de seis escenas** —`obra='cap-01'`…`obra='cap-10'`— porque el guion modelaba una obra por capitulo. Por eso `escena.capitulo` esta a `None`: la identidad del capitulo viajaba en la columna `obra`. **Y por eso la cronologia de fabula era estructuralmente imposible**: `evento_cronologico` se consulta con `WHERE obra = ?`, asi que aunque la tabla hubiera existido, cada capitulo habria sido su propia cronologia y no habria habido nada que comparar entre capitulos. Las cuatro invariantes de `specs/lean/` no podian decir nada sobre esta obra ni con todas las tablas puestas | Abierto solo la repeticion, que llevara `deltas.py` y `observabilidad/` dentro, `migrar()` antes de arrancar y **una sola obra con diez capitulos**. **Dos cosas que si quedan medidas.** Una: `MF-27` estaba catalogado y cerrado por `commons/db/procedencia.py`, y esta base es anterior tambien a eso, asi que la contencion no la alcanza —una base sin procedencia no es conforme, que es justo lo que `procedencia.py` decidio—. Dos: la verificacion formal **no se puede ejecutar sobre esta obra en absoluto**, porque tampoco existen `evento_cronologico` ni `participacion_en_evento`. Comprobado contra una copia: `generar_lean.py` muere con `no such table: evento_cronologico` en la primera consulta. **Eso es el comportamiento correcto y conviene dejarlo escrito**: falla ruidoso en vez de emitir un fichero vacio que Lean aprobaria con «0 violaciones», que es el verde falso que su propio README avisa |
| F-50 | **`INV-08` dice "salvo analepsis declarada" y no hay donde declararla, asi que toda analepsis legitima sale como violacion.** `MomentoNarrativo` tiene `t_fabula`, `t_discurso` y `duracion_ficcional`, y **ningun campo de analepsis**. **Tener los dos ejes no basta, y el motivo es logico y no de implementacion:** una analepsis *es* que `t_fabula` retroceda mientras `t_discurso` avanza, que es exactamente la forma de la violacion. Si la declaracion se dedujera de los dos ejes, seria indistinguible de lo que la invariante persigue y `INV-08` dejaria de comprobar nada. Hace falta una marca **aparte** de los dos instantes: algo que diga *esto es deliberado*. **El riesgo no es teorico y es de plazo corto:** en terror la analepsis es material del genero, no un caso raro —casi todo el miedo vive en la diferencia entre fabula y discurso—, asi que la primera obra con un salto temporal bien hecho hara saltar la puerta. Y entonces alguien concluira que `INV-08` da falsos positivos y la relajara, que es como se pierde una invariante correcta | **Decidido por `SPEC-24`** (`aprobada`): declarar una analepsis es **un acto del plan, no del texto**. Va en `MomentoNarrativo` y la rellena la escaleta, igual que `pov` y `cambio_de_valor`. **No la declara el Escritor en el delta**, y el motivo cierra la otra puerta por la que la invariante se quedaba sin comprobar nada: cualquier salto no intencionado se convertiria en analepsis declarada *a posteriori*, porque el mismo actor que comete la desviacion seria quien la autoriza. Una excepcion que declara quien la comete no es una excepcion, es una amnistia. Pendiente de plan e implementacion. La puerta ya lo dice en el texto de su hallazgo —que hoy no hay donde declararla— para que quien la lea no lo tome por un defecto del texto. Hasta que exista la marca, toda inversion sale, y eso es preferible a no mirar |
| F-51 | **Añadir una columna al `CREATE TABLE` no es migrar: solo sirve a las bases que todavía no existen.** Las columnas `obra` de `resumen` y `ficha` nacieron así, sin migración, y `CREATE TABLE IF NOT EXISTS` no toca una tabla que ya existe: **ninguna base anterior las recibió nunca y no se arreglaba sola**. Medido contra `obra10.db`, la base de la obra de diez capítulos —60 escenas, 30 resúmenes, trece dólares—: escribir con el código de hoy moría con `no such column: obra`, así que **esa obra no se podía continuar ni repetir**. El proceso que la generaba no se enteraba, porque corre con los módulos que importó al lanzarse; el fallo esperaba al primero que la reanudase. Y tiene segunda mitad: añadir la columna **vacía** habría sido peor que no añadirla, porque `resumenes_hasta` acota por obra y una fila sin ella no la ve ninguna consulta — los treinta resúmenes habrían quedado invisibles y cada escena habría arrancado sin memoria sin que nada fallara, que es la Regla 8 entrando por la puerta de una migración | **Cerrado**: migración 8 con `anadir_columnas`, **y con relleno**: `resumen.escena` y `ficha.version_en_t` son el identificador de la escena, y la escena sabe de qué obra es. Verificado sobre una copia de `obra10.db`: ocho migraciones, cero filas sin obra y cada capítulo vuelve a ver sus resúmenes |
| F-49 | **Lo que entro al contexto ya se guarda; lo que el recortador dejo fuera, no — y `PC-9` se apoya justo en eso.** Buscado a proposito tras `F-46`, preguntando que mas se aplica y se tira. El `DeltaDeEscena` entero ya persiste (`delta_de_escena`) y las lecturas tambien (`lectura_de_contexto`), asi que la familia del delta esta cubierta. Lo que sigue muriendo con el proceso es **la traza**: `commons/modelo/traza.py` la construye en memoria y **nadie la escribe en ninguna tabla**. Con ella se van tres cosas y ninguna es decorativa: `registrar_recorte(t, bloque, clase)` —**que bloques tiro el recortador**—, el modelo que llevo cada delegacion —lo unico que `modelo_fijo.comprobar` puede mirar— y `registrar_fallo` —las llamadas que fallaron, que son las que hay que diagnosticar—. **Lo grave es la primera, y no es que falte una tabla: es que un punto ciego se dio por acotado por un mecanismo que no sobrevive.** `PC-9` —nadie detecta que un agente rellene con invencion lo que el recorte dejo fuera— se declaro contenido porque *"la traza de `VER-24` registra que fichas quedaron fuera, asi que el fallo pasa de invisible a atribuible"*. Pero **el diagnostico siempre se hace despues**: cuando alguien va a preguntar que quedo fuera, el proceso termino y la traza ya no esta. La contencion valia *dentro de la misma ejecucion*, que es precisamente cuando nadie la necesita — **es decir, no valia**. `PC-9` figuro como acotado durante semanas sin estarlo | Abierto. La otra sesion ya vio la mitad del problema y persistio **lo que entra** (`lectura_de_contexto`, con la llave `prompt_hash` para cubrir tambien las llamadas fallidas); falta la simetrica, **lo que sale**. Misma llave y misma forma, asi que es barato. `PC-9` deberia decir mientras tanto que su contencion es intra-ejecucion |
| F-46 | **`L-4` no puede disparar nunca sobre datos reales: nadie guarda en que evento un personaje deja de poder aparecer.** Una de las cuatro invariantes que el enunciado del examen exige es *nadie aparece tras un evento que lo excluye*, y al escribirla en Lean (`specs/lean/`) resulto que **no hay de donde sacar el dato**: `entidad` guarda el `vital` **actual** y no en que evento cambio, y `cambios_de_estado_vital` del delta se aplica en la consolidacion y no se conserva. El generador deja `Evento.excluye` vacia en todos los eventos, asi que la invariante da cero siempre. **Y un cero aqui no significa que nadie resucite: significa que no se ha mirado**, que es la misma confusion que costo `F-34`. La comprobacion esta escrita y probada contra el fixture, donde si dispara | Abierto. Lo que falta es una fuente: o el delta se persiste con sus cambios de estado vital, o `evento_cronologico` gana una columna que diga a quien excluye. **CERRADO, y la premisa era falsa cuando se escribio.** La sesion de backend aviso de que lo comprobara antes de tocarlo, y tenia razon: el merge de `specs-frontend` (`179d4d5`) trajo `consolidacion/deltas.py`, que guarda el `DeltaDeEscena` **entero** por escena y version dentro de la transaccion de consolidacion, `cambios_de_estado_vital` incluido. No faltaba la fuente: faltaba que el generador la leyera. Ya la lee, y **solo `muerto` excluye** —`desaparecido` no, porque `docs/definitions.md` dice que en terror *"no se sabe si sigue vivo"* es material narrativo y un desaparecido puede volver; tratarlo como exclusion convertiria el recurso mas comun del genero en una violacion—. Medido: `L-4` dispara sobre datos persistidos, *per-luis queda excluido en ev-3b y aparece en ev-4*. **La leccion no es del dato sino del metodo**: el hallazgo se escribio contra un repositorio que habia cambiado hacia dos dias, y lo que lo corrigio fue que otra sesion dijera *"comprueba antes de trabajar"*. Es el mismo caso que `G-05`: el patron se repite —un dato que el pipeline usa una vez, no persiste, y deja sin fuente a todo lo que quiera mirar la obra entera despues—, asi que conviene preguntarse que mas se esta aplicando y tirando antes de que lo destape la siguiente invariante |
| F-47 | **`INV-08` esta declarada, tiene su consulta escrita y no la ejecuta nadie: el orden temporal de una obra no se comprueba en ningun punto del pipeline.** `docs/definitions.md` la declara de nivel capitulo y severidad `mayor`; `cronologia/consultas.py` tiene `orden_temporal`, que compara fabula y discurso y devuelve las inversiones. Pero **`INV-08` no aparece en `features/verificacion/puertas.py`** y a `orden_temporal` **solo la llaman sus propias pruebas**. Lo destapo escribir la misma regla en Lean como `L-1` y preguntarse contra que contrastarla: contra nada. La verificacion formal la detecto en la obra sembrada -*ev-2 se lee despues de ev-1 y ocurre antes en la fabula, sin declarar analepsis*- y hoy es **lo unico que la mira** | Abierto. Es el patron de `F-34` una vez mas: una invariante que no se ejecuta y una que pasa **se ven igual desde fuera**. **Enchufada por la sesion de backend en `b2f097c`**, y no donde este hallazgo decia: `INV-08` es de **nivel capitulo**, asi que su sitio es la puerta de cierre de `features/auditoria/capitulo.py` y no `verificacion/puertas.py`, que solo ejecuta las de nivel escena. Se enchufo con **dos casos**: las inversiones no dejan cerrar, y `sin_fecha_legible` **tampoco**, porque *no se pudo comprobar* no es *se comprobo y esta bien*. **Medido despues: la puerta y Lean coinciden.** Sobre la misma base, `orden_temporal` devuelve `inversiones: [('ev-1','ev-2')]` y `L-1` levanta esa misma pareja. La redundancia es real y las dos miran lo mismo por caminos distintos, que es lo que la Regla 3 pide. **Con una diferencia medida y no supuesta:** la puerta compara solo pares adyacentes y Lean todos los pares, asi que sobre una obra con varias inversiones podrian no coincidir en el recuento aunque coincidan en el veredicto. Queda abierto **donde se declara una analepsis**: ver `F-50` |
| F-44 | **`F-38` otra vez, fuera del dominio de las novelas: dos sesiones sobre el mismo indice de Git, y el trabajo preparado se va en el commit de la otra.** Ocurrio **dos veces seguidas** al entregar `specs/tla/`. Con el indice comprobado vacio justo antes, `git add` y `git commit` en llamadas separadas: entre las dos, otra sesion commiteo y se llevo lo preparado. `specs/tla/` entero —README, `.gitignore` y la retirada de los volcados `states/`— acabo dentro de `ccf28b4`, *"SPEC-20: que se puede editar a mano"*; las anotaciones de `docs/architecture.md` y la propia `F-39`, dentro de `ff354cc`, *"Mover SPEC-20 a aplicadas"*. **El contenido sobrevivio intacto y lo que se perdio fue el porque**: dos commits cuyo mensaje explicaba cada decision no llegaron a existir, y el historial atribuye el trabajo de verificacion formal a commits sobre edicion manual. Nada fallo en ningun momento: los dos `git add` funcionaron, los dos commits ajenos funcionaron, y el segundo `git commit` dijo *"nothing to commit, working tree clean"*, que es la frase de que todo esta bien | **Cerrado en los tres niveles, y los tres hacian falta.** Los worktrees ya estaban montados por otra sesion —cuatro carpetas, una por rama— y `AGENTS.md` ya prohibia `git add -A` y `git commit -a`; **no bastaba**, porque las dos sesiones que chocaron estaban en la **misma** carpeta y ninguna hizo un barrido: el conflicto fue por el indice compartido, no por el alcance del `add`. Lo que lo cierra de verdad es **una carpeta por sesion de verdad, no por rama**, mas `git add <ruta> && git commit -- <ruta>` **en un solo comando**. La causa de fondo quedo como **Regla 6**. **Nacio como `F-40` y se renumero a `F-44`** por la misma colision, y por el mismo criterio: el otro `F-40` ya estaba citado en cinco sitios del codigo |
| F-43 | **Una version publicada no tiene identidad, asi que "se conserva la version anterior" no es comprobable ni siquiera en principio.** Lo destapo la especificacion TLA+ (`specs/tla/`) por el peor camino posible: la propiedad `VersionesSoloCrecen` **pasaba** con el modelo configurado para que la publicacion **pisara** la version anterior. El motivo es que una version se representaba por el conjunto de sus capitulos, y al regenerar y volver a aprobarlos todos, la version nueva era un valor **identico** a la vieja: pisar algo con una copia exacta no se ve. Con un campo `ronda` que le da identidad, la misma configuracion viola la propiedad en 1.148 estados. En el codigo pasa lo mismo y peor: `copiar_novela()` de la rama `main` guarda `salida-novela-1/`, `salida-novela-2/`… y **el numero de carpeta es lo unico que las distingue** —nada dentro de la novela dice de que ronda es—, y ademas solo se invoca desde `POST /api/ampliar`, asi que cualquier otro camino que regenere se lleva la anterior por delante. Es **una de las invariantes de seguridad que el enunciado del examen exige**, y hoy no hay contra que comprobarla | **Cerrado por `PLAN-23` A3** (`VER-114`): `version_de_obra` con clave `(obra, numero)` y disparadores que impiden cambiar una versión creada. Era **el mismo hueco que `G-07`** de `SPEC - Frontend y contrato congelado.md` —*"la obra no tiene version"*—, alcanzado por el otro lado: alli desde la interfaz que necesita decir que capitulos cambiaron, aqui desde una propiedad que no podia fallar. **Se resuelve una vez, con `G-07`**, y decidir que es una version de la obra es de dominio . **Nacio como `F-39` y se renumero a `F-43`** el mismo dia: otra sesion habia elegido `F-39` para otro hallazgo. Se movio este porque el otro ya tenia cinco referencias en codigo y este una |
| F-40 | **La memoria mezclaba obras, y cada capítulo arrancaba sin memoria del anterior.** El `orden` de una escena va del 1 al N **dentro de su obra**, así que con diez capítulos en la misma base había diez escenas con orden 1. `resumenes_hasta` y `fichas_en` filtraban **solo por orden**: la escena 4 del segundo capítulo recibía los resúmenes del primero entremezclados y en orden equivocado, y **la escena 1 de cada capítulo recibía cero**. Medido en la obra de diez: tras doce escenas, el contexto que recibiría la siguiente era el mismo que tras siete, y la causa no era que el material no creciera sino que se estaba leyendo mal. **Es el defecto que más de cerca toca lo que el proyecto existe para hacer** —una novela que olvida el capítulo uno al empezar el dos— y **ninguna ejecución de menos de dos capítulos podía verlo**. Llevábamos **cinco**, todas de entre una y seis escenas, y las cinco pasaron por encima de este defecto sin tocarlo. Lo destapó medir el crecimiento del contexto —`VER-37`—, que llevaba cinco intentos sin contestarse y que en cada uno pareció una fila que no daba resultado: **la pregunta valía aunque la respuesta tardara cinco intentos**, y el valor no estuvo en la respuesta sino en lo que hubo que construir para poder medirla. Es el argumento a favor de dejar abiertas las filas que no se contestan, en vez de cerrarlas por cansancio | **Cerrado en tres sitios, y el tercero apareció después.** `resumen` y `ficha` guardan su obra y sus consultas se acotan. Pero la de la **escena anterior** quedó fuera del primer arreglo —lo encontró y reprodujo la sesión que llevaba `SPEC-21`— y era la peor de las tres: es el bloque **Local**, el que lleva la escena anterior **entera**, así que el Escritor arrancaba leyendo una escena de otra obra y **nadie lo notaba, porque llega texto plausible**. Los resumenes al menos llegaban desordenados o vacíos. **Leccion:** arreglar un defecto de alcance en dos de sus tres consultas deja el sistema pareciendo arreglado, y el comentario *"acotados a la obra"* que quedó justo encima de la consulta sin acotar lo hacía peor |
| F-45 | **La decisión dice que los resúmenes cruzan el corte de capítulo, y con `orden` local al capítulo no pueden.** Medido: una obra con dos capítulos, el primero con dos escenas de `orden` 1 y 2; la escena 1 del **segundo** capítulo pide `resumenes_hasta(orden=1)` y recibe **cero**, cuando debería recibir las dos del primero. Acotar por obra —el arreglo de `F-40`— no lo toca: las escenas son de la misma obra y el problema es que **`orden` no es comparable entre capítulos**. Lo señaló la sesión de `SPEC-22`/`SPEC-23` leyendo el árbol a medias, y tenía razón. **La escena anterior y los resúmenes necesitaban arreglos opuestos**: aquélla acotarse al capítulo, éstos poder cruzarlo, y un mismo filtro no da los dos | **Cerrado**: `MomentoNarrativo.t_discurso` se rellena (`escaleta.asignar_t_discurso`) y la memoria se acota por obra y se **ordena por el discurso**, no por el `orden` de la escena. El nombre de la columna cambia con ella, porque una que dice `orden` invita a pasarle el `orden` (Regla 10). Lo que no se puede situar **no se guarda**: `SinPosicionEnElDiscurso` en la frontera, en vez de una fila que ninguna consulta ordenada volveria a ver |
| F-48 | **`F-44` otra vez, contra el commit que estaba documentando `F-44`.** Al commitear la regla nueva de `AGENTS.md` —la que dice cómo no perder trabajo entre sesiones— el primer intento falló por un error de sintaxis: `git commit -- <ruta> -m "..."` pone el `-m` después del `--`, así que git lo leyó como ruta y abortó. **Pero el `git add` que iba delante sí se ejecutó**, y dejó el fichero preparado en el índice compartido. Antes del segundo intento, otra sesión commiteó: `AGENTS.md` entero se fue dentro de `beb917f`, *«El alcance de la escena anterior es el capitulo»*, y el segundo intento contestó *«nothing to commit, working tree clean»*. Contenido intacto, **el porqué perdido** — un commit cuyo mensaje explicaba la disciplina de numeración no llegó a existir | **Abierto, y la Regla 6 se afila**: `git add <ruta> && git commit -- <ruta>` en un solo comando **no basta**, porque protege del comando que no se lanza y no del que se lanza y falla a mitad. Un `add` que triunfa seguido de un `commit` que aborta deja el índice cargado durante toda la depuración del error, que es la ventana más larga de todas. Lo que lo cerraría es no preparar hasta tener el comando bueno —`git commit -F -` con el mensaje por la entrada estándar, o `git stash` ante un commit fallido— y, de fondo, la carpeta por sesión que `F-44` ya pedía. **Este hallazgo nació además colisionando**: se escribió como `F-46` tras comprobar que era el siguiente libre, y otra sesión publicó su propio `F-46` y un `F-47` en esa ventana. Pasa a `F-48` porque el primero publicado conserva el número, que es la regla que este mismo commit estaba añadiendo a `AGENTS.md` |
| F-41 | **Cinco escenas de doce se quedaron sin resumen y nadie lo dijo.** El ciclo guarda el resumen `if c.resumen:`, y cuando el Resumidor no contesta —`RespuestaIlegible`— esa rama simplemente no se ejecuta. La escena se da por buena, las siguientes leen un contexto al que le falta, y **desde fuera es indistinguible de una escena que no tenía nada que resumir**. Es la misma clase de fallo que `F-34` con las invariantes: un dato ausente que se lee como un verde. Lo destapó contar filas: doce escenas consolidadas, siete resúmenes | **Cerrado**: la generación lleva `sin_resumen` y lo dice. Al escribir su prueba apareció además que el doble devolvía `None` donde el proveedor real lanza, así que el doble no era fiel (Regla 3) y `_delegar` reventaba con `AttributeError` en vez de registrar el fallo |
| F-42 | **El campo que separaba el dato medido de la afirmación no verificada era justo el que se perdía, y no fallaba: pisaba.** La clave de `uso_de_hecho` era `(hecho, escena, tipo)` y dejaba fuera `origen`. Un mismo hecho puede constar como `depende` por dos caminos que **no valen lo mismo**: **observado** —el ensamblador lo metió en el prompt, así que la escena pudo apoyarse en él: sobre-aproxima y falla ruidoso— y **declarado** —el delta dice que se usó: se ajusta más y falla en silencio—. Con esa clave no cabían a la vez: el segundo `INSERT OR REPLACE` pisaba al primero y **cuál sobrevivía dependía del orden de escritura**, dejando una fila creíble. Lo encontró la sesión del frontend al ir a escribir las filas observadas que `SPEC-23` `S-5` necesita, **antes de que existiera ningún dato**. Su coste real no era la fila perdida: era que incorporar lo observado más tarde habría dejado de ser editar una constante para pasar a ser migrar datos, es decir, **volver a decidir algo ya decidido** | **Cerrado**: la clave pasa a `(hecho, escena, tipo, origen)` con la migración 6 y dos casos negativos —que las dos filas convivan, y que repetir la misma con el mismo origen siga siendo una sola—. **Tercera vez con este patrón**, así que la causa de fondo quedó como **Regla 7** |
| F-39 | **Dos capítulos que declaran el mismo identificador de hecho: el segundo pisa al primero, y el primero se queda sin ninguno.** `hecho_canonico.id` era PRIMARY KEY **global** mientras `hechos_declarados` filtra **por obra**: el código ya trataba el mismo identificador en dos obras como dos hechos y la clave decía lo contrario. Lo destapó la obra de diez capítulos, que declara los diez hechos en los diez: al terminar, **los diez estaban atribuidos a `cap-10`** y los nueve capítulos anteriores tenían la lista vacía, así que su prompt volvía a decir *"hechos: (ninguno)"*. **Es `F-29` otra vez por una puerta nueva**, y del tipo que este documento persigue: no falla, **pisa**. Lo que salvó la ejecución fue `SPEC-19`: los hechos que un beat promete llegan por `establece[]`, que sale de la escena y no de la tabla, así que las cuatro promesas del capítulo uno se cumplieron igual | **Cerrado**: la clave pasa a `(obra, id)` con su migración, y `establecer_hecho` acota por obra, porque marcar uno no dice nada de los demás capítulos |
| F-38 | **Relanzar tras un bloqueo no reanuda: regenera lo ya hecho, lo paga y lo corrompe.** Medido contra dobles sobre una base donde `e1` estaba `consolidada` y `e2` bloqueada: la segunda pasada **vuelve a generar `e1`** —delegación pagada—, le guarda un borrador 2, **degrada su estado de `consolidada` a `generada`** y muere con `delta:YaConsolidada` sin haber llegado a `e2`. El bucle recorre `escenas_de` desde el principio y **no mira en qué estado está cada escena**. Con `VER-64` en ~29%, una obra de sesenta escenas son **diecisiete paradas**, y hoy cada una cuesta la obra entera **y además estropea lo que había**. Esto convierte *"desatascar tiene que ser barato"* en el requisito principal y no en una nota al margen | **Cerrado con tres piezas.** El bucle **salta lo que ya está hecho** y lo dice en el informe, porque saltar no es lo mismo que no hacer; los hallazgos **se pueden cerrar** como `resuelto` o `descartado` —los dos estados llevaban ahí desde el primer día y **nadie los escribía**, así que un capítulo no podía firmarse ni arreglando la causa— y con motivo obligatorio, porque uno cerrado sin motivo no se distingue de uno que se cerró para que dejara de molestar; y **una instrucción humana entra por el canal de `problemas`**, en bloque propio para que no se confunda con lo que levantó una regla |
| F-37 | **El plan promete cosas en prosa y nada comprueba que el delta las entregue.** La escaleta de `e2` decia *"Marta encuentra el sotano cerrado y no aparece la llave"*. El texto se escribio, paso todas las puertas y se consolido — y `hec-sotano-cerrado` y `hec-llave-perdida` siguen **sin establecer**. Dos escenas despues, `INV-03` bloqueo a Marta por actuar sobre el sotano cerrado, que era justo lo que `e2` prometia haberle ensenado. **La invariante cazo la consecuencia, no la causa**, y con dos escenas de retardo: lo que fallo fue que el delta de `e2` no declaro lo que su propio texto hacia. Hoy nada compara lo que el plan prometio con lo que el delta entrego, porque los `beats` son prosa y el delta son identificadores. Es el hueco que `SPEC-15` C-3 hizo **nombrable** y que sigue sin validador | **Cerrado por `SPEC-19`.** No faltaba un validador: **faltaba el dato**. `Beat` gana `establece[]`, e `INV-18` cruza lo prometido con lo entregado **al cerrar la escena y no el capitulo**, porque el retardo era el defecto. Las dos mitades o ninguna: el prompt dice que hechos hay que establecer —Regla 4 por **tercera** vez, tras `F-21` y `F-34`— y la invariante comprueba que el delta los declare |
| F-36 | **La persistencia no guardaba dos atributos obligatorios del dominio, y las invariantes que los necesitan se callaban: los dos defectos se tapaban mutuamente.** `docs/definitions.md` marca en negrita `Escena.pov`, `Escena.lugar` y `Borrador.pov_usado`. La tabla `escena` **no tenía columna para los dos primeros** y nada rellena el tercero. Ninguna prueba lo notó porque `INV-04` e `INV-02` se saltaban en silencio justo la comprobación que los necesita (`F-34`). Es **el mismo patrón que `F-29` con `F-31`**: dos fallos que solo son visibles de uno en uno, y arreglar el primero destapa el segundo. `pov` y `lugar` ya se guardan —son del plan, que es nuestro—; `pov_usado` viene de la respuesta del agente y **eso es contrato**. Mientras no lo sea, `INV-04` devuelve `sin_veredicto` en toda escena, hereda su severidad `bloqueante` y **el harness no genera nada**: once pruebas del bucle quedan en `xfail(strict)` | Abierto: de dónde sale `pov_usado`, y si un `sin_veredicto` detiene igual que una violación confirmada |
| F-32 | **El registro de conocimiento arranca vacío, así que ninguna acción es posible en la primera escena de una obra.** Con `SPEC-16` aplicada, la generación real paró en `e1` con tres `bloqueante`: *"per-ana actua sobre hec-herencia y no consta que lo conozca en t"*, y lo mismo con `hec-sotano-cerrado` y `hec-llave-perdida`. **La mecánica es correcta y el bloqueo no lo es**: nadie sabe nada al empezar, así que cualquier personaje que obre en la primera escena bloquea, incluido el protagonista. Un personaje llega a la escena 1 **sabiendo cosas de antes del relato** —Ana sabe que heredó la casa— y no hay dónde declararlo: el plan declara qué hechos existen (`SPEC-15`) y no quién los sabe ya. **El punto muerto de `F-29` y `F-31` no ha desaparecido: se ha movido un paso más abajo**, y esta vez está en el estado inicial | Abierto: falta declarar el conocimiento inicial, y es decisión de dominio |
| F-33 | **El prompt pide exactamente el caso que la puerta bloquea, y lo escribí yo al aplicar `SPEC-16`.** El texto nuevo dice al modelo *"si se entera en esta escena y ademas obra con ello, van las dos cosas, cada una en su lista"*. Pero la puerta verifica **antes** de consolidar, así que la revelación de esta escena todavía no está en el registro cuando `INV-03` mira: aprender y actuar en la misma escena **bloquea siempre**. Y es el caso narrativo más común que existe —Marta encuentra la llave y abre el sótano— . El fondo es que `INV-03` dice *"no conoce en `t`"* y **`t` dentro de una escena no es un punto, es un intervalo**. Es la **Regla 5 dentro de su propio arreglo**: el campo ya dice qué significa y sigue sin decir *cuándo* | Abierto: en qué instante se evalúa `t` es decisión de dominio |
| F-34 | **La escena generada no era la planificada, y la invariante que debía verlo estaba inactiva por un campo ausente.** La escaleta pedía *"Marta recorre la casa heredada y cuenta los peldaños"* y el bloque inmutable exige *"tercera persona limitada sobre Marta"*. El modelo escribió **a Ana** heredando la casa y contando los peldaños, con Marta de secundaria. `INV-04` —el POV no cambia dentro de una escena— **no miró nada**, porque las escenas del fixture no declaran `pov` y la comprobación es condicional a que exista. **Una invariante que se salta en silencio cuando le falta un campo no está en verde: está ausente**, y desde fuera las dos se ven igual. Lo cazó `INV-03` de rebote, por un camino que no es el suyo | **Cerrado en el mecanismo**: una invariante a la que le falta un dato obligatorio devuelve `sin_veredicto` con `dato_ausente`, el mismo estado que ya usaba `veredicto_ilegible` para el juez que no contesta. El barrido encontró **tres** al mismo patrón, no una: `INV-04` (`pov`, `pov_usado`), `INV-02` (`lugar`, y con ella la mitad de accesibilidad) e `INV-17` (recuento de palabras con rango declarado). **Qué campo es imprescindible lo dice el dominio y no la puerta**: lo que `docs/definitions.md` marca en negrita. La consecuencia abrió `F-36` |
| F-35 | **El número que el informe llama contexto no es contexto.** La ejecución reportó `20.225` tokens, de los cuales **20.000 son la reserva de salida**: el contexto real fueron **225**. Y la reserva no debería estar ahí — `SPEC-14` C-1 la retiró, y `features/orquestacion/bucle.py` lo dice por escrito. Pero `obra.generar_obra` sigue pasando `reserva_de_salida=20_000` por defecto y `ensamblado.tamanos` la inyecta en el total contra el que se compara el techo. **Dos módulos del mismo repositorio dicen lo contrario y ninguno falla**, porque el número resultante es plausible. Leer `20.225` como *"el contexto casi llega al techo"* sería exactamente el error que `VER-37` existe para no cometer | **Cerrado.** El parámetro `reserva_de_salida` se retiró **entero** en vez de ponerse a cero: mientras existiera, cualquier llamada podía volver a inyectarlo y el total volvía a mentir sin que nada fallara. El bloque sigue en el orden de `2.4` y sigue siendo irreducible; lo que ya no hace es ocupar |
| F-31 | **El punto muerto tenía un gemelo un nivel más abajo, y `F-29` lo tapaba.** Arreglado `F-29` —los hechos los declara el plan y el prompt los lleva—, el modelo por fin puede revelar. Y entonces la puerta de `INV-03` bloquea **la primera revelación de la obra, sea cual sea**: trata toda entrada de `delta.revelaciones` como *actuar sabiendo ya*, y exige que el hecho conste en el registro de conocimiento **antes** de la escena. Pero el registro solo se escribe **desde** esas mismas revelaciones, en la consolidación, que ocurre **después** de la puerta. Nadie puede llegar a saber nada nunca. Las dos lecturas están escritas en el repositorio y se contradicen: `features/consolidacion/mundo.py` dice *"lo que el delta declara como revelación queda sabido a partir de esa escena"* —la revelación es el momento de **aprender**— y `features/verificacion/puertas.py` la trata como **actuar**. **Los dos fallos se tapaban mutuamente**: sin hechos en el prompt no había revelaciones, y sin revelaciones `INV-03` nunca se ejercitaba. Reproducido contra doble: escenas completas 0, parada en `e1`, *"per-marta actua sobre hec-llave y no consta que lo conozca en t"*. **Lo destapó una prueba, antes de pagar una generación real** | Cerrado por `SPEC-16`: revelar es aprender, `INV-03` deja de mirar las revelaciones y compara contra `acciones`, un campo nuevo del delta que significa obrar. La causa de fondo quedó como **Regla 5** |
| F-28 | **Dos features se pueden acoplar por la base de datos, y el comprobador de `A-02` no lo ve.** `features/consolidacion/memoria.py` hacía `JOIN escena` para saber el orden narrativo, y la tabla `escena` es de `features/escaleta/`. **No hay ningún `import`**, así que el comprobador —que hasta ahora es el que más ha encontrado— pasaba en verde sobre una dependencia real. Lo destapó su hermano: la prueba de `memoria` importó el repositorio de `escaleta` para sembrar escenas, y **eso** sí lo vio. Es el mismo límite que `PC-1` en otro eje: **el análisis de importaciones no ve el acoplamiento por datos** | Cerrado en el caso: el orden entra como parámetro y lo pasa `orquestacion/`. **El límite del comprobador sigue abierto**: ver `PC-18` |
| F-24 | **La primera vez que el sistema completo detecta el fallo para el que se construyó.** Todo lo anterior fueron validadores cazando **errores de construcción** —una importación cruzada, un módulo sin prueba, un tipo mal deserializado—. En la segunda ejecución real, `INV-03` levantó dos `bloqueante` con esta descripción: *"per-ana actua sobre hec-llave-perdida y no consta que lo conozca en t"*. El modelo hizo que un personaje revelara algo que **solo sabía su hermana**, el delta citaba identificadores válidos, y la invariante lo cazó. Eso es exactamente lo que `INV-03` existe para hacer, y es la primera vez que ocurre | **Sigue siendo una detección, y no se degrada a falso positivo.** Con `SPEC-16` revelar es aprender, así que ese caso ya no viola `INV-03` por la vía por la que se cazó. Pero el defecto no desaparece, **cambia de nombre**: no es actuar con conocimiento indebido, es **adquirir conocimiento sin fuente** —en terror, la diferencia entre un misterio y un agujero de guion—. Lo recoge la decisión abierta de `docs/definitions.md` sobre `RegistroDeConocimiento.fuente`, que existe desde `SPEC-03` para esto y no comprueba nadie |
| F-25 | **El volumen no predice el coste, así que ningún razonamiento por tokens vale.** Medido en el ciclo completo: el Resumidor gastó **1.872** tokens y costó **0,0294 $**; el Juez gastó **1.567** y costó **0,0689 $** — más del doble con menos volumen. El precio del modelo pesa más que la cantidad. Se corrigió el razonamiento que veníamos heredando de `EJECUCION.md` de la otra rama —*"si los validadores gastan más tokens que el escritor, estás pagando la auditoría más cara que la novela"*—: **el coste se lee, no se deduce** | Cerrado: el coste sale de `total_cost_usd` por delegación |
| F-26 | **El aislamiento del Juez, decidido por corrección, resultó ser la partida más rentable del ciclo.** `SPEC-11` C-4 lo aisló para que su desempate no fuera un eco. Al medirlo: su directorio no tiene `CLAUDE.md`, así que crea **2.562** tokens de caché en vez de los **8.434** del Escritor, y cuesta **cuatro veces menos**. **El aislamiento no es solo rigor, es economía**, y eso lo hace mucho más fácil de defender el día que alguien quiera quitarlo para simplificar | — |
| F-27 | **El transporte impuso a todos la forma del Escritor, y el ciclo informó éxito habiendo tirado el juicio.** `_normalizar` devolvía `{texto, delta}`, que es el contrato del Escritor: el veredicto del Juez salió `None` y los `hechos_clave` del Resumidor se perdieron, **sin que nada fallara**. Es una instancia de la **Regla 4** —la forma de una respuesta es cosa del contrato de su agente, no del transporte— y del peor tipo: **un fallo que no produce error**. Lo destapó leer una salida, no una prueba | Cerrado: el transporte entrega lo que llegó y cada contrato impone su forma |
| F-23 | **Un módulo sin prueba no está verificado: está sin ejecutar.** `prompt.py` se quedó con un error de sintaxis y la suite siguió en verde, porque ningún test lo importaba. Lo cubre `VER-63`, y su primera versión **marcó cinco módulos que sí se ejecutan** —los de `features/brief/` entran por `app.main` cuando una prueba levanta el cliente HTTP—: hubo que cambiarla de *importación directa* a **alcanzabilidad transitiva**. Marcar código que se ejecuta es cobertura falsa al revés, y enseña a ignorar al validador | Cerrado por `VER-63` |
| F-21 | **El delta volvió en prosa, y `INV-03` lo denunció como otra cosa.** En la primera generación real el modelo devolvió `{"sujeto": "casa familiar", "hecho": "el reloj de pared funciona sin que nadie le haya dado cuerda"}` — **frases, no identificadores**. `INV-03` levantó tres `bloqueante` diciendo que un personaje actúa sobre un hecho que no conoce, y la causa real era que **el delta no usó referencias**. La puerta no puede distinguir una violación de continuidad de una violación de contrato: las dos producen el mismo hallazgo, con la severidad de la primera. `SPEC-03` decidió "referencias, no prosa" para el dominio y **nadie lo hace cumplir en la frontera del contrato** | Abierto |
| F-22 | **Una delegación usa más de un modelo, y ninguno se llama como el declarado.** `modelUsage` de la ejecución devolvió `["claude-fable-5-1", "claude-haiku-4-5-20251001"]` para una sola llamada con `--model fable`. `VER-62` compara un valor contra un valor: tal como está marcaría **todas** las delegaciones. Hay que decidir qué significa "el modelo fijo" cuando el transporte devuelve un **conjunto** | Abierto |
| F-20 | **`omitClaudeMd` se ignora en silencio, y era el mecanismo del aislamiento del Juez.** Comprobado con un control en la versión **2.1.274**, por encima de la mínima que pedía el hallazgo 3: con la opción a `false` el agente enumeró los seis niveles del presupuesto de contexto, exactos; **con la opción a `true`, exactamente lo mismo**. Y el cuerpo del agente **sí** se aplica —un marcador arbitrario apareció—, así que la definición se carga y es la opción la que no hace nada. Lo que sí aísla es el **directorio de trabajo**: la misma pregunta desde un directorio vacío devolvió *"NO LO SE"* | Cerrado: el transporte acepta `cwd` y el Juez se lanza desde un directorio sin `CLAUDE.md`. **Y no basta con eso**: el agente aislado ofreció ir a buscar el fichero, así que el aislamiento es directorio vacío **más** ninguna herramienta que lea el proyecto |
| F-19 | **Una enumeración que hereda de `str` hace que casi todo funcione, y por eso el fallo se esconde.** `"mayor" == Severidad.MAYOR` es cierto, `"mayor" in {Severidad.MAYOR}` es cierto y hasta los hashes coinciden — **solo `is` falla**. El repositorio devolvía severidades como cadenas y `impide_cerrar_el_capitulo` comparaba con `is`: **un `mayor` leído de la base no bloqueaba el cierre de capítulo**, y la puerta quedaba abierta sin que nada fallara. Las pruebas unitarias de la puerta no lo veían porque le pasaban miembros de la enumeración directamente; lo destapó la primera prueba que recorrió el camino entero, de la base al endpoint | Cerrado: **la deserialización es la frontera de validación de la base**, igual que Pydantic lo es de la API. Un dato que entra al dominio entra con el tipo del dominio |
| F-17 | **La primera vez que un validador encontró algo que nadie había visto.** El bucle de generación se escribió en `features/generacion/` e importaba de otras tres features, violando `A-02`. Lo cazó el comprobador de importaciones cruzadas de `B1`, **el más barato del proyecto**: diez líneas que recorren `features/`. Todos los hallazgos anteriores de esta sesión salieron de **cruzar documentos a mano**. Es el argumento de que una regla que nunca ha fallado en las pruebas no está verificada, sino declarada: **esa ya falló, contra su propio autor, y desde entonces vale** | Cerrado: el bucle vive en `features/orquestacion/`, la única autorizada a componer |
| F-18 | **Un doble que solo sabe portarse bien pasa contra sí mismo.** Es la Regla 3 aplicada a las pruebas: si el doble no puede producir lo que el real produce mal —un delta fuera de esquema, una respuesta muda, un texto corto—, el bucle pasa contra el doble y falla contra el proveedor, y la prueba habrá verificado su propia idea del mundo. El doble de `PLAN-01` E2 lleva un guion de comportamientos, y cuatro de los seis son malos | Cerrado por convención |
| F-16 | **`MF-24` se materializó en `VER-45`, y lo hizo al revés de como lo esperábamos.** No fue un criterio que remitiera a algo inexistente, sino una **exención** que lo era: *"se eximen las de la columna «Dónde vive», que son planes por construcción"*. La exención era cierta —`features/` y `commons/` no existen en la raíz— y la conclusión no: acabó tapando diecisiete rutas mal escritas que el validador existe para cazar, incluidas las nueve de `F-2`. Un criterio que no puede marcar nada está verde por construcción, y eso no se distingue de estar verde por mérito | Cerrado: la exención se eliminó y `VER-45` resuelve contra la raíz declarada en `docs/architecture.md` |

---

## Orden de implantación

Ordenado por **hueco que tapa**, no por facilidad. Un validador barato que no
tapa nada nuevo no va primero por ser barato.

| # | Qué | Por qué en ese puesto |
| --- | --- | --- |
| **1** | **`VER-38`** — clasificación de invariantes contra la tabla | Es el validador que **valida a otros tres**: hoy `VER-11`, `VER-12` y `VER-22` pueden estar los tres en verde con una severidad mal puesta, que es justo el anti-patrón *"bajar una bloqueante a mayor para desatascar"* de la skill `harness-invariantes`. Parsear una tabla y comparar dos diccionarios |
| **2** | **Las cuatro modificaciones**: `VER-01` bidireccional, `VER-02` con cardinalidad, `VER-22` con mutación de verdad, `VER-30` de inspección a regresión | Rinden más que las altas y cuestan menos: es el mismo comprobador recorrido en los dos sentidos, un conteo añadido, un criterio reescrito y un corpus movido a CI |
| **3** | **`VER-39`** — menciones del delta en el texto | Ataca el hueco más grande, que nada contraste el delta con su origen, y es el **único validador determinista que lee el texto generado** |
| **4** | **`VER-41`** — reconciliación de trazas | Barato, cierra el punto ciego de `VER-24` y **desbloquea `VER-34`, `VER-36` y `VER-37`**, que hoy medirían sobre un dato sin validar |
| **5** | **`VER-40`** y **`VER-42`** | Los dos fallos silenciosos: el agente que devuelve vacío y el delta que se aplica a medias |
| **6** | **`VER-43`** | Necesita un tope que hay que medir antes de fijar; la maquinaria se puede escribir ya |
| **7** | **`VER-10`, `VER-11`, `VER-28`** — máquina de estados y puertas | Siguen siendo lo que corta la propagación del error, pero después de `VER-38`, que es quien garantiza que las severidades sobre las que operan son las correctas |
| **8** | **`VER-13`…`VER-17`, `VER-23`** — comprobadores de estructura | Baratos y se escriben una vez, pero **comparten `PC-1` entre todos**: implantarlos pronto da una sensación de cobertura que no se corresponde con los huecos que quedan |
| **9** | **`VER-02`, `VER-20`, `VER-27`** — contratos de datos | Baratos y cazan mucho |
| **10** | **`VER-05`, `VER-06`, `VER-07`, `VER-09`** — presupuesto y deltas | Donde vive el riesgo de que el sistema no escale. `VER-05` depende de `VER-41` para tener referencia externa |
| **11** | **`VER-03`, `VER-04`, `VER-24`** — trabajos asíncronos y trazas | — |
| **12** | **`VER-22`, `VER-26`, `VER-30`** — mutación, evals y red-teaming | Los más caros, y sin sentido hasta que haya código que probar |

---

## La obra de diez capítulos

> **Cero hallazgos en 41 escenas no es un resultado de calidad: es la firma de validadores
> leyendo entradas vacías.**

Es la lección más cara que ha pagado este proyecto y se lee aquí, al principio de la sección
que describe esa ejecución, porque quien venga a interpretar sus números tiene que verla antes
que los números.

**Y hay tres mecanismos distintos detrás, no uno.** Importa separarlos porque cada uno se
arregla por su lado, y quien solo recuerde *«aquel cero no valía»* no sabrá cuál vigilar:

| Mecanismo | Qué se midió | Qué lo arregla |
| --- | --- | --- |
| **Los hechos colapsaron bajo una sola obra** | Los diez `HechoCanonico` estaban **todos** bajo `obra=cap-10`, con escenas de establecimiento de `cap-01` a `cap-05`. Como `hechos_declarados` filtra por obra, los siete capítulos que llegaron a correr generaron con *«hechos: (ninguno)»*. `INV-03` compara las `acciones` del delta contra los hechos declarados: sin hechos en el prompt no hay acciones sobre hechos, y sin acciones no hay nada que comprobar | La clave `(obra, id)` de `F-39`, **y** dejar de modelar diez capítulos como diez obras (`F-56`) |
| **El mundo estaba compartido y casi vacío** | `conocimiento`, `entidad`, `lugar` y `hallazgo` **no tienen columna `obra`**: eran globales y podían contradecirse. No lo hicieron porque no había con qué. **Dos entidades** en toda la novela —`per-marta` y `per-ana`—, las dos `vivo` y las dos en la cocina. `INV-02` exige que los presentes estén vivos y sean accesibles: contra eso no puede fallar | Un brief que siembre un mundo con material, y declararlo como parte de la configuración en vez de dentro del guion |
| **Los resúmenes barrían por `orden` entre capítulos** | `resumen` no tenía columna `obra` y `resumenes_hasta` filtraba solo por `orden`, que se repite en cada capítulo: 6 resúmenes con `orden=1`, 7 con `orden=3`. La escena 4 de un capítulo recibía las escenas 1–3 de **todos** los capítulos escritos hasta ese momento, ordenadas por número de escena y no por la historia | `F-40` —la columna `obra`— y `F-45` —ordenar por `t_discurso`, que numera la obra entera— |

**No eran diez islas que no se tocaban.** Esa era la primera lectura y es falsa: el estado del
mundo estaba compartido. Lo cierto es peor y más simple — **podía contradecirse y no tenía con
qué**.

Conviene añadir que esa ejecución **no llegó al final**: 41 escenas consolidadas de 60, hasta
el capítulo 7, con 19 aún `planificada`. Cualquier cifra suya es un suelo por partida doble.


**Es la primera ejecución con longitud, y por eso es la primera que puede contestar lo que
las anteriores no.** Sesenta escenas en diez capítulos, con el estado del mundo compartido
entre ellos, `SPEC-15` a `SPEC-20` aplicadas y la reanudación de `F-38` en uso real.

Qué mide, y por qué ninguna de las anteriores podía medirlo:

| Fila | Qué se sabía | Qué añade la longitud |
| --- | --- | --- |
| `VER-37` | Cinco medidas y las cinco de sobra, ninguna con más de tres escenas | Si el contexto **se estanca o sigue creciendo**: las condensaciones y las fichas se acumulan por escena, así que el crecimiento solo se ve con decenas |
| `VER-64` | ~29% con `n=7` | Una frecuencia con `n=60`, y sobre escenas que **no son todas primeras escenas de capítulo** |
| `F-38` | Probado contra dobles | Si la reanudación aguanta cuando lo que se salta son diez capítulos y no dos escenas |
| `VER-36` | Coste por escena de dos ejecuciones cortas | El coste de una obra, que es la cifra que decide si esto es viable |

### Lo que la repetición tiene que contestar

La primera ejecución no pudo contestar nada de esto, y no por poco: le faltaban las tablas.
La repetición se lanza con **todo puesto** —migraciones al día, procedencia con commit y
huella de configuración, deltas persistidos, capítulos de verdad y un brief que siembre
material— y tiene que salir de ella con estas cuatro respuestas:

| Pregunta | Con qué se contesta | Qué decide |
| --- | --- | --- |
| **¿El contexto crece entre capítulos?** | Las medidas por escena que el guion ya acumula, agrupadas por capítulo. Lo que hay que mirar es si la primera escena de cada capítulo arranca donde acabó el anterior o vuelve a empezar | `VER-37`. Y ahora sí se puede: hasta `F-45`, los resúmenes no cruzaban el corte, así que un estancamiento entre capítulos habría sido del filtro y no del sistema |
| **¿Cuántas `acciones` se declaran en toda la obra?** | `delta_de_escena`, que ya se persiste, contando el campo `acciones` | Si el cero de `INV-03` significa algo. **Cero acciones y cero bloqueos es `F-30`; cien acciones y cero bloqueos es una medida.** Sin este número el otro no se puede interpretar |
| **¿Se llena `evento_cronologico`?** | La tabla, tras la obra: cuántos eventos, cuántos con `t_fabula` legible, cuántos participantes `presente` | Si la verificación formal puede correr sobre una obra real. Hoy `generar_lean.py` muere con `no such table`, que es el comportamiento correcto pero deja las cuatro invariantes sin ejercitar |
| **¿Cuánto arrastra un cambio, por hecho?** | `consultas.capitulos_a_regenerar` y `arrastre_de_incluir_mencion`, sobre los diez hechos | La elección entre `S-1` y `S-2` de `SPEC-23`, y de paso si `menciona` entra en el conjunto de la regeneración, que `SPEC-21` C-2 dejó **pendiente de medida** |

**Y la ejecución queda registrada con su commit y su huella de configuración**, que es lo que
`MF-27` existe para exigir: un artefacto que no dice con qué código y con qué configuración se
produjo es indistinguible de uno producido por los de ahora. `procedencia.registrar()` guarda
las dos mitades, y **las dos hacen falta**: el mismo código con otro brief produce otra obra, y
comparar las dos tandas sin saberlo sería comparar dos cosas distintas creyendo que son la
misma.

**El desatasco automático solo dice cosas del contrato.** Cuando un capítulo se para, el
guion reintenta **una vez** con una instrucción que recuerda la diferencia entre revelar y
actuar (`SPEC-16`). **No inventa revelaciones ni toca el estado**: si un personaje sabe algo
que no debería, eso es una decisión sobre la ficción y la toma una persona. Que la
intervención automática esté acotada a lo que es materia de contrato es lo que permite que
`VER-64` siga midiendo algo — si el guion desatascara decidiendo sobre la novela, estaría
contando paradas de un sistema que él mismo va corrigiendo.

## Decisiones abiertas

**Dos etiquetas, y la diferencia importa.** Las decisiones marcadas **«se contesta sola»**
esperan un dato y el dato las resuelve. Las marcadas **«deja de ser ciega»** esperan un dato
que las informa y **no las decide**: seguirán necesitando que alguien elija, solo que ya no
a oscuras. Estaban todas escritas en el mismo tono, y eso prometía que ejecutar las
resolvería todas — el día que llegue la primera traza, alguien miraría las segundas
esperando una respuesta que el dato no da.

- [ ] **Si el reparto por niveles describe una obra que todavía no existe.** `CLAUDE.md`
      reparte 15.000 tokens al bloque inmutable, 25.000 al local y 20.000 al recuperado. El
      contexto **medido** de una escena real es de **1.339 tokens**, y en cinco ejecuciones
      **no se ha recortado ni una vez**. Eso significa que todo el orden de recorte de
      `SPEC-12` —las siete formas reducidas, los bloques irreducibles, `RF-26`— **se diseñó
      para un problema que nunca se ha dado**. No está mal: está **sin ejercitar**, y un
      mecanismo que nunca se ejerce **no está verificado**, solo declarado — que es
      exactamente lo que este documento dice de las invariantes. Las pruebas lo ejercitan con
      techos artificiales, y eso comprueba que el algoritmo funciona, no que el reparto sea
      el correcto. La decisión tiene dos salidas y hay que elegir una: **o las cifras se
      ajustan a lo que se mide**, y entonces el recorte empieza a ejercerse de verdad, **o se
      declara que describen una obra larga que todavía no existe**, y entonces queda escrito
      que el mecanismo está en reserva y nadie debe leer su verde como cobertura. sigue siendo de alguien.

      **Y hay una consecuencia que no es de presupuesto sino de diseño, medida por la sesión
      de `SPEC-23`:** el único criterio de relevancia de hechos que el proyecto tiene escrito
      vive en la **forma reducida** del bloque de estado, es decir **en el camino del
      recorte**. Con el contexto real a 1.300 tokens de un techo de 100.000, ese camino **no
      se ejecuta nunca**, así que el criterio está escrito y no se aplica jamás: el conjunto
      de hechos que el modelo ve sale **constante**, y cualquier medida de *«qué fracción de
      la obra tenía este hecho delante»* da 1,0 por construcción. Es la **Regla 8 a escala
      de diseño**: no una rama que no se ejecuta, sino un **mecanismo entero declarado y no
      ejercido**, y con la misma apariencia desde fuera —todo en verde—. Esto sube el precio
      de dejar las cifras como están: no solo el recorte queda sin verificar, también
      **cualquier pregunta que dependa de qué vio el modelo pierde resolución**.
- [ ] **Qué comprobador de importaciones** se usa para `VER-13`, `VER-14` y `VER-15`.
- [ ] **Qué herramienta de FSD** cierra `VER-17`.
- [ ] **Qué conjunto de escenas** sirve de corpus para los evals de `VER-26`.
- [ ] **Qué defecto planta el canario** de `VER-40`, y cada cuánto se cambia para
      que el Juez no acabe acertándolo por memorización.
- [ ] **Quién genera prompts nuevos** para el corpus de `VER-30` y cada cuánto.
- [ ] **Dónde viven las trazas** de `VER-24` y quién las mira. **Deja de ser ciega, pero no se contesta sola**: el dato informa, no decide.
- [ ] **El tope de ciclos de regeneración** de `VER-43`. Sale de observar cuántas
      veces regenera de verdad una escena problemática. **Se contesta sola** con una traza real.
- [ ] **Umbrales de `INV-15` e `INV-16`** (`VER-32`, `VER-33`). Es la misma
      decisión "Umbrales" de `docs/definitions.md`: se cierran las tres a la vez. **Se contesta sola** con una traza real.
- [ ] **Coste por escena** (`VER-36`) y **suficiencia del reparto por niveles**
      (`VER-37`). Nacen aquí y hay que abrirlas en `docs/architecture.md`. **Se contesta sola** con una traza real.
- [x] ~~Cómo se comprueba que el cambio de valor entregado es el planificado~~
      — **cerrada por `SPEC-03`**: el delta lo declara y la tabla de
      correspondencia lo contrasta.
- [x] ~~Si `hechos_clave` de un `Resumen` son identificadores o texto libre~~
      — **cerrada por `SPEC-03`**: son identificadores.
- [x] ~~Reclasificar `INV-03` de `juez_llm` a `regla` con juez de desempate~~
      — **cerrada por `SPEC-04` C-6**. Conserva la severidad `bloqueante`.
- [ ] **Si la condición de una marca `Caduca con:` puede dejar de ser una ruta.**
      **Dispararon por tercera vez en `PLAN-01` D2**, al crear
      `features/orquestacion/`, y las seis afirmaciones de medidas **siguen siendo
      ciertas**: hay máquina de estados y no hay ni una llamada al modelo.
      **No se han vuelto a reapuntar a propósito**: reapuntar por cuarta vez sería
      seguir fingiendo que una ruta puede expresar lo que no puede. Quedan apuntando a
      `features/orquestacion/` y **caducadas a sabiendas** hasta que esta decisión se
      cierre. Una ruta
      no puede expresar *"existe el dato"*, y siempre hay una carpeta antes que la primera
      medida: las marcas de medidas caducaron dos veces antes de tiempo, en `PLAN-01` A1 y
      B2. La salida sería una condición sobre datos —*"cuando exista una traza con
      `tokens_declarados`"*— y reapuntar solo acerca el proxy. **Se decide cuando haya
      dato**, que es dentro de poco. **Se contesta sola** con una traza real.
- [ ] **Si un resultado de verificación guarda contra qué estado se evaluó.** Es la causa
      de fondo de **`MF-26`**, el verde heredado, y hoy la respuesta es que no: un
      `Hallazgo` cita su invariante y su verificador, y **nada dice en qué mundo se
      levantó**. Sin ese dato, un verde que dejó de valer —porque el estado que lo
      sostenía cambió debajo— **no se puede caducar ni encontrar**: no hay forma de saber
      cuáles habría que revisar, ni siquiera de saber que hay alguno. Es lo que convierte
      a `MF-26` en el único modo de fallo catalogado sin ningún validador que lo mire, y
      no por falta de validador, sino porque **no hay contra qué comprobarlo**.
      Si el resultado llevara el estado contra el que se evaluó, **caducaría solo**: la
      misma forma que las marcas `Caduca con:` —una condición comprobable en lugar de una
      nota que alguien tiene que acordarse de revisar—, y por el mismo motivo, que es que
      nadie se acuerda. Queda **abierta a propósito**: qué identifica un estado, si basta
      con la última escena consolidada o hace falta algo más fino, y qué se hace con un
      verde caducado —invalidarlo, marcarlo o reevaluarlo— son decisiones que no toma la
      spec que lo destapó. **No se contesta sola**: ningún dato la resuelve, porque no es
      una medida que falte sino un atributo que nadie ha decidido tener. Ver `SPEC-23`,
      que la nombra y no la resuelve.
      **Media cerrada por `PLAN-23` `C-3`:** *qué identifica un estado* es la semilla más
      la lista ordenada de deltas aplicados (`EstadoDelMundo.huella`), y una
      `Reverificacion` la guarda; un verde con otra huella **no cuenta** (`sin_reverificar`,
      `D-1`) y se reevalúa volviendo a reverificar. **Sigue abierta la otra mitad para
      `Hallazgo`**, que no tiene huella: qué se hace con los verdes de la primera escritura.
- [ ] **Los umbrales de `VER-48` y `VER-51`.** Las dos series se registran desde
      el primer día; los números salen de mirarlas, como en `VER-32`. **Se contesta sola** con una traza real.
- [x] ~~Bajar `INV-14` a `regla` y `INV-11` a `regla` con juez de desempate~~
      — **cerradas por `SPEC-04` C-4 y C-5**.
