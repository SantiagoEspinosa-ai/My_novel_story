[project]
name = "harness-novela"
version = "0.1.0"
description = "Backend del harness que escribe y verifica novelas largas"
requires-python = ">=3.12"
dependencies = ["fastapi", "pydantic>=2"]

[tool.pytest.ini_options]
testpaths = ["app"]
python_files = ["test_*.py"]
